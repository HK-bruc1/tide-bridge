# BLE 单连接多服务共存与统一广播方案——RDX 私有服务与 HOGP

> 架构名称：Single-Connection Multi-Service GATT Server with Unified Advertising。
>
> 文档定位：基于当前 T2620 RDX + HOGP 代码，取消 CONFIG/HOGP 分时广播和 KEY5 三击切换，改为一个统一广播入口、一套多服务 GATT Profile、最多一条 BLE 连接。

## 1. 结论

本方案可行，并且与当前产品需求更匹配。

目标运行模型如下：

```text
                 统一 connectable legacy advertising
          HID UUID + Keyboard Appearance + RDX 私有数据
                               │
                ┌──────────────┴──────────────┐
                │                             │
          Windows / PC                  手机 BLE App
          以 HOGP 连接                   以 RDX 连接
                │                             │
                └──────────────┬──────────────┘
                               │
                      任一中心连接成功
                               │
                        停止 BLE 广播
                               │
                  同一 GATT Profile 全部可发现
                 RDX 与 HID 按会话能力独立激活
                               │
                           连接断开
                               │
                        恢复统一广播
```

本方案明确不支持手机和 PC 同时保持两条 BLE 连接。工程继续保持：

```c
config_le_hci_connection_num = 1;
config_le_gatt_server_num = 1;
```

这里的“统一入口”不是在广播阶段判断连接方属于 APP 还是 HID，而是让任意中心从同一广播进入同一条连接。连接建立后，根据对端实际访问、订阅和鉴权的服务决定启用哪些能力。

## 2. 当前方案存在的问题

当前代码虽然已经把 RDX Service 和 HID Service 放进同一个 `rdx_profile_data[]`，但运行时仍然人为分成两个互斥身份：

- `rdx_ble_mode_controller.c/.h` 保存 `requested_mode`、`advertised_mode` 和 `connection_owner`；
- CONFIG 广播只放 RDX 名称和私有 Manufacturer Data；
- HOGP 广播放 HID Service UUID、Keyboard Appearance 和名称；
- 连接建立时，根据连接前的广播模式把连接标记为 `RDX_BLE_OWNER_CONFIG` 或 `RDX_BLE_OWNER_HOGP`；
- ATT read/write、RDX notify、HID notify 和配键协议继续按 owner 拒绝另一侧访问；
- KEY5 三击调用 `rdx_ble_mode_request_toggle()`，有连接时还要先断开再切换广播。

这套 owner 模型解决的是“两个广播身份分时复用”，不是 BLE 栈的硬限制。统一入口后，广播身份和连接归属都不再成立，应直接删除，而不是增加第三种 `BOTH` 模式。

## 3. 目标与非目标

### 3.1 目标

1. 上电后始终发送一种统一广播，不需要 KEY5 切换。
2. PC 能根据标准 HID 信息发现并连接键盘。
3. 私有 BLE App 能根据既有 RDX Manufacturer Data 发现并连接设备。
4. 任一中心连接后停止广播，只维持一条 BLE 连接。
5. 同一连接上的 RDX Service 和 HID Service 都保持可发现。
6. RDX 数据通道与 HID 键盘通道各自根据实际使用状态启用。
7. 手机仅连接 RDX 时不主动弹出系统 BLE 配对提示。
8. PC 使用 HID 时完成配对/加密，只有 ready 后才能发送键盘报告。

### 3.2 非目标

- 不支持手机和 PC 双中心并发连接；
- 不增加第二个 `app_ble_hdl`；
- 不增加第二套 GATT Server；
- 不使用 Extended Advertising；
- 不根据远端 MAC、操作系统或连接时序猜测对端类型；
- 不隐藏同一 Profile 中的另一套 Service；
- 不改变现有 HID Report Map、Report 长度和 handle 布局。

## 4. 统一广播设计

### 4.1 广播内容

继续使用一个 `ADV_IND` legacy advertising set。建议字段布局如下：

#### Advertising Data，最多 31 字节

| 字段 | AD Type | 数据长度 | AD 总长度 | 说明 |
|---|---:|---:|---:|---|
| Flags | `0x01` | 1 | 3 | 保留双模产品当前 `0x0A` |
| Complete 16-bit Service UUIDs | `0x03` | 2 | 4 | HID Service `0x1812` |
| Appearance | `0x19` | 2 | 4 | Keyboard `0x03C1` |
| Complete Local Name | `0x09` | 动态 | `2 + N` | 统一设备名称 |

T2620 当前基础名称为 `Beanstalk RKB`，长度 13 字节：

```text
3 + 4 + 4 + (2 + 13) = 26 bytes
```

当前名称还可能附加空格和 4 位认证尾码，最大为 18 字节：

```text
3 + 4 + 4 + (2 + 18) = 31 bytes
```

因此当前产品可以在 legacy advertising 的 31 字节限制内同时容纳 HID 标识和完整动态名称，不需要缩短名称。构包函数仍必须按实际长度检查边界，不能依赖产品宏永远不变。

如果以后产品名称超过 18 字节，按以下顺序降级：

1. 保留 Flags、HID UUID 和 Appearance；
2. 将名称改为 Shortened Local Name `0x08`；
3. 不得删除 HID UUID 来换取完整名称。

#### Scan Response Data，最多 31 字节

继续放置现有 RDX Manufacturer Specific Data `0xFF`，字段和字节序保持不变：

```text
PRODUCT_CODE
BLE MAC
FACTORY_CODE
bound / mode byte
ability
protocol version（按现有编译条件）
PRODUCT_TYPE
"NV"
```

T2620 当前 `PRODUCT_CODE="607"`、`FACTORY_CODE="BEANTK"`，现有私有数据可以继续放入一个扫描响应包。这样私有 App 的扫描过滤逻辑无需因为增加 HID 能力而改变。

需要注意，Manufacturer Data 位于 Scan Response，手机 App 必须使用 active scan 才能读取。这是当前 RDX 广播已经存在的依赖，不是统一入口新增的限制。固件仓库不包含手机 App 源码，实施前必须由 App 侧确认前台扫描、后台扫描和快速重连等场景均会获取 Scan Response；不能仅凭现有固件推断 App 已正确配置 active scan。

### 4.2 统一名称

广播名称和 GAP Device Name 必须保持一致，统一使用 `rdx_ble_server_get_local_name()` 的结果。不要再使用 HOGP 独立名称或按模式切换名称。

建议产品名称继续使用：

```text
Beanstalk RKB [认证尾码]
```

`RDX_HOGP_CUSTOM_NAME` 和 `RDX_HOGP_NAME_SOURCE` 在统一入口模型下不再承担运行时身份切换，可删除或仅保留为其他产品的编译期兼容项。

当前 BLE 改名路径只更新 RAM/VM，没有直接调用 `rdx_ble_server_adv_data_changed()`。统一入口必须明确名称更新策略：

- 无连接时改名：写入成功后立即停止并重建统一广播，使 ADV Name 与 GAP Device Name 同步；
- 已连接时改名：更新 RAM/VM，标记 `adv_refresh_pending`，当前连接断开后使用新名称恢复广播；
- 认证尾码变化：与显式 BLE 改名走同一刷新入口，不允许只改 GAP 名称或只改广播名称；
- 已配对主机可能缓存旧名称，量产验收需区分固件未刷新与主机缓存两类现象。

### 4.3 广播生命周期

统一生命周期只有三条规则：

1. 无连接且未被 DUT、关机、Wi-Fi 传输或格式化等条件抑制时，发送统一广播；
2. 任一 BLE 中心连接成功后，停止广播，不再启动其他身份广播；
3. 当前连接断开且广播未被抑制时，恢复统一广播。

`rdx_ble_server_adv_data_changed()` 只重建统一广播数据，不再根据 HOGP/CONFIG mode 分流。

## 5. 单连接会话模型

### 5.1 删除 connection owner

统一入口后，连接本身不应再具有 `CONFIG` 或 `HOGP` owner。一个连接只保存链路级状态：

```c
typedef struct {
    u16 con_handle;
    u16 mtu_size;
    u8 connected;
    u8 encrypted;
} rdx_ble_link_state_t;
```

在这条链路之上维护两个互不排斥的能力状态：

```c
typedef struct {
    u8 ccc_configured;
    u8 stream_tx_ready;
    u8 protocol_authorized;
} rdx_ble_config_session_t;

typedef struct {
    u8 input_ccc_enabled;
    u8 encrypted;
    u8 suspended;
    u8 ready;
} rdx_ble_hid_session_t;
```

状态关系为：

```text
link.connected
├─ config_session.active = RDX CCC/握手/鉴权达到现有协议要求
└─ hid_session.ready     = HID CCC enabled && encrypted && !suspended
```

两项可以都为 0、单独为 1，也可以在同一条连接上同时为 1。不要再引入 `OWNER_BOTH`，否则 owner 互斥思路仍会渗透到发送、按键和清理路径。

### 5.2 连接建立

`HCI_SUBEVENT_LE_CONNECTION_COMPLETE` 和 `HCI_SUBEVENT_LE_ENHANCED_CONNECTION_COMPLETE` 统一执行：

1. 保存唯一 `con_handle`；
2. 设置 `ble_conn = TRUE`；
3. 初始化链路 MTU 和发送状态；
4. 初始化 RDX session；
5. 调用 `rdx_hogp_on_connected(con_handle)` 初始化 HID session；
6. 不设置 connection owner；
7. 不在连接完成时调用 `sm_api_request_pairing()`；
8. 不重新启动任何广播。

当前 enhanced connection complete 分支没有执行完整 RDX connected 初始化，重构时应与普通 connection complete 收敛到同一个内部 helper，避免两条路径继续漂移。

### 5.3 连接断开

`HCI_EVENT_DISCONNECTION_COMPLETE` 必须使用事件里的原连接句柄完成清理，不能先把本地句柄写成 0 再传给子模块。建议顺序：

1. 从 event packet 解析 `disconnected_handle`；
2. 停止 RDX 发送、同步、强制断开和流相关定时器；
3. 执行完整的 RDX disconnected cleanup；
4. 调用 `rdx_hogp_on_disconnected(disconnected_handle)`；
5. 清除该连接的 CCC runtime config；
6. 清空唯一连接句柄和全部 session 状态；
7. 若未命中广播抑制条件，恢复统一广播。

不再根据 previous owner 选择只清理其中一套模块。

建议把断开顺序收敛为一个内部 helper，伪代码如下：

```c
static void rdx_ble_link_disconnected(u16 disconnected_handle)
{
    rdx_config_session_stop(disconnected_handle);
    rdx_ble_server_disconnected_cleanup_internal();
    rdx_hogp_on_disconnected(disconnected_handle);
    multi_att_clear_ccc_config(disconnected_handle);
    rdx_ble_link_state_reset(disconnected_handle);
    rdx_ble_unified_adv_restart_if_allowed();
}
```

关键约束是先保留并使用事件中的 `disconnected_handle` 完成两套 session 清理，最后再清空全局连接句柄并恢复广播。

## 6. ATT 访问模型

### 6.1 服务发现

保留当前 `rdx_profile_data[]`，其中：

- GAP 和既有 RDX Service 保持原 handle；
- HID Service 保持 `0x0016–0x0025`；
- HID Report Map、HID Information、Input/Output Report 字节契约保持不变。

同一 GATT Server 下，连接方能够发现完整数据库是正常行为。广播只负责帮助中心发现产品能力，不负责隐藏 Service。

### 6.2 owner gate 替换原则

以下判断最终全部取消，但 RDX 相关 gate 只有在 6.3 节的新授权边界实现并验证后才能删除：

```c
rdx_ble_connection_owner_get() != RDX_BLE_OWNER_CONFIG
!rdx_ble_connection_owner_is_hogp()
```

替换原则：

| 路径 | 新准入条件 |
|---|---|
| HID 静态 read | 连接有效；按原 offset/length 读取 |
| HID Control Point / Protocol Mode / Output Report write | 连接有效；必要时检查 HID session |
| HID Input Report notify | `rdx_hogp_keyboard_is_ready()` |
| RDX ATT write | 连接有效；命令解析后按授权策略决定是否执行 |
| RDX notify/indicate | 对应 RDX CCC、发送状态和授权满足要求 |
| HOGP keymap 配置 | RDX 当前会话已授权，再检查产品授权和 busy 状态 |
| OTA / bulk send | RDX session 已建立且对应 CCC ready |

特别注意：广播中包含 HID UUID 不等于任意连接都已获得 HID 输入能力；连接中存在 RDX Service 也不等于任意中心都通过了私有协议授权。

### 6.3 RDX 会话授权是 owner 删除前置项

当前仓库没有找到可直接复用的 `rdx_protocol_is_authorized()` 一类接口。`connection_owner` 仅由连接前的广播模式赋值，不是远端身份认证；`rdx_hogp_keymap_product_authorized()` 的弱实现当前还直接返回 1。因此不能把“删除 owner gate”理解为“换成已有 session 鉴权函数”。

Phase 2 必须先定义并实现一个以当前 `con_handle` 为边界的授权状态，例如：

```c
typedef enum {
    RDX_SESSION_UNAUTHORIZED = 0,
    RDX_SESSION_IDENTIFIED,
    RDX_SESSION_AUTHORIZED,
} rdx_session_auth_state_t;

u8 rdx_protocol_session_is_authorized(u16 con_handle);
```

授权状态必须满足：

1. 每次新连接从 `UNAUTHORIZED` 开始，不能使用 VM bound 状态替代当前远端身份；
2. 只有现有 RDX 协议完成可验证的握手后才能进入 `AUTHORIZED`；
3. 断开、协议重置、鉴权失败和连接句柄变化时立即清零；
4. 所有敏感命令在产生副作用前统一检查，不依赖调用方自觉；
5. `rdx_hogp_keymap_product_authorized()` 只保留产品策略职责，其正式实现必须结合当前 session 授权，默认弱实现不得在量产配置中放行。

实施前需要审计 `rdx_protocol_packet_recv()` 进入的完整命令分发表，至少按以下等级建立白名单：

| 等级 | 示例 | 未授权会话策略 |
|---|---|---|
| 公开只读 | 版本、基础能力、电量 | 产品确认后允许或拒绝 |
| 会话建立 | 认证 SN、挑战/响应、绑定握手 | 仅允许严格校验的握手命令 |
| 普通控制 | RTC、名称、录音控制、文件操作 | 必须授权 |
| 高敏感 | OTA、格式化、恢复出厂、绑定/解绑、keymap 修改 | 必须授权，并保留 busy/完整性检查 |

在该授权 API、状态清理和命令审计落地前，不得删除 RDX owner gate。owner 本身不能提供密码学安全，但它仍是当前的行为隔离；应在新边界可验证后再移除。

### 6.4 CCC 必须按连接句柄处理

当前 HID CCC 已经使用：

```c
multi_att_get_ccc_config(connection_handle, att_handle);
multi_att_set_ccc_config(connection_handle, att_handle, cfg);
```

统一入口后保持这一做法。RDX 侧仍使用 `att_set_ccc_config()` 的路径应核对是否隐含当前连接；即使当前只允许一条连接，也建议所有新的 session 判断都使用 callback 传入的 `connection_handle`，避免以后出现陈旧连接状态。

## 7. HID 配对与加密策略

### 7.1 推荐策略：按 HID 使用延迟配对

当前 `rdx_hogp_on_connected()` 会立即调用：

```c
sm_api_request_pairing(con_handle);
```

统一广播后不能继续这样做，否则手机 BLE App 只要连接 RDX，也可能立即收到系统配对请求，破坏私有 App 体验。

推荐改为：

1. 连接完成时只初始化 HID runtime，不主动请求配对；
2. 对端启用 `HID_INPUT_REPORT_CLIENT_CONFIGURATION_HANDLE` 时记录 `input_ccc_enabled`；
3. 如果链路尚未加密，调用 `sm_api_request_pairing(connection_handle)`；
4. 在 `HCI_EVENT_ENCRYPTION_CHANGE` 成功后设置 `encrypted`；
5. 只有 `input_ccc_enabled && encrypted && !suspended` 时，`rdx_hogp_keyboard_is_ready()` 返回 1；
6. 加密失败、CCC 关闭或断开连接时立即清除 ready，并发送/收敛本地 release 状态。

参考状态转换：

```text
CONNECTED
   │
   ├─ RDX App 写私有特征 ──> RDX session active，不触发 HID 配对
   │
   └─ Host 写 HID Input CCC
          │
          ├─ 已加密 ─────────> HID READY
          │
          └─ 未加密 ─────────> request pairing
                                  │
                                  ├─ encryption success -> HID READY
                                  └─ failure            -> HID NOT READY
```

### 7.2 主机兼容性验证点

Windows、macOS、iOS/iPadOS、Android 对 HOGP 的配对发起时机可能不同。部分主机可能在写 CCC 前主动发起 SM，也可能在发现 HID Service 后先读取 Report Map。实现应允许主机主动配对，并把所有 Encryption Change 都按当前连接句柄更新。

当前全局 SM 配置使用 `IO_CAPABILITY_NO_INPUT_NO_OUTPUT`，同时请求 Bonding、Secure Connections 和 MITM，而 HOGP 配置又声明 Just Works。实现不能仅根据配置位假定最终一定获得 MITM；必须记录目标主机上实际协商的配对方法、加密状态和 bond/重连结果。

如果实机发现某个目标 Windows 版本不会走到 CCC 写入，可采用以下兼容回退，而不是恢复“连接即配对”：

1. 在首次明确的 HID 动态特征访问时触发配对；或
2. 在 HID CCC 写入返回安全错误并触发配对，配对完成后由主机重试。

最终选择以目标 Windows 版本、手机 App 和实际 JL ATT 回调行为的联调结果为准。

日志中若出现失败，必须记录完整事件、阶段和 HCI reason。`0x05` 可表示 Authentication Failure；`0x3E` 通常属于 Connection Failed to be Established/同步超时类原因，不能直接归因为配对失败。

## 8. 物理按键路由

这是统一入口最容易引入产品回归的地方。

当前 `rdx_app_earphone_key_remap()` 使用 `rdx_hogp_keyboard_is_connected()` 决定是否吞掉所有按键。统一入口后，如果它仍等价于“BLE 已连接”，手机 App 一连接，KEY1～KEY5 就会被当作 HID 按键处理，即使手机从未订阅 HID。

必须改成：

```c
if (rdx_hogp_keyboard_is_ready()) {
    // 执行 HID keymap，并消费对应按键事件
} else {
    // 继续走原有本地/离线产品按键路径
}
```

同时删除 KEY5 三击的以下专用路径：

```c
if (num_idx == 4 && index == KEY_ACTION_TRIPLE_CLICK) {
    rdx_ble_mode_request_toggle();
    ...
}
```

删除后 KEY5 三击是恢复到现有离线 key table、改作其他产品功能还是显式忽略，应由产品按键表决定；它不再承担 BLE 身份切换。

HID ready 从 1 变 0 时必须：

- 取消延迟 release timer；
- 发送或至少在本地收敛全零 report；
- 清除 held key 状态；
- 防止旧连接的回调继续影响新连接。

## 9. 代码改造清单

### 9.1 `rdx_ble_server.c`

1. 将 `rdx_ble_server_fill_adv_data()` 改为统一 ADV 构包：Flags + HID UUID + Appearance + Name；
2. 保留 `rdx_ble_server_fill_rsp_data()` 的 RDX Manufacturer Data 格式；
3. 所有启动/重启广播路径只调用统一 `rdx_ble_server_adv_enable(1)`；
4. 删除 CONFIG/HOGP 广播启动、切换、pending apply 和 owner dump 逻辑；
5. 合并普通和 enhanced connection complete 的公共处理；
6. 连接时同时初始化 RDX 与 HOGP runtime，但不主动配对；
7. 断开时同时清理 RDX 与 HOGP runtime，再恢复统一广播；
8. 删除 HID read/write 的 HOGP owner gate；
9. 新授权边界验证通过后，删除 RDX read/write/send/OTA 的 CONFIG owner gate，替换为 session/CCC/授权条件；
10. `rdx_ble_server_adv_data_changed()` 不再分流到 HOGP 广播；
11. DUT 及其他广播抑制路径只操作统一广播。

### 9.2 `rdx_hogp_keyboard.c/.h`

1. 删除或收敛 `s_hogp_mode`、`rdx_hogp_mode_set/get()`；
2. 删除模块内部 HOGP 专用 adv start/stop 和构包职责；
3. `rdx_hogp_on_connected()` 只初始化 session，不立即请求配对；
4. HID CCC enable 时按需触发配对；
5. Encryption Change 按当前 `con_handle` 更新状态；
6. `rdx_hogp_keyboard_is_connected()` 若继续保留，只表示链路存在；
7. 产品按键路由统一使用 `rdx_hogp_keyboard_is_ready()`；
8. report send 继续要求 CCC、加密、非 suspend 和当前连接有效；
9. 广播构包 helper 可迁入 `rdx_ble_server.c`，或改成只向统一构包器追加 HID AD structures。

### 9.3 `rdx_ble_mode_controller.c/.h`

统一入口落稳后删除整个模块，并从 `SDK/Makefile` 移除源文件。不要把它改造成只有一个 mode 的空壳。

### 9.4 `rdx_app.c`

1. 删除 KEY5 三击 `rdx_ble_mode_request_toggle()`；
2. 按键 HID 分流条件由 `is_connected()` 改为 `is_ready()`；
3. 保留 HOGP ready 时对未实现 action 的消费规则，防止事件泄漏到离线功能；
4. HOGP not ready 时恢复原产品按键路径。

### 9.5 `rdx_hogp_keymap_config.c`

将 `RDX_BLE_OWNER_CONFIG` 授权判断替换为当前 RDX session 授权状态。`rdx_hogp_keymap_product_authorized()` 仍只负责产品层授权，不应被用作当前远端身份认证的替代品；当前默认返回 1 的弱实现必须由量产实现覆盖，或改为默认拒绝。

### 9.6 `rdx_dut.c`

删除进入/退出 DUT 时的 `rdx_ble_mode_request_hogp(0)`。进入 DUT 关闭统一广播并按原逻辑断开；退出 DUT 重建并恢复统一广播。

### 9.7 配置与构建文件

- `t2620_project_config.h`：删除 `RDX_BLE_DEFAULT_MODE`；
- `rdx_hogp_config.h`：保留加密要求、Report 和 Appearance 配置，删除不再使用的独立名称/模式配置；
- `SDK/Makefile`：删除 `rdx_ble_mode_controller.c`；
- 保持 `config_le_hci_connection_num = 1`；
- 保持 `config_le_gatt_server_num = 1`。

### 9.8 测试与关联文档

`test_hogp_profile_contract.ps1` 当前冻结了模式切换、owner、KEY5 三击和双广播行为，需要整体更新为统一入口契约，而不是简单删除失败断言。

`test_hogp_keymap_architecture.ps1` 中对 CONFIG owner 的依赖需要替换为 RDX session 授权检查。

`docs/1.HFP麦克风模块化实施方案.md` 中“KEY5 三击继续切换 HOGP/Config”的描述会过期，应在实现阶段同步修改，避免后续 HFP/PTT 开发重新引入模式切换假设。

## 10. 推荐实施阶段

### Phase 1：统一广播

- 合并 HID 和 RDX 广播字段；
- 固定单一名称和单一广播重启入口；
- 确认手机 App 使用 active scan，并覆盖前台、后台和重连场景；
- 明确无连接立即刷新、已连接延迟到断连刷新的名称更新策略；
- 增加 host test，冻结字段顺序、UUID、Appearance、RDX Manufacturer Data 和 31 字节上限；
- 用 Windows 扫描确认显示为键盘，用手机 App 确认仍可识别设备。

### Phase 2：建立授权边界并去除模式与 owner

- 定义按当前 `con_handle` 保存和清理的 RDX session 授权状态；
- 审计完整 RDX 命令分发表，对格式化、恢复出厂、绑定/解绑、OTA、文件和 keymap 等敏感操作增加统一授权 gate；
- 先用 host test 和定向行为测试证明未授权命令无副作用；
- 合并连接/断开生命周期；
- 删除 mode controller、KEY5 三击切换和 DUT mode request；
- 在新授权边界生效后删除 ATT owner gate；
- 用链路状态、CCC 和 session 授权替代 owner；
- Phase 2 与 Phase 3 分开提交和验证，不同时改变配对触发时机。

### Phase 3：延迟配对与 HID ready

- 连接时不主动配对；
- HID CCC/Encryption Change 驱动 ready；
- 手机 App 连接不弹配对；
- PC 连接完成配对并可持续发送键盘报告。

### Phase 4：清理与回归

- 删除不再使用的专用 HOGP advertising API；
- 更新 host tests 和关联文档；
- 完整 host test、固件 build 和实机矩阵验证；
- 验收完成后删除 `TCFG_RDX_HOGP_UNIFIED_ENTRY_ENABLE`、旧分时广播实现和兼容分支，不长期维护双状态机。

每个 Phase 都应保持固件可构建、现有 RDX 基础功能可验证，不建议一次提交同时删除状态机、改变配对和重写广播后再集中联调。

## 11. 验收标准

### 11.1 广播

- 上电不按 KEY5 即可同时被 Windows 键盘发现流程和私有 App 扫描发现；
- ADV 包包含 `0x1812` 和 `0x03C1`；
- Scan Response 保留原 RDX Manufacturer Data；
- ADV 和 Scan Response 各自不超过 31 字节；
- 设备只显示一个统一名称；
- 任一连接建立后设备不再可被第二个中心连接；
- 断开后恢复同一种统一广播。

### 11.2 手机 App 路径

- App 前台、后台和快速重连场景均能通过 active scan 获取 RDX Scan Response；
- 手机连接后不因固件主动请求而立即弹出系统配对；
- RDX CCC、命令、状态同步、录音、文件传输和 OTA 行为不回归；
- 手机未启用 HID 时，物理按键继续走产品原有路径，不被 HOGP 吞掉；
- 每条新连接的 RDX session 从未授权开始，旧连接授权不得继承；
- 未授权会话不能执行名称修改、录音控制、文件操作、OTA、格式化、恢复出厂、绑定/解绑和 HOGP keymap 修改；
- 允许未授权访问的只读/握手命令必须有明确白名单。

### 11.3 PC HID 路径

- Windows 能发现 HID Service、读取 Report Map 并完成配对；
- HID Input CCC 打开且链路加密成功后 ready；
- KEY1～KEY5 按当前 keymap 发送 8-byte Input Report；
- CCC 未打开、未加密、suspend 或断开时禁止发送；
- 断开、配对失败或取消订阅时按键状态收敛为 release；
- 重连后 CCC、加密和 ready 状态不会错误继承旧连接 runtime。

### 11.4 主机实机矩阵

自动化 PowerShell 文本检查不能证明配对时序、主机缓存和空口行为。Phase 3/4 至少记录以下矩阵的设备型号、操作系统版本、蓝牙主控/驱动、首次配对和重连日志：

| 主机类别 | 最低覆盖 | 验证内容 |
|---|---|---|
| Windows | Win10、Win11；至少 2～3 种不同蓝牙主控 | 发现、首次配对、删除 bond 后重配、自动回连、输入、Caps Lock Output Report、断连 reason |
| Apple（若为产品目标） | macOS；需要移动端支持时增加 iOS/iPadOS | 发现、配对方法、输入、重连、系统软键盘/物理键盘行为 |
| Android（若为产品目标） | 至少 2 个厂商版本 | HOGP 配对、输入、重连，以及普通 RDX App 连接不主动弹配对 |
| 私有 App | 正式发布版本 | active scan、RDX 命令、OTA、未授权拒绝、手机连接时按键不被吞 |

每个主机至少覆盖：主机先发起配对、固件因明确 HID 访问请求配对、已有 bond 重连、配对取消/失败、连接中关闭 CCC、异常断开六类路径。失败时记录 ATT 操作顺序、SM event、Encryption Change 和 HCI reason，不能只记录“连接失败”。

### 11.5 单连接边界

- PC 已连接时手机不能再建立第二条 BLE 连接；
- 手机已连接时 PC 不能再建立第二条 BLE 连接；
- `config_le_hci_connection_num` 和 `config_le_gatt_server_num` 均保持 1；
- 代码中不存在第二个 RDX/HOGP `app_ble_hdl`。

## 12. 风险与回退

| 风险 | 影响 | 应对 |
|---|---|---|
| Windows 配对发生在预期 CCC 时机之外 | HID 无法进入 ready | 抓 HCI/ATT 日志，改为首次明确 HID 访问触发，不恢复全连接强制配对 |
| SM 配置与实际配对方法不一致 | 错误假定已获得 MITM | 记录实际配对方法、加密和 bond 状态；安全策略以协商结果为准 |
| App 未执行 active scan | 无法获取 RDX Manufacturer Data | 作为既有依赖由 App 侧确认前台、后台和重连扫描策略 |
| 手机通用 GATT 浏览器误开 HID CCC | 手机出现配对提示 | 这是明确 HID 使用行为，可接受；普通私有 App 不访问 HID CCC |
| 手机连接导致按键被吞 | 产品功能失效 | 按键只看 `rdx_hogp_keyboard_is_ready()`，禁止只看 BLE connected |
| 在新授权边界建立前删除 owner gate | 私有敏感命令暴露 | 先实现 session 授权和命令白名单，再删除 owner；默认拒绝 keymap 修改 |
| 动态名称增长导致 ADV 超长 | 广播启动失败或字段截断 | 构包逐项检查，名称超过 18 字节自动使用 shortened name |
| 改名后未刷新统一广播 | ADV Name 与 GAP Device Name 不一致 | 无连接立即重建广播；已连接设置 pending 并在断连后刷新 |
| 断连清理仍按旧 owner 分支 | timer、按键或流状态残留 | 无条件同时清理两套 session，并增加断连回归测试 |
| 历史 bond 与 Service Changed 缓存 | PC 使用旧 GATT 缓存 | 开发阶段删除主机 bond；若量产 Profile handle 不变则不主动改变数据库版本 |

回退开关只允许保留在 Phase 1～3 开发阶段，例如 `TCFG_RDX_HOGP_UNIFIED_ENTRY_ENABLE`。Phase 4 验收项必须包含删除该开关、旧分时实现和兼容测试，避免长期维护两套状态机。

## 13. 最终建议

采用“单 BLE 连接、私有广播与 HID 广播统一入口”作为正式产品模型：

- 广播统一，不再让用户理解或操作 BLE 身份；
- Profile 统一，沿用当前一个 Server、一套 handle；
- 连接唯一，符合当前 RAM、栈配置和产品需求；
- 能力按实际协议行为激活，而不是按广播身份互斥；
- 手机路径以 RDX session 为准，PC 路径以 HID CCC + Encryption 为准；
- KEY5 不再承担模式切换。

这比在现有模式控制器中增加例外条件更简单，也更符合 BLE/GATT 的能力模型。实施工作的主要难点不在广播合并，而在删除 owner 假设后，把配对、按键路由、授权和断连清理改成真正的 session 状态。
