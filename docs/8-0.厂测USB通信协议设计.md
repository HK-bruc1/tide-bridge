# T2620 厂测 USB CDC ACM 通信协议设计（运行时分流版）

> 更新日期：2026-09-17
> 边界确认：CDC 仅供产测，正常用户模式不枚举。阶段 1 的 DUT 门控基础回归已通过；本轮推进阶段 2 字节传输，不开放厂测命令。
> 状态：基础枚举及退出已获用户确认，本轮收束测试包见第 8.6 节。背压、多轮稳定性及两个新增异常尚未闭环；完整厂测协议仍待实施，不能作为已交付能力说明。
> 推进方式：按“串口识别 → 双向收发与接收回调 → 厂测协议接入”逐阶段验收。用户确认串口可见后已授权推进阶段 2 实施；阶段 1A/1B 未完整验收的项目继续保留，不据此跳过验收门槛。阶段 2 已实现字节收发、独立回显和 PC 脚本，软件检查与固件构建已通过，日志版本已通过 hello 与脚本收发板测，本轮架构收束仍待实机回归；阶段 3 未实施。阶段 1 历史记录见第 8.6 节，阶段 2 见第 8.7 节。
> 适用工程：T2620 / AC701N（BR28），RDX + HOGP 双 Peripheral ACL，SD0 整盘 MSC 导出。
> 基线：沿用 [doc 7](7.T2620_U盘与BLE及充电共存可行性评估与实施方案.md) 的 SD 所有权与拨码分流规则。量产目标同时编译 MSC 与 CDC，运行时选择一个 USB 数据功能。

> 文档拆分：阶段三详见 [8-1.产测USB_CDC接入BLE产测流程.md](8-1.产测USB_CDC接入BLE产测流程.md)；本次仅整理文档，实施与验收状态不变。

## 1. 范围与现有代码边界

CDC 仅提供字节传输，不直接访问 SD；但通过 CDC 发起的录音、格式化和整包结束会访问 SD，仍须经过已有业务与存储准入检查。

当前仓库已有 CDC 类驱动、后台枚举入口、DUT 动作和拨码/MSC 切换逻辑；阶段 1A 新增厂测 CDC 开关和 `usb_factory_*` 枚举协调接口，实现在 `device/usb/device/task_pc.c`，由拨码的 app_core 定时服务发布准入，USB 任务执行类切换。阶段 2 新增 `usb_factory_cdc.c/h` 字节传输层与 USB 任务消息，实施状态见第 8.7 节；厂测命令消息、身份查询和 DUT 公共接口仍为阶段 3 要求。

| 已有代码 | 源码事实 | 对设计的约束 |
|---|---|---|
| `device/usb/device/task_pc.c` | OTG 插入时可经 standby 启动 CDC，返回 0 可抑制应用 IN 事件 | 能避免插线进入 PC，但不能独自覆盖带线拨码切换 |
| `device/usb/usb_task.c` | START 先 stop；PAUSE 后可直接启动 CDC；CDC_BACKGROUND 直接启动 CDC | 所有启动入口须统一门控，不能只改 standby |
| `device/usb/device/cdc.c` | 收发共用 mutex；write 按包循环并返回累计 offset；release 释放全局句柄 | 须核实发送计数、短写、等待时间和收发/释放并发 |
| `rdx_protocol/rdx_dip_switch.c` | ON→OFF 等待 OTA/格式化/DUT，再排空业务并受控复位；OFF→ON 请求退出 PC | 保留当前保护，CDC 不绕过排空、复位和 SD 恢复 |
| `rdx_protocol/rdx_dut.c` | BLE 入口返回 void，异步投递；进入 DUT 会断开 BLE、关闭广播 | 不能仅更名入口就获得可靠 USB 命令接口 |
| `rdx_protocol/rdx_vm.c` | 初始化已有 AuthKey 打印 | “AUTH 不记日志”需要同时整改已有日志 |

表中 `device/usb/` 与 `rdx_protocol/` 分别位于 `SDK/apps/common/` 和 `SDK/apps/common/third_party_profile/`。

阶段 1A 的 factory 分支不注册应用收发消费者，bulk OUT 配置为不触发接收中断、不读取端点数据，等待硬件缓冲耗尽后背压；实际 NAK、主机写超时及总线复位后行为须板测。停止时先屏蔽设备中断源再释放资源，并回收 CDC mutex；该阶段约束不能替代阶段 2 的在途 I/O 与会话同步。

## 2. 运行时分流与业务边界

### 2.1 稳态分流

| 拨码/OTG 条件 | USB 数据功能 | BLE/正常业务 | 充电 |
|---|---|---|---|
| OFF + 电脑，从机角色确认且安全切换完成 | 仅 MSC，APP_MODE_PC | 关闭，SD0 由电脑独占 | 保持 |
| ON + 电脑，未进入 DUT | 不枚举 USB 数据类，不进入 APP_MODE_PC | 正常开机业务保持 | 保持 |
| ON + 电脑，按键进入 DUT 且准入通过 | 仅 CDC ACM，不进入 APP_MODE_PC | BLE/SPP 厂测通道，见 2.2 | 保持 |
| 充电器，仅 VBUS、无从机角色 | 不枚举数据类 | 按现有拨码和充电策略 | 保持 |
| OFF + 无 USB | 无 | 正常关机 | — |
| ON + 无 USB | 无 | 正常 BLE 业务 | — |

这是稳态目标；切换期间允许暂时无 USB 数据功能，不允许 MSC/CDC 复合枚举或 SD 双重所有权。

正常 CDC 启动必要条件为 `ON && factory_dut_active && get_charge_online_flag() && usb_otg_online(0)==SLAVE_MODE`（`factory_dut_active` 表示按键流程已完成进入厂测模式的逻辑状态，具体接口实施时确定）。该组合表示本工程已确认 USB 主机角色，不是主机操作系统或厂测站身份认证。还必须满足：PC 已完成退出、SD 已归还固件并恢复就绪、不在关机/切换失败/排空状态。角色未确认时保持安全充电状态，通过后续协调重试，不因一次判断失败永久漏启动。阶段 3 的受限 CDC 按第 4.2 节处理：DUT 等待退出是唯一的 OFF 重连例外，厂测失败恢复另允许 ON 下不依赖 SD 就绪的受限重连。

### 2.2 CDC 与厂测模式的边界

CDC 仅用于产测，对正常用户模式不可见：开机但未进入 DUT 时，插入电脑仅供电/充电并保持正常业务，不枚举 CDC，也不提供 USB 查询入口。按键完成进入 DUT 后，满足第 2.1 节条件才枚举 CDC；支持先进入 DUT 再插线，也支持先插线再按键进入，无需重新插拔。插线或主机请求不能触发 DUT。OFF 稳态仍仅 MSC。第 4.2 节受限恢复仅属于本次已进入过厂测的故障/退出过程，不是正常用户模式入口。

进入 DUT 产测模式沿用原有按键事件流程；CDC 不另建自动进入流程，不以收到测试命令替代按键进入。正常模式无 CDC，主机不能发送测试请求或期待 ERR STATE；仅在受限恢复会话仍存在、或退出前请求已进入处理层时执行状态拒绝。退出已使会话失效的旧请求直接清理，不跨会话补发响应。厂测 DUT 与经典蓝牙射频 DUT 必须区分，不能为启用 CDC 测试而无条件切入会断开 BLE/SPP 的射频测试状态。

| 厂测通道 | 目标用途与边界 |
|---|---|
| BLE | 保留工厂产测 APP 的现有测试入口及外部协议 |
| 经典蓝牙 SPP | 保留 MIC 频响测试通道，不将音频测试数据改走 CDC |
| USB CDC | 在按键进入 DUT 后收发测试指令，复用 BLE 对应测试命令的业务语义与公共处理逻辑 |

厂测模式目标为 BLE、经典蓝牙 SPP 与 CDC 通道可用；通道可用不代表允许同时操作 MIC、录音或存储，同一资源仍经过公共仲裁。正常模式不启动 CDC，原有 HOGP 行为保持不变，DUT 内 HOGP 行为需随原有厂测流程核实，本文不新增 DUT 内 HOGP 保持在线的承诺。

现有 DUT 代码中断开 BLE、关闭广播及进入经典蓝牙 DUT 的分支是阶段 3 必须核对、适配的实现差异，不能当作三通道共存已实现。CDC 与 BLE 共享测试能力不等于两者传输帧逐字节相同：第 6 节仍保留 CDC 文本封装设计，阶段 3 核对现有 BLE 命令映射、参数与结果后接入同一公共接口，响应回到请求来源，避免向其他通道误发。

上述边界不改变阶段顺序：阶段 1 接入按键 DUT 状态观察、枚举门控与生命周期；阶段 2 验证该门控下的双向字节收发及任务回调；阶段 3 接入测试命令准入、共享测试逻辑及 BLE/SPP/CDC 厂测共存。阶段 1/2 不提前开放厂测命令，也不以 DUT 三通道共存作为已完成能力。

## 3. 构建与配置

项目开关放在 `SDK/apps/earphone/include/t2620_project_config.h`，不修改工具生成的 `sdk_config.h/c`：

```c
/* 开发默认关闭；量产启用须完成本文验收。 */
#ifndef TCFG_T2620_FACTORY_USB_CDC_ENABLE
#define TCFG_T2620_FACTORY_USB_CDC_ENABLE 0
#endif

#if TCFG_T2620_FACTORY_USB_CDC_ENABLE
#undef TCFG_USB_CDC_BACKGROUND_RUN
#define TCFG_USB_CDC_BACKGROUND_RUN 1
#endif
```

合入 `不加HFP` 后，`TCFG_T2620_PC_STORAGE_ENABLE` 保持当前产品默认值 0，CDC 不再要求开启 U 盘导出。需要复测原分支的 MSC/CDC 切换时，显式设置该宏为 1；`usb_start()` 在厂测配置下不加入 `CDC_CLASS`，仅在允许 PC 存储接管后启动 MSC。关闭导出时 PC 就绪查询固定返回 0。

以上宏片段不是完整实现：`app_config.h` 后续调音配置可能覆盖后台 CDC 宏，`usb_common_def.h` 又会派生 USB 类与 OTG 配置。必须在最终派生配置后增加编译检查：厂测开启时 CDC、USB slave、SD0 和拨码依赖全部成立，否则编译失败；可选 MSC/PC 依赖由存储导出开关独立检查。

本版本厂测 CDC 与在线调音工具配置互斥，组合启用时编译报错，避免 UART 调音覆盖后台宏或 USB 调音争读同一端点。保留原有非厂测调音行为；不得同时派发 `MSG_FROM_CDC_DATA` 与 `MSG_FROM_FACTORY_CDC` 消费同一份数据。

新增源文件列入 `SDK/Makefile`，消息编号在现有应用消息定义处分配，避免冲突。分别检查开关 0/1 的最终预处理结果和构建；默认关闭不代表量产目标已启用。

## 4. USB 生命周期与热切换

### 4.1 统一协调入口

新增幂等的厂测 USB 协调逻辑，区分 `NONE / MSC / CDC / STOPPING` 实际状态。拨码、OTG、按键进入 DUT 完成、DUT 退出、模式退出完成、存储恢复完成和关机事件驱动协调；app_core 决定目标，USB 任务串行执行硬件启动/停止并回报结果。请求入队不能当成启动成功。

`usb_cdc_background_standby()` 在 ON 时抑制应用 IN 事件，即使业务尚未就绪也不能转入 PC；允许 CDC 时请求启动，否则由后续协调重试。OFF 稳态继续现有 MSC 路径；第 4.2 节受限重连状态同样抑制 PC IN。所有 `USBSTACK_START`、`USBSTACK_PAUSE`、`USBSTACK_CDC_BACKGROUND` 和直接后台启动入口均执行相同策略检查。

`usb_cdc_background_run()` 只注册 CDC 并配置设备，不负责停止旧类。实际类变化必须先停止旧 USB、调用 `usb_device_mode(..., 0)` 释放，再注册目标类；同类重复协调不得反复重枚举。过期启动请求须在 USB 任务执行前重新核对生命周期与最新目标。

### 4.2 转移规则

| 场景 | 必须执行的顺序 |
|---|---|
| ON 插电脑 | 未进入 DUT 时仅充电及正常业务；已按键进入 DUT 时，从机确认 → 业务/SD 准入 → CDC；不进入 PC |
| ON 带线按键进入 DUT | 厂测进入完成 → 重查角色及存储准入 → CDC，不等待新 OTG IN |
| 正常退出 DUT | 关闭新测试准入 → 完成业务清理；阶段 3 尽力有界发送退出响应 → 停止 CDC 并清理会话 → 恢复允许的正常业务。不得无限等待主机读取，响应丢失按结果未知处理；失败恢复例外见下文 |
| OFF 插电脑 | 沿用冷启动导出准入 → PC/MSC；不初始化 CDC 厂测会话 |
| 带线 OFF→ON | PC 停止 MSC → SD 归还并完成恢复 → 正常业务就绪；未进入 DUT 不启动 CDC，后续按键进入再枚举，不能等待新的 OTG IN |
| 带线 ON→OFF | 关闭新增业务准入 → 等待已有 OTA/格式化/DUT → 按现有录音/BLE/file fence 排空并复位 → 冷启动重新采样 → MSC 或最新目标 |
| 拔线/角色失效 | 关闭新请求准入 → 停止并清理 CDC 会话 → 不启动其他数据类；BLE 按现有策略运行 |
| 切换失败或关机 | 停止接受厂测动作；保留现有失败保护，不强行导出或启动 CDC |

等待 DUT 退出时，已有 CDC 会话可保留用于 GET_STATUS、`ft_dut 0` 或停止当前测试；拒绝新测试、格式化和整包结束。该状态是 ON→OFF 的排空例外，不是 OFF 稳态 CDC。DUT 已退出并开始排空后停止 CDC。不得先切断退出 DUT 的唯一通道，再无限等待 DUT 结束。

阶段 3 增加独立的 DUT 等待退出状态：仅当本次启动已进入 DUT、收到拨 OFF 请求且尚未开始业务排空时建立。此状态不随 USB 拔线或总线复位清除。恢复 VBUS 且重新确认从机角色后，允许重新枚举受限 CDC；不得启动 MSC，也不要求正常业务/SD 恢复就绪才能查询或请求安全退出。仍禁止在关机、切换失败或排空已开始时启动，动作本身继续执行存储安全检查。

受限会话仅允许 PING、GET_STATUS、`DUT ft_dut 0` 和当前可停止测试的停止命令；其他命令返回 ERR STATE。RUNNING 时仍按8-1 文档第 3.2 节返回 BUSY，不中断格式化或正在保存的录音。拔线不自动退出 DUT或强制取消动作；重新插线后可查询和退出。拨回 ON 且尚未排空时取消该等待限制；DUT 完成退出或整机复位后清除此状态，冷启动 OFF 不得据此恢复 CDC。阶段 1/2 不提供这条厂测恢复通道，也不得以 CDC 可退出 DUT 作为阶段验收前提。

OUT 仍需走到 USB stop。当前 `usb_task.c` 会转发 OUT，`pc_device_event_handler()` 在非 PC 模式下可请求 STOP；实施时须核对所有活动模式下的派发可达性。连续插拔和拨码反转依靠最新目标协调，不能靠旧消息继续枚举。

**厂测失败后的 ON 受限恢复（阶段 3）**

厂测故障恢复是独立于 DUT 动作开关的受限状态，不属于正常用户模式。正常退出且无失败标志时关闭 CDC；存在失败标志时，退出 DUT 仅结束测试动作，设备仍处于厂测故障恢复状态，可按本节重连查询。整机复位清除此运行时状态，不能据此自动枚举 CDC；重新进入须遵守按键及存储准入。故障状态下不得向工站宣称已恢复正常用户模式。

本次启动内，已接纳的格式化或整包结束动作进入 FAILED 时，设置独立的厂测失败恢复标志。该标志不随拔线、总线复位、拨码反转或退出 DUT 清除，仅在整机复位时清除；不得由冷启动的 SD 异常自行建立。它与 DUT 等待退出状态分别维护，不能随 LAST 记录被下一动作覆盖而消失。

该标志存在且拨码为 ON 时，已有 CDC 转为受限准入；重新确认 VBUS 和从机角色后允许受限重连，不以 SD 就绪或正常业务恢复为前提。仍须确认 PC/MSC 已退出、USB 旧类已释放，且未进入关机、切换失败或业务排空；不能绕过 SD 所有权保护。此处的厂测动作失败不等于 USB 切换失败，后者仍禁止启动 CDC。

ON 失败恢复仅允许 PING、GET_STATUS、`DUT ft_dut 0`、当前可停止测试的停止命令，以及 DUT 内的 `DUT ft_key_dut_enable 1`；其余命令返回 ERR STATE。重新使能按键须确认 VM 持久化成功。停止/退出仍须等待必要的保存与资源释放，不得将存储未就绪一概作为拒绝退出的理由，也不得丢弃未保存数据后回复 DONE；无法安全完成时保留故障保护并反馈失败。退出 DUT 不代表存储已恢复，不得恢复依赖故障存储的业务。

失败恢复与 OFF 等待退出同时存在时，使用 OFF 等待退出的较窄白名单，禁止重新使能按键；拨回 ON 只取消 OFF 等待限制，失败恢复限制仍保留。OFF 且 DUT 已退出时不再据此启动 CDC，后续排空/导出仍须通过原有存储保护。工站应在提交停止、退出或使能按键前读取并保存不含 AUTH 的失败状态；这些成功接纳的新动作会按8-1 文档第 3.2 节覆盖 LAST 记录。后续修复遵循第 7 节人工/工站恢复流程，不在该受限状态中自动重试格式化或整包结束。

### 4.3 会话与资源释放

每次 CDC 启动分配递增 session generation。RX 通知、命令和 TX 队列均携带 generation；重新枚举后丢弃旧消息。消息传稳定的 USB ID 与 generation，避免把可过期资源指针当作会话身份。

阶段 2 起，会话失效同时覆盖正常 DUT 退出、物理拔线、角色失效、USB 总线复位、取消配置及发送超时重建；不依赖这些事件一定经过完整 stop/start。先关闭准入并使旧 generation 失效，清理旧数据，完成传输准备后才开放新会话。USB 总线复位不等于整机复位，不清除已接受动作的状态记录。

串口关闭/重开不必触发 USB 复位，不能自动视为新会话。阶段 2 实施前须确定控制线变化的检测与清理方式，并实测目标驱动；若无法可靠检测，异常恢复必须显式重新枚举或拔插，不能仅依靠 PC 清空输入缓冲或发送 PING 判定会话已清理。工站遵守同一恢复步骤。

USB 任务停止前关闭准入并等待在途 I/O 安全退出，之后才能 `cdc_release()`；app_core 不能在释放过程中使用句柄。需实现生命周期锁或等效串行 I/O 所有权，且定义锁顺序，禁止 app_core 同步等待 USB stop、USB 任务又等待 app_core 的死锁。

断线清除半行、超长丢弃状态、待解析数据和未发送响应。已接受的格式化等动作不因拔线强制中止；安全完成并保留本次启动内的状态，供重连查询。旧回调不得向新会话发送旧响应。

## 5. CDC 收发与背压

### 5.1 接收

`usb_cdc_wakeup()` 位于中断上下文，只投递或合并 RX 通知，不解析、不执行 DUT、不收发协议响应。厂测消息处理在任务上下文调用 `cdc_read_data()`，每次读缓冲至少覆盖当前最大包长（当前 64 字节），跨包组帧。

现有驱动会按最大包读取后裁剪到调用者长度，小缓冲可能丢尾部。通知队列满不能只打一条日志后遗失数据：采用 pending 标志与有界补偿调度，并确认端点 NAK/重通知行为；不能假定丢一次消息必有下一次中断。每轮处理包数设上限，主动让出 app_core。

### 5.2 发送

协议层只生成完整响应，进入有界 TX FIFO。初始容量为 4 条、每条最多 258 字节（256 字节正文加 CRLF），实施时计入 RAM 预算。动作执行前必须预留应答容量，不能执行完才发现无处回复。

发送保留 `offset`，按 `cdc_write_data()` 返回长度推进，只有完整行写完才移除。返回 0 表示本次无进展，可能是未就绪或底层发送失败；不能立即通过同一不可用通道发送 `ERR BUSY`。正数短写必须继续发送剩余部分，不得插入另一条响应造成串行混帧。

采用有界重试与发送超时。超时后停止该会话的后续响应并清理/重建传输，客户端按超时处理；禁止将半条响应与后续响应拼接。`ERR BUSY` 仅用于通道可回复且命令尚未执行的业务占用或容量拒绝。队列完全耗尽时通过接收背压或关闭会话处理，禁止无限分配内存。

`cdc_write_data()` 当前含无限等待语义的 mutex 获取和循环 bulk 写，尚不能证明 app_core 最坏耗时有界。阶段 2 必须验证底层等待行为：若不能保证有界，需改成有界非阻塞发送或由可安全停止的专用 I/O 任务承担，协议与 DUT 仍留在 app_core。仅规定“单写者”不能替代此项。读写共享 mutex，迁移发送任务时也要验证接收不会被长时间阻塞。当前发送循环在累计 offset 前检查传输状态，须核实底层返回正数但状态改变时的计数路径，禁止把已发送数据当作未发送重新续传。

字节传输的保证限定于正常连接且成功接纳的数据：完整、有序、不重复。队列满时明确拒绝或背压，不允许静默丢弃后继续报成功。超时、复位或拔线允许未完成数据丢失，但必须报告传输失效并清理会话；不提供跨会话自动重传或业务“恰好执行一次”的保证。协议动作是否已执行仍按第 6 节查询与结果未知规则处理。

当前控制线处理收到 SET_CONTROL_LINE_STATE 后直接置就绪位，未按 DTR/RTS 清零。因此不能以 DTR 作为厂测身份或可靠断线判据。客户端打开串口完成控制请求后先 PING；波特率只是 CDC line coding 参数，不决定 USB bulk 速率。

## 6. 文本协议 v1（已拆分）

分帧、命令响应、错误码及身份字段设计已迁至 [8-1.产测USB_CDC接入BLE产测流程.md](8-1.产测USB_CDC接入BLE产测流程.md) 第 3 节。

## 7. DUT 公共接口与动作语义（已拆分）

统一动作仲裁、命令语义、异步结果及整包结束恢复要求已迁至 [8-1.产测USB_CDC接入BLE产测流程.md](8-1.产测USB_CDC接入BLE产测流程.md) 第 4 节。

## 8. 实施阶段与交付要求

### 8.1 三阶段总览

第 2–5 节及 8-1 文档描述最终能力，不要求第一阶段一次实现。每阶段完成代码检查与对应板测后再进入下一阶段；构建成功不等于电脑已识别串口，串口识别成功也不等于收发或厂测协议已完成。

| 阶段 | 目标 | 交付物 | 当前状态 |
|---|---|---|---|
| 1：串口识别 | ON 未进入 DUT 隐藏 CDC，按键进入后识别 COM，OFF 保留 MSC | 枚举固件、描述符记录、插拔及切换板测记录 | 新门控基础板测通过；本轮收束基础回归通过，背压、多轮稳定性与新增异常未闭环；1A/1B 未完整验收 |
| 2：双向收发与接收回调 | PC 数据到达任务回调，设备数据完整返回 PC | 传输接口、独立收发测试程序、异常收发记录 | 收发层、回显和 PC 工具已实现；软件检查及板测交付见 8.7，日志版本收发板测通过，完整回归及架构调整待验收 |
| 3：厂测协议接入 | 在可靠传输上实现本文身份查询及 DUT 流程 | 厂测固件、工站客户端、完整验收记录 | 待实施 |

### 8.2 阶段 1：先让电脑识别虚拟串口

**目标与范围**

本阶段必须先观察真实按键进入完成与退出状态，接入所有枚举入口；不得用按键按下、异步请求入队或通用射频 DUT 标志代替进入完成。初始化和复位默认关闭准入，进入失败不枚举，退出开始即禁止新启动；USB 任务执行前重查最新状态。原按键进入/退出若不能可靠完成，须在本阶段补齐最小生命周期适配，不能伪造 DUT 状态绕过验收。完整 BLE/SPP 厂测适配仍留到阶段 3。

只开放 CDC 枚举及串口打开、关闭所需的标准控制请求。此阶段不实现应用数据回显、PING、身份读取或 DUT 命令；串口工具发送数据不应触发业务动作，也不要求收到回复。

**检查点 1A：基础识别**

先完成配置、单独枚举和电脑驱动识别，以固定拨码状态验证。1A 是中间检查点，不代表热切换已通过；其固件仅用于受控板测，任何未完成的切换路径也不得允许 MSC/CDC 同时注册或 SD 双重所有权。

1. 增加第 3 节的项目级开关及最终配置检查。开发默认关闭，板测固件明确启用；保持 MSC、PC 模式和调音互斥约束。
2. 隔离现有调音工具接收入口，避免单独启用 CDC 后仍引用或调用调音消费者。
3. 实现第 4 节中枚举所需的启动/停止协调：ON 未进入 DUT 不枚举数据类、ON 按键进入 DUT 后仅 CDC、OFF 仅 MSC；所有启动入口统一门控，禁止同次注册两个数据功能。
4. 对主机误发数据，优先使用 bulk OUT 端点背压，允许主机写超时，不解析、不回复。必须确认中断不会持续触发或重复投递；若现有驱动无法安全背压，才采用任务上下文每轮有界读取并丢弃，不注册业务接收回调、不打印原始内容。该最小丢弃路径的 I/O 与释放同步必须在本阶段完成，不能延期到阶段 2。
5. 拔线、角色失效和关机时可靠停止 CDC；保护标准控制请求、中断及可能的丢弃读取与资源释放的并发。应用收发队列及完整会话机制留到阶段 2。
6. 检查 CDC 设备、接口、端点及字符串描述符，记录实验 VID/PID、序列号来源、Windows 版本、驱动名称和 COM 号。确认目标 Windows 环境是否自动绑定串口驱动；如需要 INF，明确交付和安装方式。

**1A 通过标准**

- ON 插入电脑、ON 带线冷启动但未进入 DUT：不出现 CDC 或 MSC，充电及正常业务保持。分别验证先按键进入 DUT 再插线、先插线再按键进入：出现可正常打开/关闭的 COM，无设备错误；退出 DUT 后 CDC 消失。冷启动不得因上次曾进入 DUT 自动开放 CDC。
- OFF 插入电脑、OFF 带线冷启动：保持现有 MSC 行为，不出现 CDC COM 口；纯充电器不枚举数据类。
- 串口工具误发单包及连续数据时，无业务动作或回复、无中断/消息洪泛，拔线后仍可重新枚举。允许主机因背压写超时，不以此判定阶段 2 收发能力。
- 完成首次枚举和端口打开/关闭记录，再推进 1B；不得以固件启动日志代替设备管理器及驱动记录。

**检查点 1B：生命周期与业务共存**

1. 覆盖带线 OFF→ON：PC 停止 MSC、归还并恢复 SD、业务就绪后保持正常模式且不启动 CDC；按键进入 DUT 后启动 CDC，不等待新的 OTG IN。ON→OFF 沿用现有等待、排空和复位规则。异步请求必须核对最新目标，同类重复请求不反复枚举。
2. 使用 1A 的实验标识，实测 MSC/CDC 切换的 Windows 缓存和驱动绑定；此项不能推迟到阶段 3。最终量产标识及其授权在阶段 3 前确定，标识或描述符变更后重跑 1A/1B。
3. 分别验证正常模式插线不枚举 CDC、原 BLE/HOGP/录音行为保持，以及 DUT 内原流程允许的业务不因 CDC 枚举和串口开关额外中断。使用真实按键进入及原有安全退出路径；退出路径不可用时修复最小生命周期适配，退出验收不得标记通过，也不将尚未实现的 CDC 命令作为退出通道。

**1B 通过标准**
- 带线双向拨码、切换中拔线和重新插线：最终功能符合最新拨码及角色，SD 所有权正确，无重复注册、卡死或异常重启。
- 正常模式确认无 CDC 且原有 BLE/HOGP/录音无回归；DUT 内以不接 USB 时的模式行为为基线，确认 CDC 不额外停止允许的业务。记录基线及差异，不要求本阶段完成 DUT 三通道测试，也不新增 DUT 内 HOGP 在线要求。
- 至少完成 DUT 内 20 轮插拔、20 轮串口打开/关闭及 10 轮带线双向拨码切换（每轮回 ON 后按键重新进入 DUT，并验证正常模式无 CDC）；这些是阶段门槛，不替代最终稳定性验收。

1A、1B 均通过才算阶段 1 完成。可描述为“电脑已识别并可打开虚拟串口”，不可描述为“USB 通信或厂测已打通”。

### 8.3 阶段 2：打通发送、接收与任务回调

**目标与接口边界**

建立与厂测命令无关的字节传输层：PC → USB RX 通知 → 任务上下文接收回调；设备 → 有界 TX 队列 → PC。USB 包边界不是应用消息边界，回调只交付字节与长度，不假定一次回调对应一次 PC 写入。

具体函数名在实施时确定，但接口必须明确：回调运行任务、数据缓冲的有效期、发送时是否复制数据、返回值表示入队还是完成、未就绪/队列满/会话失效如何报告。建议接收数据仅在回调期间有效，消费者需保留时自行复制；发送入队成功不能表示主机已经收到。

**计划工作**

1. 中断只通知或合并 pending；任务按最大包长读取，有界处理并让出 CPU。处理通知投递失败后的补偿，避免一次丢通知导致永久不再接收。
2. 实现有界发送、短写续传和超时处理。验证底层 bulk 写的最坏耗时及返回长度计数，主机停止读取不得阻塞正常业务；若现有驱动无法保证，改造有界 I/O 或引入可安全停止的专用 I/O 任务。
3. 实现 generation、I/O 与 release 同步及旧队列清理。会话失效须覆盖物理拔线、总线复位、取消配置及超时重建；另行定义串口关闭重开后的清理策略，不依赖现有 DTR 位作为可靠断线判据。
4. 提供仅用于阶段 2 板测的原始字节回显入口，以及设备主动发送固定测试字节的入口，验证两个方向。测试入口有独立开关，阶段 3 接入协议时关闭，不能与协议消费者同时读取 CDC 数据。
5. 提供最小 PC 测试脚本，按预期长度累计接收并逐字节比较；测试数据由 PC 生成，不使用 AUTH 或真实业务数据。

**通过标准**

- PC 发送的数据进入任务回调，设备回显完整一致；设备主动发送的数据也能被 PC 正确接收。
- 正常连接且数据被成功接纳时，覆盖 1、63、64、65、128、256 字节及跨多包数据、连续发送和含 `0x00` 的二进制数据，不丢字节、不乱序、不重复。至少完成 1000 轮完整字节比较。
- 覆盖 RX 通知失败、TX 队列满、0 写、正数短写、主机停止读取及超时，无无限等待、内存持续增长或正常业务卡死。
- 故障时允许未完成数据丢失，不要求跨会话补发；必须报告失效并按第 4.3 节完成清理与恢复，新会话不收到旧数据。分别记录拔线、总线复位、取消配置、发送超时和串口关闭重开的恢复步骤，不能仅用重新打开 COM 口代替会话失效处理。
- 测试回显仅在真实按键进入 DUT 后开放，退出时关闭并清理在途 I/O。与该 DUT 模式已允许的业务并行验证，对尚未实现的 BLE/SPP 共存记录为阶段 3 待验；另测退出后的正常 BLE、双 ACL、HOGP 和录音回归。TX 满时明确拒绝或背压，不得报成功后静默丢弃。

阶段 2 通过后，可描述为“USB 双向字节传输与接收回调已打通”，此时仍没有厂测命令语义。

### 8.4 阶段 3：产测 USB_CDC 接入 BLE 产测流程

本阶段已独立为 [8-1.产测USB_CDC接入BLE产测流程.md](8-1.产测USB_CDC接入BLE产测流程.md)，实施顺序与交付要求见其第 5 节，专项验收见其第 6 节。当前仍为待实施；阶段 1/2 的局部成功不改变本阶段状态。

### 8.5 各阶段的软件检查与记录

固件改动后从 SDK 执行 `.\.vscode\winmk.bat all`，并从仓库根执行：

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File .\tests\host\run_host_tests.ps1
```

新增有意义的协议/生命周期验证接入统一入口，不以静态文本匹配替代短写、队列满和断线并发测试。文档修订本身不等于这些检查已运行。

### 8.6 阶段 1A 固件与板测记录

日期：2026-09-14。状态：枚举代码与软件检查已完成；用户已确认新增串口可见、ON/OFF 热切换通过，其余实机项目继续补测，1A/1B 尚未完整验收。验收依据为本文第 8.2 节。

#### 阶段 1 提交前收束

本轮测试包：`output/factory-usb-key-dut-stage1-review/`，不覆盖上一轮固件。最终构建与文件哈希以该目录 `manifest.json` 为准。

软件验证：CDC=0 与 CDC=1 均构建成功；统一五组主机检查通过，新增执行真实 DUT 关机函数的用例，检查测试清理前撤销授权，以及延迟关机未完成时授权不会恢复。日志为包内 `factory_usb_review_build0.log`、`factory_usb_review_build1.log`、`factory_usb_review_host.log`。用户已完成本轮基础回归，可准备提交，详见下方最终回归记录。

- RDX 的 `rdx_dut_factory_usb_ready()` 统一提供产品准入，包含物理按键授权、真实 DUT 状态、业务/拨码/关机守卫。JL USB 层保留 VBUS、OTG 角色、PC 排斥及类资源管理，执行前仍重查准入，防止旧消息启动 CDC。
- 普通 DUT 退出及 `rdx_dut_poweroff()` 在测试清理前统一撤销授权。后者不再依赖延迟关机最终成功才能撤销 CDC；finalpack 最终调用该关机入口时同样生效，但不代表 finalpack 整体安全问题已经修复。
- CDC 编译开关为 0 时，按键包装入口直接沿用原 DUT 处理函数，不额外增加本轮 CDC 守卫。BLE/SPP 原有行为不变，未实现阶段 2/3。
- 回归：正常 ON 插 USB 不出现 COM；带线 KEY5 八击进入后出现 COM；八击退出后消失；先进入再插线也可枚举；重新开机未进入 DUT 不恢复 COM；普通退出后 OFF/MSC 正常。DUT 关机入口的撤销另作软件检查；不要依据旧模块注释使用 KEY5 六击，当前该按键表未映射六击关机。按现有阶段验收表补充重复切换、打开/关闭和背压项目。
- BLE 恢复出厂无响应与 USB 插入欠压复位继续作为未解决问题保留在下一节，不能因本轮代码收束而标记修复。用户回归通过后可提交本阶段范围，提交不等于两个异常或完整阶段验收已闭环。

#### 提交前最终基础回归（20:36–20:38）

日志：`COM3_2026-09-14_20-36-30.log`。用户确认按既定步骤测试通过。本记录更新上文“收束待回归”状态及下方验收表的基础门控结果；不代表所有待测项目通过。

- 20:36:44 按键进入 DUT 完成并授权；20:36:55 插入 USB，20:36:56 出现 `cdc_reset`，支持先进入再插线。
- 20:37:10 普通退出 DUT，设备运行时间 34.220 秒释放 USB 配置资源；用户确认 COM 消失。
- 20:37:54 保存及静默完成后受控重启，复位源为 `P33_SOFT_RST`；20:37:56 OFF 进入 PC，存储转为 `HOST_OWNED`。
- 20:38:04 拨回 ON，PC 退出，`HOST_OWNED -> DEVICE_OWNED, sd0 remount ok`；此后至再次进入 DUT 前无 `cdc_reset`。20:38:20 带线按键进入 DUT 后再次枚举，支持带线进入及回到正常 ON 后不自动恢复 CDC。
- 正常 ON 无 COM、串口可见及退出消失采用用户的主机侧确认；设备日志不能单独证明 Windows 设备列表和串口开关结果。日志在第二次枚举后结束，不额外声称完成第二次退出或多轮压力测试。
- 本次未发现 `P33_VDDIO_LVD_RST`，不能据此关闭间歇性欠压问题；也无恢复出厂指令的明确执行证据，BLE 恢复出厂问题仍未解决。启动和模式切换时仍有 `timer_no_response: app_core`，其后周期日志及按键继续运行，保留观察。

当前 SDK 源码与测试包 `factory-usb-key-dut-stage1-review/manifest.json` 的源码哈希全部一致；统一五组主机检查再次通过，日志为 `cache/factory_usb_final_review_host.log`。本次未改固件代码，沿用已完成的 CDC=0/1 构建结果。源码、文档和测试的差异空白检查通过；原始串口日志含行尾空格，保留采集原文。

结论：本轮 DUT 门控与 USB/MSC 生命周期基础回归通过，可准备提交本阶段实现。独立带线冷启动、背压、规定次数的插拔/串口开关/双向切换及完整业务共存仍待补测，1A/1B 不标记完整验收；阶段 2/3 未实施，两个历史异常继续跟踪。

#### 新 DUT 门控回归及新增异常（20:00–20:05）

日志：`COM3_2026-09-14_20-00-48.log`。用户确认按既定步骤测试无问题：正常 ON 隐藏 CDC、两种插入顺序、退出撤销 CDC、退出后 OFF/MSC 的基础回归通过。本记录更新前文“新门控待板测”状态；背压、多轮稳定性及新增异常仍未验收，阶段 2/3 未实施。

- 20:03:27：按键进入完成并开放 CDC，随后 `cdc_reset`；20:03:59 退出。第二次进入后，20:05:00 插 USB 并再次枚举，20:05:20 退出。部分采集时间存在批量滞后，须结合设备运行时间判断顺序。
- 20:05:28：保存并静默后受控重启，重启源为 `P33_SOFT_RST`；随后 OFF 进入 PC，存储转为 `HOST_OWNED`。
- **USB 插入相关欠压复位待定位**：20:01:12–20:02:07 共 21 条 `P33_VDDIO_LVD_RST`，均早于首次按键进入 DUT/CDC 枚举。首条紧邻充电状态事件，支持 VDDIO 欠压复位，但尚不能确定供电瞬态或软件电源配置的具体原因。电池 ADC 约 4.3 V 不能排除 VDDIO 瞬态跌落；需捕获插入时 VBUS、VBAT、VDDIO 波形，并对比正常 ON 与 DUT 状态。
- `charge_ldo5v_in_deal` 的“关闭内置充电, reset”是现有日志文字；源码该分支仅在 `goto_poweroff_flag` 为真时调用 `cpu_reset()`，不能凭文字认定发生软件重启。枚举中的 `usb reset` 也不是芯片重启证据。
- **BLE 恢复出厂设置无响应：已补充复现证据，关机等待风险已修正，待板测确认**。此前日志未找到恢复出厂命令；2026-09-15 新日志已明确收到 `*APP#default#` 并进入关机入口，详见下方更新。

本轮未修改固件处理上述异常；`timer_no_response: app_core` 继续保留为待观察项，不能单凭此提示判定永久卡死。

#### BLE 恢复出厂问题更新（2026-09-15）

本记录接续上述已有问题，不另建问题文档。新增 CDC 不得破坏原有蓝牙、APP 恢复出厂、录音、关机及 MSC 功能；串口回显或编译成功不能代替原有功能回归。

**日志证据**

- `正常日志.log`：收到 `*APP#default#` 后，设备时间 18.866 秒进入 `sys_enter_soft_poweroff`，18.867 秒关闭自动关机定时器，18.930 秒执行 `cpu_reset`。
- `COM3_2026-09-15_11-22-21.log`：26.725 秒收到同一命令，默认设置写入继续执行；27.749 秒进入 `sys_enter_soft_poweroff` 后日志结束。异常发生时日志显示 DUT 状态为 0；编译启用 CDC 后，普通模式关机也会经过新增的 USB 停止门控。

排查范围因此缩小到关机入口与后续清理之间。两份日志的广播刷新行为也有差异，不能当成同一固件的严格 A/B 实验；日志没有任务栈，尚不能证明实际卡在互斥锁、信号量还是 USB 清理内部。

**第二次板测与边界纠正**

`COM3_2026-09-15_11-44-41.log`：APP 恢复出厂在设备时间 15.814 秒进入关机，15.815 秒打印 shutdown requested 和 CDC STOP；拨码关机在 9.485 秒进入关机，随后同样停在 STOP。两者都没有 shutdown complete。用户确认触发时未插 USB；STOP 是清理入口日志，不代表 CDC 曾运行。尚无任务栈，不能确定卡住的具体底层指令。

前一版将同步等待改成异步停止确认，但仍要求 USB 清理完成才允许关机，甚至空闲 USB 也无条件执行 usb_stop；超时或重试定时器分配失败则阻止关机。这个依赖边界错误，原先主机测试验证了错误规则，不能作为修复成功的依据。此前的 `output/factory-usb-reset-fix/` 是这一被板测否定的版本。

当前修正取消 CDC 对系统关机/复位的完成确认门槛：

- TWS 和非 TWS 关机入口只调用无返回值的通知接口，随后继续原有流程；删除 20 ms 重试定时器、两秒等待和超时阻止关机逻辑。
- 通知立即关闭新会话准入和软件收发，不等待互斥锁、信号量、USB 任务或主机读取，不在调用任务操作 USB 硬件、释放 DMA 缓冲或打印 CDC 日志。
- 当前未运行 CDC 时不投递清理消息；延迟消息也检查实际类状态，不对 NONE 执行 usb_stop。旧启动消息执行时重新检查关闭准入，不能重新打开 CDC。
- 运行中的 CDC 由 usb_stack 尽力清理；投递失败或任务未响应不能阻止系统关机/复位，待发送数据允许丢弃。已经提交的 DMA 缓冲仍由 USB 任务管理，通知路径不提前释放。
- CDC 通知不停止 MSC，PC 模式原有 SD 所有权交接和存储保护流程保持原路径。这里取消的是 CDC 新增门槛，不能绕过录音/文件系统原有保护。

**验证与待复测项**

统一五组主机检查通过（`cache/factory_shutdown_boundary_host.log`）。执行真实协调器、TWS/非 TWS 通知段及 CDC 传输核心，覆盖未插 USB 零清理、旧启动消息拒绝、USB 任务未确认、队列满仍继续关机、MSC 不被 CDC 清理接管，以及撤销后禁止重新打开/继续发送。启用 CDC 的固件构建通过（存在 SDK 编译/链接警告），结果记录在 `cache/factory_shutdown_boundary_build.log`；本次测试包为 `output/factory-usb-shutdown-boundary/`。

板测仍待确认：正常模式未插 USB 时分别执行 APP 恢复出厂和拨码关机；再覆盖正常模式插 USB、DUT/CDC 打开及持续收发时的两个操作，并回归 BLE、录音关闭和 OFF/MSC 退出。COM3 应进入原有复位/关机流程，恢复出厂应看到 cpu_reset；不再要求出现 shutdown requested/complete。未插 USB 时本通知不应产生 CDC STOP。

用户已复测确认 APP 恢复出厂和拨码关机不再阻塞。该反馈不代表持续收发、BLE、录音及 MSC 的全部组合回归完成；USB 插入欠压复位仍单独跟踪。

#### 首轮板测反馈（19:21–19:23）

用户确认电脑出现新增串口，并通过拨码 ON/OFF 热切换。日志来源：仓库根目录 `COM3_2026-09-14_19-21-05.log`；文件名中的 COM3 是采集通道，不能据此认定新增 CDC 的端口号。

- 19:21:20：出现 `cdc_reset`，与用户观察到串口一致；驱动名称、描述符及串口打开/关闭仍待记录。
- 19:22:57：ON→OFF 先关闭准入，完成录音关闭、BLE FIFO 排空与文件屏障，再执行 `controlled reset, latest ON=0 VBUS=1`。这是现有受控重启切换路径。
- 19:22:59：`OFF + confirmed host -> PC`，存储由 `DEVICE_OWNED -> HOST_OWNED`，随后 PC 启动。
- 19:23:16–17：OFF→ON 先停止 PC、释放 USB 资源，`HOST_OWNED -> DEVICE_OWNED, sd0 remount ok`，索引刷新结束后再次出现 `cdc_reset`。本轮日志支持存储交接后再恢复 CDC 的顺序。

本轮可记录为串口可见及一次双向热切换的初步通过，不能替代 1B 的 10 轮双向切换。日志中启动和 BT 恢复期间出现 `timer_no_response: app_core`，随后仍继续运行；保留为待观察项，尚不能归因于 CDC，也不能据此宣称时序完全正常。枚举期间的 `usb reset` 即使标注 `<error>`，也不能单独认定为枚举失败。

#### 历史板测固件范围（尚无 DUT 枚举门控）

- ON + 已确认电脑从机角色，业务就绪且 PC/SD 交接完成后，仅启动 CDC；OFF 保留现有 MSC 准入与独占存储规则。
- app_core 每 100 ms 发布 CDC 准入，USB 任务重查最新条件；重复同类请求不重枚举，未就绪或投递失败由后续定时协调重试。
- 仅支持串口枚举及打开/关闭所需的 line coding、control line 请求。没有回显、PING、GET_INFO 或 DUT 命令。
- bulk OUT 不注册应用消费者且关闭接收中断；预期硬件缓冲满后背压，允许串口写超时。实际表现必须填写下表。
- ON→OFF 的旧 DUT 等待保护保持生效；阶段 3 的 CDC 安全退出通道尚未实现，本阶段不可用它退出 DUT。

源码默认 `TCFG_T2620_FACTORY_USB_CDC_ENABLE=0`。本次启用固件单独保存在 `output/factory-usb-stage1a/`，以该目录的 `manifest.json`、哈希和构建日志识别，不能仅按 `FIRMWARE_VERSION` 判断 CDC 是否启用。标准构建输出可能被后续构建覆盖。

#### 重建板测固件

2026-09-14 新门控测试包：`output/factory-usb-key-dut-stage1a/`，与旧 `factory-usb-stage1a/` 分开保存，刷写时以新目录的 `manifest.json` 和文件哈希为准。

- `APP_MSG_DUT` 的按键分发改用独立入口；只有实际进入流程返回且 DUT 已生效，才发布 CDC 准入。BLE 进入 DUT 不会授予 USB 准入；进入失败、按键入口被禁用、OFF 或存储未就绪均不授予。
- 初始化清除准入，退出开始立即撤销；USB 任务执行旧启动消息时再次检查。支持先按键后插线、先插线后按键，正常退出后串口消失。
- 本轮保留已有 DUT 蓝牙行为。当前源码在进入处主动断开 BLE 并关闭广播，广播恢复也拦截 DUT；这些限制并非本次 CDC 修改引入。`bt_bredr_enter_dut_mode(0, 0)` 本身是否支持 BLE 共存不能仅凭名称判定，共存仍需后续结合控制器行为实测。
- 按现有 KEY5（`KEY_IO_NUM4`）八击进入/退出 DUT。先停止正在运行的测试再退出；带线拨 OFF 时现有 DUT 等待保护仍生效，应先通过按键退出再观察 MSC，不能等待尚未实现的 CDC 退出命令。
- 主机检查覆盖正常模式隐藏、两种插线顺序、退出前旧请求失效、失败进入不授予、BLE 进入不授予，以及原有存储交接与关机保护。串口仍无回显和厂测命令。

在 SDK 目录使用原 VS Code wrapper。切换构建配置时先 clean，避免 Make 将另一配置生成的 ELF 误认为最新；此命令仅用于明确启用 CDC 的板测构建。

```powershell
$factoryPreviousMakeflags = $env:MAKEFLAGS
try {
    $env:MAKEFLAGS = 'EXT_CFLAGS=-DTCFG_T2620_FACTORY_USB_CDC_ENABLE=1 BUILD_DIR=objs/factory_cdc'
    .\.vscode\winmk.bat clean
    if ($LASTEXITCODE -ne 0) { throw 'Factory clean failed' }
    .\.vscode\winmk.bat all
    if ($LASTEXITCODE -ne 0) { throw 'Factory build failed' }
} finally {
    $env:MAKEFLAGS = $factoryPreviousMakeflags
}
```

回到默认配置也需先执行 wrapper 的 clean 再 all。刷写使用 JL Studio / ISD 工具，选择本次明确启用的固件；实际验收进度以本节实测记录为准。

#### 源码描述符基线与实测记录

本阶段沿用 SDK 实验标识，不将其视为量产授权。MSC/CDC 共用 VID/PID，Windows 缓存和驱动绑定必须实测；标识变更后重跑 1A/1B。

| 项目 | 源码预期 | 实机记录 |
|---|---|---|
| VID / PID | `3654 / 4155`，PID 来自字符 `U`,`A` 的小端值 | 待填 |
| bcdDevice | `0100` | 待填 |
| CDC 设备类 | `EF/02/01`，IAD 关联两个 CDC 接口 | 待填 |
| CDC 接口 | 控制接口 0：`02/02/01`；数据接口 1：`0A/00/00` | 待填 |
| CDC 端点 | bulk IN `81` / OUT `01`，64 字节；通知 IN `82`，8 字节 | 待填 |
| MSC 设备类 | 保留 SDK 的 `00/00/00`；不包含 CDC 接口 | 待填 |
| 序列号 | NOR Flash UUID 前 8 字节转 16 位大写 HEX；无 UUID 时沿用 SDK 固定回退串 | 待填实际值及是否回退 |
| Windows / 驱动 / COM | 已反馈串口可见；系统版本、是否自动绑定 usbser 及 COM 号待记录 | 待填 |
| 固件 / 板号 / 工具版本 | 使用本次 manifest 中固件哈希 | 待填 |

固定回退序列号不能证明多台设备可被唯一识别；若板测发现使用回退串，需在后续工站识别设计中处理，不能按 COM 号或该回退串判定设备身份。

#### 1A 实机验收（按新边界补测）

| 场景 | 通过标准 | 结果 |
|---|---|---|
| ON 未进入 DUT，插电脑/带线冷启动 | 不出现 CDC/MSC，充电及正常业务保持 | 新门控已实现、待测；旧固件出现 COM 不作为本项通过 |
| 按键进入 DUT 后插线 / 带线按键进入 DUT | 仅 CDC，可打开关闭，不进入 PC | 新门控已实现、待测 |
| 正常退出 DUT | 安全清理后 CDC 消失，恢复允许的正常业务 | 待测 |
| DUT 内准入延迟就绪 | 准入通过后出现 CDC，不因早期未就绪永久漏启动 | 待测 |
| OFF 插电脑及带线冷启动 | 仅 MSC，SD 正常导出，不出现 COM | 待测 |
| 纯充电器 | 无数据类枚举，充电符合原有策略 | 待测 |
| 串口单包/连续写入 | 无业务动作和回复，无中断/消息洪泛；允许背压写超时 | 待测 |
| 写入中拔线再插入 | 不崩溃、不死锁，可再次枚举；无旧数据消费者 | 待测 |
| 总线复位及串口反复开关 | 标准控制请求正常，OUT 仍不产生业务动作或中断洪泛 | 待测 |

请记录错误码、系统设备事件、是否需要 INF、实际 NAK/超时表现和固件日志。日志不采集 AUTH 或原始业务数据。1A 未通过时修复本阶段，不提前开放阶段 2/3 功能。

#### 已执行的软件检查及边界

已完成默认开关 0 与显式开关 1 的 wrapper 构建；真实 SDK 预处理检查覆盖 CDC 开关、MSC 保留、端点分配，以及 UART/USB 调音冲突组合编译拒绝。

统一主机入口保持五组契约，其中产品契约新增执行真实枚举协调 C 代码的行为测试，覆盖 SD 所有权、延迟启动、旧请求、投递失败与初始化失败重试、重复 IN、资源释放顺序及关机门控。依赖说明见 `tests/host/README.md`。

这些软件检查不验证真实 USB IRQ/DMA 时序、驱动绑定、硬件 NAK 或 BLE/录音共存性能。1B 的 20 轮插拔、20 轮串口开关、10 轮带线双向切换及业务共存均尚未验收。

### 8.7 阶段 2：字节收发实现与板测交付

日期：2026-09-15。用户在确认串口可见后授权实施。本节是新的阶段 2 状态，不修改第 8.6 节历史板测结论；阶段 1 未闭环项目仍保留。完整厂测命令、DUT 三通道共存及受限恢复仍属于阶段 3，尚未接入。

**本轮实现**

- 新增 `device/usb/device/usb_factory_cdc.c/h`。`usb_stack` 串行拥有端点 I/O 和 stop/release，短临界区保护队列及会话；回调在该任务运行、临界区外执行，最多交付一个 64 字节包。消费者需保留数据时自行复制，未来厂测解析与 DUT 处理仍投递 app_core。
- RX 中断屏蔽当前端点通知并合并投递 `USBSTACK_FACTORY_CDC_IO`，10 ms 定时通知补偿队列满、丢通知及 TX 进展。每次只处理有界工作，不把 USB 包边界当作应用消息边界。
- 固定 4×258 字节 TX FIFO，另有一个 64 字节待消费 RX 包。发送全量复制入队或明确返回满/失效/参数错误；成功仅代表入队。短写按 offset 续传，消费者拒收时保留同一 RX 包；最后一个已提交 DMA 包及 ZLP 仍受超时保护。
- 核对当前 BR28 链接产物后，未采用会循环等待的 `usb_g_bulk_read/write`。CDC 使用与 SDK 一致的单包 DMA 提交和 `maxp+4` 交替 RX 缓冲规则，每次只消费一个包，包含 ZLP；端点固定 64 字节并有编译检查。真实 DMA/IRQ 时序仍需板测。
- 启动、总线复位、SET_CONFIGURATION（包含取消配置）、DTR 关闭、停止和超时使 generation 失效。旧通知无资源指针，旧 callback 不能向新会话发送。已打开端口的 DTR 下降沿和 2 秒发送无进展触发停止，至少间隔 300 ms 再按现有 DUT 准入重新枚举。
- 串口关闭是否可靠产生 DTR 下降沿须记录目标 Windows 驱动实测结果。关闭后没有重新枚举、或发生任何收发错误时，工站必须拔插 USB / 退出再进入 DUT，不能只清输入缓存或重开端口后假定会话已清理。
- 独立 `TCFG_T2620_FACTORY_USB_CDC_TEST_ENABLE` 默认 0，依赖 CDC 开关。测试开启后每个打开会话主动发一次 `00..FF` 共 256 字节，随后原始字节回显；TEST=0 不注册测试消费者。阶段 3 接入时必须关闭该测试开关。

**验证与板测入口**

统一主机入口新增执行实际传输 C 和 DMA 适配 C 的故障注入，覆盖队列满、短写/零写、通知失败补偿、回调期间复位、旧 generation、最后 DMA 包超时、时钟回绕、交替短包及 ZLP。软件模拟不证明 Windows 驱动、供电或 USB 时序通过。

板测说明统一见本节。PC 工具：[tests/board/factory_cdc_bytes.py](../tests/board/factory_cdc_bytes.py)。测试包位置 `output/factory-usb-stage2/`，以包内 `manifest.json` 的配置、构建记录和 SHA256 为准。源码默认 CDC=0 的 clean/all 构建通过；CDC=1/TEST=1 的 clean/all 构建及最终会话同步修正后的重建通过，沿用 VS Code wrapper，日志分别为包内 `factory_stage2_build0.log`、`factory_stage2_build1_final.log`、`factory_stage2_build1_release.log`。统一检查日志为 `factory_stage2_host_final.log`。SDK 既有编译警告保留在构建日志中，不作为无警告构建说明。

```powershell
python -m pip install pyserial
python tests/board/factory_cdc_bytes.py --port COM6
```

COM7 须替换为新增 USB CDC 端口。先 ON + KEY5 八击进入 DUT，使用新枚举会话运行。脚本先验证设备主动图样，再累计接收并逐字节比较 1、63、64、65、128、256、258、1024、4096 字节的人工二进制数据（包含 NUL），最后检查无额外字节。发生错误不自动重放。

| 验证项 | 本轮状态 |
|---|---|
| 实际 C 故障注入及统一五组主机检查 | 已通过，最终交付日志随包保存 |
| CDC=0 与 CDC=1/TEST=1 固件构建 | 均通过，板测包明确为 CDC=1/TEST=1 |
| 主动发送、回显至少 1000 轮 | COM6 首轮：主动 256 字节图样通过；回显第 1 轮（1 字节）内容不一致，已停止，未通过 |
| DTR 开关、拔插、总线复位、取消配置、背压与超时 | 待板测 |
| 阶段 1 缺项与正常业务回归 | 继续补测，不宣称完成 |
| USB 插入欠压复位、BLE 恢复出厂无响应 | 欠压继续定位；恢复出厂已修正 USB 同步关机等待风险，软件检查通过，待新版板测确认，详见前文问题更新 |

#### 阶段 2 首次 COM6 脚本板测

用户退出占用 COM6 的串口工具后，执行 `python -u tests/board/factory_cdc_bytes.py --port COM6 --rounds 1000`。端口打开成功，主动发送的 `00..FF` 256 字节逐字节校验通过；第 1 轮发送一个 `00`，收到 1 字节但内容不一致，脚本按失败策略立即结束，未继续 1000 轮。日志：`cache/factory_stage2_COM6_board.log`。当前日志只记录错误位置和长度，未记录实际返回值，不能据此确定具体根因；后续须在新会话中采集已知测试字节的返回内容，并核对 RX DMA 处理顺序。此次结果不能标记双向传输通过。

#### CDC 日志收束（2026-09-15）

COM3 是固件日志观察端，COM6 是 CDC 交互端。默认 `TCFG_T2620_FACTORY_USB_CDC_LOG_LEVEL=1`，不打印收发内容，不需要脚本才能观察交互。

- 日常模式每 1000 ms 汇总一次同一会话的 `RX` 与 `TX_DONE`，`len` 为累计字节数，`count` 为非零 USB 数据包数量，不是应用命令数量。小数据同样在下一个汇总周期显示；USB 任务繁忙时可能延后。
- `TX_DONE` 只在端点不再忙后计入对应包的实际字节数；入队和提交 DMA 不算完成，ZLP 不增加字节数或包数。它证明设备侧观察到端点完成，不代表电脑应用已经读取、解析或执行。复位、关闭、超时会清除未确认字节，不能将它们算到新会话。
- 默认隐藏 `TX_QUEUED`、`TX_SUBMIT`。保留 START、RESET、CONFIG、OPEN、CLOSE、STOP、TIMEOUT；重复的 `TX_REJECT` 按会话及错误码汇总。`rc=-1` 表示会话未就绪或已失效，`-2` 表示发送队列满，`-3` 表示参数无效。
- `ms` 是该条记录首个事件的设备时间，`gen` 是会话代次；跨会话不合并。记录按首次出现位置输出，汇总日志不能用于重建逐包时序。
- 队列固定 16 条，满时丢弃日志并显示 `LOG_DROPPED`，此时统计不是完整流量。日志不背压数据通道。等级 2 保留最多 32 字节的包内容预览，仍受队列和周期限制，仅供已知测试数据诊断；涉及 AUTH 的业务不得开启内容日志。等级 0 关闭本模块诊断。
- 厂测 CDC 下关闭 `cdc.c` 控制/端点回调的直接打印，诊断统一由 USB 任务在开中断时输出；非厂测 CDC 保留 SDK 日志行为。USB 停止时由 USB 任务有界输出剩余日志；恢复出厂及拨码关机调用路径不输出、不等待日志，不依赖输出完成。

例如 COM6 发送 `hello`（5 字节、不附加换行），测试回显开启时 COM3 在汇总周期内可见以下形式；时间和会话号以实机为准：

```text
[FACTORY_CDC] ms=12000 gen=3 RX len=5 count=1 rc=0
[FACTORY_CDC] ms=12020 gen=3 TX_DONE len=5 count=1 rc=0
```

测试模式打开端口时原有的主动 256 字节测试图样仍保留，其发送量也计入 TX_DONE；与 hello 同处一个周期时发送汇总可能为 261 字节。本次只收束日志，不改变该测试行为。

统一五组主机检查通过；启用 CDC=1/TEST=1、默认 LOG_LEVEL=1 的固件构建通过（保留 SDK 既有警告）。独立测试包：`output/factory-usb-cdc-log-summary/`；构建及检查记录随包保存，配置及 SHA256 见 `manifest.json`。软件验证覆盖日志等级 0/1/2、1000 次收发汇总、周期与时间回绕、会话/错误码隔离、DMA 忙时不报完成、65 字节分包完成及失效清理。板测需确认 hello 回显、持续收发时日志量及恢复出厂/拨码关机回归；此前用户已确认关机修复有效，不等于本次日志版本已完成板测。

#### RDX 架构边界收束（2026-09-15）

本轮保持“RDX 决定产品准入、USB 任务拥有端点与释放、字节传输不执行 DUT 命令”的分层。必要的 JL 原生接入保留在 CDC 驱动、配置请求及 USB 任务分发中。

- 通用 `usb_pause()` 仅在实际状态为 CDC 时停止厂测传输；NONE/MSC 停止不再递增 CDC 会话、删除 CDC 定时器或输出 CDC STOP。CDC 启动失败仍在局部失败路径清理。恢复出厂、拨码关机继续只通知，不等待 USB 清理。
- `factory_cdc_requested` 是 app_core 的协调请求，不是可长期缓存的授权。USB 执行前继续通过 RDX 的只读准入接口重查 DUT/关机/业务守卫，并检查线缆、角色和资源条件，防止退出后的旧启动消息恢复 CDC。未将这条实时保护替换为周期快照；也未将不透明的底层状态查询包进新的关中断区。未来修改这些查询时必须保持无等待、无业务副作用。
- `usb_factory_cdc.h` 仅公开字节收发、会话及消费者接口；生命周期、驱动原语和端点参数移至 `usb_factory_cdc_internal.h`。业务消费者在启动前注册，停止/重启保留注册关系；消费者不得同步等待 USB 任务。
- 回显和 256 字节图样移至独立 `usb_factory_cdc_test.c`，仍由 TEST 开关控制。测试消费者仅在没有已注册消费者时作为默认值，不再覆盖正式消费者；自定义消费者不发送测试图样。DTR 只记录测试打开事件，图样在 USB 任务、临界区外生成并通过公共发送接口入队。测试模式既有串口交互行为保持一致。
- 本轮未实现阶段 3 的分帧、厂测命令及 app_core 消费者；这些业务不能直接放入 USB 回调。

前一日志版本的板测补充：`cache/cdc_log_summary_COM6_board.log` 与 `COM3_2026-09-15_14-02-07.log` 对照，hello 5 字节回显通过，脚本 1000 轮共 661006 字节一致；COM3 同会话 RX 合计 661011、TX_DONE 合计 661267，差额为主动图样 256 字节。未见 CDC TIMEOUT、TX_REJECT、LOG_DROPPED。该结果不解释更早首次单字节失败的根因，也不代表本轮架构调整已经板测。COM6 已释放，后续只做 hello 和少量交互，不再默认执行大批量测试。

本轮验证记录：`cache/cdc_architecture_host.log`、`cache/cdc_architecture_build.log`、`cache/cdc_architecture_build_verify.log`。主机检查覆盖 NONE/MSC 不触发 CDC 清理、CDC 停止只清理一次、旧准入消息被拒绝、自定义消费者重启不被测试覆盖，以及原有收发与日志契约。统一五组主机检查与 CDC=1/TEST=1 固件构建通过，独立测试包为 `output/factory-usb-cdc-architecture/`（含配置、产物 SHA256 和检查记录）。实机回归待刷入本轮固件后执行；不占用 COM6。

#### 测试入口简化（2026-09-15）

- 日常主机检查仍只运行根目录命令 `powershell -NoProfile -ExecutionPolicy Bypass -File .\tests\host\run_host_tests.ps1`，保留原有五组契约。CDC 收发、驱动、日志验证并入 `tests/host/test_factory_usb.py`，不再单独维护 `test_factory_cdc_transport.py`；保留已发现故障对应的队列、会话、关机与 MSC 隔离回归。
- 板测仅保留 `python tests/board/factory_cdc_bytes.py --port COM6`，依赖 `pyserial`，设备需进入 DUT 且启用 CDC/TEST。固定 115200 8N1，读取固件主动图样后验证 hello、短文本、5 字节二进制和 65 字节跨包数据，共 4 次、81 字节。默认约 6 秒，成功或失败均关闭端口，不重试；移除随机轮数、压测和暂停读取选项。
- 删除独立板测 README、临时 COM6 测试脚本及已跟踪 Python 缓存；忽略后续生成的 `__pycache__`/字节码。保留原始测试日志，不把历史压测命令当作当前工具用法。原有存储补丁、拨码切换专项测试有独立故障覆盖，继续保留。
- 本轮简化前的基本板测记录：`cache/cdc_architecture_COM6_basic.log`，14:53:14–14:53:20，主动图样和四次回显全部通过，COM6 已释放。脚本整理本身只运行主机检查与命令行帮助验证，不额外占用串口。

## 9. 基础验收矩阵

本节保留 USB 基础与阶段 1/2 回归项；阶段三专项验收已迁至 [8-1.产测USB_CDC接入BLE产测流程.md](8-1.产测USB_CDC接入BLE产测流程.md) 第 6 节，两份矩阵共同构成完整验收范围。

| 分类 | 必测场景 | 通过标准 |
|---|---|---|
| 基线 | 开关 0/1 构建、最终宏、非法调音组合 | 两个有效配置构建通过，冲突配置明确失败 |
| 稳态枚举 | OFF/ON 插电脑、冷启动已插线、纯充电器 | OFF 仅 MSC；ON 未进入 DUT 无数据类，按键进入 DUT 后仅 CDC；纯充电器无数据类，SD 所有权正确 |
| 阶段 1 误发 | 串口工具发送单包/连续数据，发送中拔线 | 有界丢弃或背压，无业务动作、中断洪泛及释放竞争 |
| 热切换 | 带线 OFF→ON、ON→OFF、反复拨动、切换中拔线 | 不依赖新 IN；先释放旧类；沿用排空/复位保护，无重复注册 |
| 正常业务 | 未进入及正常退出 DUT 后插线，BLE、双 ACL、HOGP、录音回归 | 无 CDC；原有正常业务保持，无不可接受的任务卡顿 |
| 背压 | RX 通知投递失败、TX 队列满、0 写、正数短写、主机停止读取 | 无死锁、无限等待或混帧；拒绝的动作未执行 |
| 断线与会话 | 半请求/半响应、RUNNING 时拔插、总线复位、取消配置、超时及串口重开 | 无资源释放竞争；失效可识别、旧包不进入新会话；已接受动作安全收尾，不自动重放 |
| 恢复 | DUT 退出、关机命令仍接外部供电、实验及量产标识的 MSC/CDC Windows 切换 | 蓝牙/HOGP 可恢复；当前标识驱动绑定通过，标识变更重跑 1A/1B |

达到以上验收并记录固件版本、配置、主机环境和实测结果后，才能将本文状态改为已实施/已验收。
