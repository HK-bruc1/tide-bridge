# BLE 一拖二双连接多服务共存实施方案——RDX 私有服务与 HOGP

> 架构基线：单链接复合 Profile 共享同一 GATT 数据库，在此基础上使用两个 `app_ble_hdl` wrapper 承载最多两条 BLE Peripheral 连接。
>
> 文档定位：本文只描述如何实施、验证和验收，不重新展开架构选型论证。设计缘由、产品边界和冻结决策见[四号设计理念](./4.BLE一拖二双连接多服务共存方案_RDX私有服务与HOGP.md)。
>
> 当前准入：Phase 0A-T（双 wrapper transport）、Phase 0B（双连接 ATT 隔离 POC）、Phase 1（双 link transport）、Phase 2A（`1 + 1 capability` 最小单元）、Phase 2B-1（RDX 核心 transport token 化）、Phase 2B-2（legacy query兼容视图收敛）、Phase 2B-3a/3b/3c（录音、OTA与源码异步边界 token 化）均已通过对应Host contract、固件构建与本阶段可执行的实机回归。Phase 2B-3d已完成在线Keymap源码请求、VM事务和响应的owner token收敛及固件构建。新增硬约束为`librdxApp.a`二进制不可修改；Phase 2B-4已按“每次上电最多一个RDX会话”完成安全降级实现，Host contract、静态库hash、固件链接、镜像打包和第11.7节核心真机矩阵均已通过。
>
> 实施状态：2026-07-22 Phase 2A 正式完成。唯一双 slot registry 已增加 `NONE / RDX / HID` capability、唯一 RDX/HID owner index、原子 claim 结果、同 link 互斥和断连前粘性 ownership；HID runtime 已定向绑定实际 owner wrapper。2026-07-23 已完成 Phase 2B-1至2B-3d的transport、录音、OTA与Keymap源码异步token边界。对静态库create/free路径的LLVM IR审计表明，现有ABI不能证明task、queue、timer、mutex和buffer完整退出，也没有可靠失败回滚，因此禁止运行时销毁/重建。Phase 2B-4改为安全降级：开机进入`READY`，首次合法RDX访问进入`ACTIVE`；该owner断开时先失效token并清理业务，再进入`FAILED`，同一开机周期拒绝后续RDX claim，但另一条HID链路继续可用；重启恢复`READY`。真实RDX命令和OTA输入已由runtime状态门按此模型放开。
>
> 范围：仅 BLE，不包含 Classic Bluetooth HFP、SCO 或其他经典蓝牙音频链路。

> **Phase 2 状态补充（2026-07-23）**：Phase 2B-4状态为“安全降级已实现 / Automated Gate Passed / On-device Gate Passed”。`librdxApp.a`冻结为不可修改二进制，内部receive/file/bulk队列不增加link token；当前量产候选边界是双BLE连接、一个HID owner、一个RDX owner，并且每次上电最多使用一个RDX会话。软件侧19/19 Host tests、固件编译/链接/打包和静态库SHA-256校验已通过；本轮验证镜像关闭RDX本地播放与OGG/Opus解码以释放CODE0，保留RDX录音所需的Opus编码。第11.7节第1至11项已通过真机验证，确认一拖二核心业务可行；反向连接顺序仍作为后续追溯项。

## 1. BLE 栈与编译配置

### 1.1 正式配置

目标配置为：

```c
config_le_hci_connection_num = 2;
config_le_gatt_server_num = 1;
config_le_gatt_client_num = 0;
config_le_sm_support_enable = 1;
```

含义如下：

| 配置 | 目标值 | 含义 |
|---|---:|---|
| `config_le_hci_connection_num` | 2 | 最多两条 BLE ACL connection |
| `config_le_gatt_server_num` | 1 | 设备只承担一套 GATT Server role/Profile 定义 |
| `config_le_gatt_client_num` | 0 | 设备不作为 BLE GATT Client/Central |
| `config_le_sm_support_enable` | 1 | 支持 HOGP 配对、加密与 bonding |

两个远端设备都是 Central，但本设备仍只承担 Peripheral/GATT Server 角色；本地不需要启用 GATT Client。

### 1.2 项目开关

建议在 `t2620_project_config.h` 中增加产品级正式开关：

```c
#define TCFG_RDX_HOGP_DUAL_LINK_ENABLE    1
```

`lib_btstack_config.c` 根据该开关选择 `config_le_hci_connection_num = 2`。不要把该值散落在 RDX 或 HOGP 模块中。

该开关只用于产品能力配置和受控实施，不得重新发展为单连接/双连接长期并存的复杂运行时模式。方案完成验收后，双连接应成为本产品唯一正式模型。

### 1.3 ATT RAM 与发送缓存

`multi_protocol_main.c` 已调用：

```c
ble_op_multi_att_send_init(att_ram_buffer,
                           ATT_RAM_BUFSIZE,
                           ATT_LOCAL_PAYLOAD_SIZE);
```

说明工程已经启用 multi ATT 发送基础设施。双连接实施时必须：

- 继续保留唯一的正式 ATT RAM 初始化入口；
- 删除或禁止使用 `rdx_ble_server.c` 中未接入初始化的重复 `att_ram_buffer`；
- 评估现有 `ATT_SEND_CBUF_SIZE` 对 RDX 业务发送与 HID report 的容量；
- 通过真实 App 文件/状态发送和连续 HID 输入测量峰值，而不是盲目扩大 RAM；
- 若扩容，统一修改 `multi_protocol_main.c` 的正式共享 buffer。

## 2. 双 wrapper 设计

### 2.1 数据结构

wrapper 和 link runtime 必须收敛到唯一固定数组。`rdx_ble_session.c`（也可命名为 `rdx_ble_link_registry.c`）是该数组、capability owner index 和 slot generation 的唯一所有者；`rdx_ble_server.c` 只负责 wrapper 初始化、广播策略和 SDK callback，不得再维护第二份连接状态影子。

```c
#define RDX_BLE_LINK_MAX 2
#define RDX_BLE_LINK_INVALID_INDEX 0xff

typedef enum {
    RDX_BLE_CAPABILITY_NONE = 0,
    RDX_BLE_CAPABILITY_RDX,
    RDX_BLE_CAPABILITY_HID,
} rdx_ble_capability_t;

typedef struct {
    void *ble_hdl;
    u16 con_handle;
    u16 mtu_size;
    u32 slot_generation;

    u8 connected;
    u8 encrypted;
    u8 peer_addr_type;
    u8 peer_addr[6];
    u8 capability;

    u8 rdx_ccc_configured;
    u8 rdx_stream_tx_ready;

    u8 hid_notify_enabled;
    u8 hid_suspended;
    u8 hid_protocol_mode;

    u8 conn_param_index;
} rdx_ble_link_ctx_t;

typedef struct {
    rdx_ble_link_ctx_t links[RDX_BLE_LINK_MAX];
    u8 rdx_link_index;
    u8 hid_link_index;
    u8 advertising_link_index;
    u32 transport_epoch;
} rdx_ble_transport_t;

typedef struct {
    u8 slot_index;
    u32 slot_generation;
    u32 transport_epoch;
} rdx_ble_async_token_t;
```

`slot_generation` 在 slot 每次建立新连接或释放时推进，用于拒绝旧 timer、跨 task 消息和异步完成事件；`transport_epoch` 在 BLE transport init/deinit、poweroff 和整体重建时推进，使上一生命周期的所有异步 token 一次性失效。同步 HCI/ATT 事件仍以 `hdl + connection_handle` 校验为主，不能假设原始 SDK 事件自带 slot generation。本文的 BLE `slot_generation` 与 keymap事务自身的 generation 是两个独立概念。

`link.capability` 是 slot业务归属的唯一权威字段，不再额外维护等价的 `rdx_active` 或 `hid_attached` 布尔值。`rdx_link_index` 与 `hid_link_index` 只是 registry 内部的派生快速索引，必须在同一次 claim/release 状态转换中与 `link.capability` 一起更新，并保持以下不变量：

```text
link.connected == 0
    → link.capability == NONE

link.capability == RDX
    → rdx_link_index指向该slot && hid_link_index不指向该slot

link.capability == HID
    → hid_link_index指向该slot && rdx_link_index不指向该slot
```

设备级名称、MAC、广播参数、发送 mutex 等全局配置可以继续保存在 server/global context；以下字段必须从全局 `rdx_ble_server_info_t` 移出：

- `ble_con_handle`；
- `ble_mtu_size`；
- `ccc_configured`；
- `stream_tx_ready`；
- 单一连接意义下的 `ble_conn`；
- 连接参数更新计数。

### 2.2 wrapper 初始化

两个 wrapper 均执行：

```c
app_ble_hdl_alloc();
app_ble_set_mac_addr(...same address...);
app_ble_profile_set(..., rdx_profile_data);
app_ble_att_read_callback_register(...);
app_ble_att_write_callback_register(...);
app_ble_att_server_packet_handler_register(...);
app_ble_hci_event_callback_register(...);
app_ble_l2cap_packet_handler_register(...);
app_ble_sm_event_callback_register(...);
```

任一分配或注册失败都必须回收已创建的 wrapper，并禁止进入半初始化运行状态。

### 2.3 wrapper 与 connection handle 映射

Connection Complete、Disconnection Complete、Encryption Change、MTU、连接参数、L2CAP 和 SM 等链路事件必须把 callback 的 `hdl` 一直传入最终处理函数，并同时校验 wrapper 与事件中的 connection handle：

```c
u16 event_handle = ...;

if (app_ble_get_hdl_con_handle(hdl) != event_handle) {
    return;
}
```

Connection与断连处理函数本身也必须保留wrapper参数，禁止在入口校验后调用一个丢失 `hdl` 的全局处理函数：

```c
static u8 rdx_ble_link_connected(void *hdl,
                                 const u8 *packet,
                                 u16 size,
                                 u8 enhanced);
static void rdx_ble_link_disconnected(void *hdl,
                                      const u8 *packet,
                                      u16 size);
```

随后通过 `hdl` 或 `event_handle` 定位唯一 link slot。不得再把每个 HCI 事件无条件写入同一个全局 session。

同步事件采用以下路由规则：

1. callback 的 `hdl` 必须属于 registry 中的一个 wrapper；
2. `app_ble_get_hdl_con_handle(hdl)` 必须与事件中的 connection handle 一致；
3. ATT read/write callback 中的 `hdl` 与 `connection_handle` 必须映射到同一 slot；
4. 同一 SDK 事件若被另一个 wrapper 的 callback 收到，必须无副作用地过滤；
5. slot 已释放或 connection handle 不匹配时，不得修改 capability、CCC、MTU、发送信号量或业务单例。

建议提供统一查询 API：

```c
rdx_ble_link_ctx_t *rdx_ble_link_find_by_hdl(void *hdl);
rdx_ble_link_ctx_t *rdx_ble_link_find_by_con_handle(u16 con_handle);
rdx_ble_link_ctx_t *rdx_ble_link_get_rdx(void);
rdx_ble_link_ctx_t *rdx_ble_link_get_hid(void);
u8 rdx_ble_link_active_count(void);
```

这些 API 读取的是同一个 registry，不允许 `rdx_ble_server_info_t`、HOGP 模块或录音模块另外缓存“最近一次 connection handle”作为路由依据。

## 3. 统一广播与连接槽管理

### 3.1 广播内容保持不变

继续使用三号方案已通过正式 App 与 Windows 实机验证的候选 C：

```text
Advertising Data:
    Flags
    Complete/Shortened Local Name

Scan Response:
    RDX Manufacturer Data（保持第一个 AD Structure）
    HID Service UUID 0x1812
```

不修改 Manufacturer Data 字节、AD Structure 顺序和 HID UUID。

### 3.2 单一活动 advertiser

虽然存在两个 wrapper，但任意时刻只启用一个空闲 wrapper 的 legacy connectable advertising：

```text
两个 wrapper 都空闲：
    选择 Link 0 广播

Link 0 已连接、Link 1 空闲：
    Link 1 广播

Link 1 已连接、Link 0 空闲：
    Link 0 广播

两个 wrapper 都已连接：
    停止广播
```

禁止两个 wrapper 使用同一设备地址同时开启重复 legacy advertising。

### 3.3 连接后的广播恢复

legacy advertising 通常在连接建立后由栈停止。Connection Complete callback 返回后，应通过短延迟 one-shot timer 在另一个空闲 wrapper 上恢复统一广播，避免与 wrapper 内部 connection handle 更新竞争。

该逻辑与三号方案“断连后延迟恢复广播”的经验一致，但触发条件改为：

```text
只要 active_link_count < RDX_BLE_LINK_MAX
且广播没有被 poweroff / DUT / Wi-Fi 等现有策略禁止
就选择一个空闲 wrapper 恢复广播
```

不再是“只有全部断开后才恢复广播”。

所有延迟广播、断连恢复和广播刷新必须复用同一个调度器，并遵守 stale-token 规则：

1. 调度前取消同类型旧 timer；
2. timer 参数携带 `slot_index + slot_generation + transport_epoch`；
3. callback 执行时重新校验 token，不相信调度时的连接计数；
4. 重新确认 `active_link_count < RDX_BLE_LINK_MAX`；
5. 重新确认目标 wrapper 仍空闲，且 `app_ble_get_hdl_con_handle(link->ble_hdl) == 0`；
6. 重新执行 poweroff、DUT、Wi-Fi 和现有广播禁止策略；
7. 任一条件不满足时直接退出，不开启广播；
8. deinit/poweroff 先取消所有广播 timer，再推进 `transport_epoch`。

示意逻辑：

```c
if (!rdx_ble_async_token_is_current(token)) {
    return;
}
if (rdx_ble_link_active_count() >= RDX_BLE_LINK_MAX) {
    return;
}
link = rdx_ble_link_get_by_index(token->slot_index);
if (!link || link->connected ||
    app_ble_get_hdl_con_handle(link->ble_hdl) != 0 ||
    !rdx_ble_adv_policy_allowed()) {
    return;
}
rdx_ble_adv_start(link);
```

### 3.4 名称或广播数据更新

名称、Manufacturer Data 等广播内容改变时：

- 更新设备级缓存；
- 若存在空闲 advertising wrapper，在该 wrapper 上安全重建；
- 已连接 wrapper 不需要为更新广播而断开；
- 两个 wrapper 下一次承担广播时必须使用相同最新内容。

## 4. Capability 激活与容量策略

### 4.1 连接建立时不分配身份

Connection Complete 只执行：

1. 找到对应 wrapper slot；
2. 推进该 slot 的 `slot_generation` 并保存 `con_handle`；
3. 初始化 link-level MTU、加密、peer 和连接参数状态；
4. 增加 active link count；
5. 停止自动关机；
6. 若仍有空闲 slot，安排另一个 wrapper 恢复广告；
7. 不激活 RDX；
8. 不激活 HOGP；
9. 不请求配对；
10. 不设置 owner。

### 4.2 HOGP capability 激活边界

以下行为可以激活 HOGP：

- 已加密链路对 `HID_INPUT_REPORT_CLIENT_CONFIGURATION_HANDLE` 写入 `0x0001`；
- 已绑定 peer 加密成功，并按该 peer identity 找到持久化 HID subscription；
- 目标 Windows 重连后，per-peer CCC 被安全恢复。

以下行为不能激活 HOGP：

- 读取 HID Service declaration；
- 读取 Report Map；
- 读取 HID Information；
- 读取 Device Information；
- 普通 GATT 全服务发现；
- 仅建立 BLE 连接。

原因是手机 App 或通用 GATT 工具可能枚举整个复合 Profile。若静态 HID read 就认领 HID slot或触发配对，会造成 App 链路误占 HOGP capability。

HOGP attach 的顺序必须固定为：

```text
按 hdl + connection_handle 定位 link
→ 校验CCC值、长度和link加密状态
→ 原子 claim HID capability
→ 将 HOGP 单例绑定到该 link 的 wrapper/handle/slot_generation
→ 更新或恢复该连接的 HID CCC
→ 满足加密、CCC 和非 suspend 条件后进入 ready
```

禁止先修改 `s_hid_notify_enabled`、peer、HOGP wrapper 或其他 HOGP 单例，再判断 claim 是否成功。

首次未加密的 HID Input CCC enable必须走独立的配对前置路径：

```text
收到CCC = 0x0001
→ link.encrypted == 0
→ 只调用sm_api_request_pairing(connection_handle)
→ 返回ATT错误INSUFFICIENT_ENCRYPTION（0x0f）
→ 不claim HID
→ 不写multi-ATT CCC
→ 不持久化subscription
→ 不绑定或修改HOGP单例
```

只有 Windows 在加密完成后重试 CCC enable，或已bonded peer加密完成并命中持久化subscription时，才允许claim HID。这样未完成配对或中途放弃的Central不会占用HID sink直到断连。Protocol Mode、Control Point和Output Report等其他HID动态写只能由现有HID owner处理，不得作为首次claim入口。

只有满足以下条件时才进入 ready：

```text
link.connected
&& link.capability == RDX_BLE_CAPABILITY_HID
&& link.hid_notify_enabled
&& link.encrypted
&& !link.hid_suspended
```

### 4.3 RDX capability 激活边界

以下行为可以激活 RDX：

- RDX notify CCC 开启；
- 收到合法 RDX value write；
- 收到合法 OTA/私有命令 characteristic write。

普通 GAP、Battery、HID 静态读取以及对 RDX value/CCC 的普通读取都不激活 RDX。只有成功校验的 RDX CCC enable 或合法私有写入才允许进入 RDX 协议库；claim 失败的输入不得继续传给 `librdxApp.a`。

RDX激活后，现有单例`librdxApp.a`、RDX发送队列、文件和录音状态绑定该link，直到该RDX link断开。断开后静态库runtime不得直接重绑定到新owner；当前安全降级路径进入`FAILED`，本次上电继续拒绝RDX输入，整机重启后才重新进入`READY`。

### 4.4 每条链路最多一种 capability、每种 capability 最多一个 sink

MVP容量策略为：

```text
active HID link <= 1
active RDX link <= 1
physical BLE links <= 2
capability per physical link <= 1
```

正式 MVP 采用同链路 capability 互斥：

```text
link.capability == RDX_BLE_CAPABILITY_RDX
    → 拒绝 HID claim

link.capability == RDX_BLE_CAPABILITY_HID
    → 拒绝 RDX claim
```

若第二条链路尝试开启已被另一条链路占用的 HID CCC：

- 拒绝新链路的 capability 激活；
- 返回明确 ATT error；
- 不清除旧 HID CCC；
- 不切换 report 发送目标；
- 不断开正在工作的 HID link。

若第二条链路尝试激活已被占用的 RDX session，同样拒绝新激活，不允许覆盖当前 RDX link。

这种策略避免第二个 PC 或第二个 App 抢占，同时不把 wrapper 序号固化为远端身份。

所有认领必须经过 registry 的唯一入口：

```c
int rdx_ble_link_claim_hid(rdx_ble_link_ctx_t *link,
                           u32 expected_slot_generation);
int rdx_ble_link_claim_rdx(rdx_ble_link_ctx_t *link,
                           u32 expected_slot_generation);
```

claim 必须在一次不可拆分的状态转换中完成：检查 link 仍连接、校验 slot generation、检查同链路 capability、检查全局同类 owner、设置 capability 与 owner index，然后才允许修改 HOGP/RDX 业务单例。claim 固定在 BTStack task 串行执行；其他 task 必须投递回 BTStack task，不得直接修改 registry。若未来允许跨 task 直接 claim，则必须增加临界区或等价同步。

claim 失败不能返回成功后静默丢弃，也不能抢占、切换或断开当前 owner。registry先冻结内部结果语义，ATT层再按实机验证结果映射错误码：

```c
typedef enum {
    RDX_BLE_CLAIM_OK = 0,
    RDX_BLE_CLAIM_BUSY,
    RDX_BLE_CLAIM_CONFLICT,
    RDX_BLE_CLAIM_STALE,
} rdx_ble_claim_result_t;
```

- 同一 link 重复 claim 已拥有的同类 capability：幂等成功；
- capability 已被另一 link占用：内部返回 `RDX_BLE_CLAIM_BUSY`；
- 当前 link 已拥有另一类 capability：内部返回 `RDX_BLE_CLAIM_CONFLICT`；
- link/slot generation失效：内部返回 `RDX_BLE_CLAIM_STALE`；
- HID/RDX CCC写 `0`：不触发claim，即使该link不是owner也只清除该connection的对应CCC；
- bonded HID subscription恢复前先claim HID；claim失败时不得恢复CCC、不得修改HOGP单例，也不得发起配对。

ATT错误码采用分阶段策略：

1. Phase 0B拒绝路径先将 `BUSY/CONFLICT/STALE` 映射为现有标准 `UNLIKELY_ERROR (0x0e)`；
2. 实测 Windows 对 HID Input CCC失败后的重试、配对、缓存和重连行为，确认不会出现无限重试、错误配对或异常缓存；
3. 只有验证 Windows与正式App均兼容后，才允许把 `BUSY/CONFLICT` 分别映射为application error `0x80/0x81`；
4. 若自定义码没有明确产品收益或存在兼容风险，正式实现继续统一使用 `0x0e`。

因此 `0x80/0x81` 不是当前冻结的外部ABI；冻结的是“忙、同链路冲突、stale”三种内部结果及其无副作用拒绝行为。

### 4.5 Capability ownership 为粘性生命周期

capability 一旦成功 claim，就绑定到该物理链路直到该链路断开：

- HID CCC 写 `0` 只令 `hid_notify_enabled/ready` 失效，不释放 `hid_link_index`；
- RDX CCC 写 `0` 只令 `rdx_ccc_configured/stream_tx_ready` 失效，不释放 `rdx_link_index`；
- HID suspend 只影响 ready，不释放 HID ownership；
- 不通过超时、静态读取、临时发送失败或后台状态自动迁移 capability；
- 只有匹配 owner link 的 Disconnect Complete 才释放 capability index；
- 若未来增加显式 App logout capability release，必须作为新协议行为单独设计，不能隐式复用 CCC disable。

## 5. ATT、CCC 与定向发送

### 5.1 ATT callback

read/write callback 已接收：

```c
void *hdl;
hci_con_handle_t connection_handle;
uint16_t att_handle;
```

所有动态处理都必须首先验证 `hdl` 与 `connection_handle` 映射到同一个 link context，并拒绝未知、已断开或映射不一致的 stale handle。禁止继续使用全局 `rdx_ble_session_is_current()` 的单链接语义。

静态读取与 capability 激活必须解耦：完整服务发现、Report Map/HID Information 读取、RDX value读取和 CCC读取均不得产生 claim、配对、业务连接回调或发送状态变化。

### 5.2 CCC 必须 per connection

所有 RDX/HID CCC 都使用：

```c
multi_att_set_ccc_config(connection_handle, att_handle, cfg);
multi_att_get_ccc_config(connection_handle, att_handle);
multi_att_clear_ccc_config(connection_handle);
```

当前 RDX callback 中的：

```c
att_set_ccc_config(handle, value);
```

必须删除或替换，否则两个连接会共享、覆盖或错误读取 CCC。

### 5.3 HID report 定向发送

HID 发送只使用当前 HID link 对应 wrapper：

```c
app_ble_att_send_data(hid_link->ble_hdl,
                      HID_INPUT_REPORT_VALUE_HANDLE,
                      payload,
                      sizeof(payload),
                      ATT_OP_NOTIFY);
```

不得无连接目标地广播给两个 wrapper，也不得根据最近一次连接选择目标。

### 5.4 RDX 数据定向发送

RDX response、notify、OTA 与 bulk data 只使用当前 RDX link：

```c
app_ble_att_send_data(rdx_link->ble_hdl,
                      rdx_att_handle,
                      data,
                      len,
                      op_type);
```

`app_ble_att_vaild_len_get()`、disconnect、remote MAC 查询和 send fail 统计也必须传入 RDX link wrapper。

### 5.5 ATT_EVENT_CAN_SEND_NOW

两个 wrapper 都可能收到发送事件。callback 必须：

- 先确认事件属于哪个 wrapper/connection；
- 只有 RDX link 的事件才能唤醒 RDX bulk-send semaphore；
- HID link 的 CAN_SEND_NOW 不得误清 RDX bulk flag；
- 断开后的迟到事件不得操作已复用 slot。

处理顺序必须至少满足：

```c
link = rdx_ble_link_find_by_hdl(hdl);
if (!link || !link->connected ||
    app_ble_get_hdl_con_handle(hdl) != link->con_handle ||
    link != rdx_ble_link_get_rdx()) {
    break;
}

/* 只有此后才能清 bulk/send-confirm 并 post RDX send semaphore。 */
```

若 CAN_SEND_NOW 事件格式中包含 connection handle，还必须同时比较事件 handle；若该 SDK 事件不携带 handle，则 RDX发送路径在请求/等待 CAN_SEND_NOW 时必须保存 `slot_index + slot_generation + transport_epoch` pending token，callback同时校验 `hdl` 当前映射和 pending token，且只能清除匹配 token。没有有效 RDX pending token 的事件不得唤醒发送信号量。

### 5.6 MTU 与连接参数

MTU、connection update index、interval 和 timeout 均改为 per-link。

MTU发送上限使用：

```c
ble_op_multi_att_set_send_mtu(connection_handle, mtu);
```

不得继续使用无 connection handle 的 `ble_op_att_set_send_mtu(mtu)`。

建议策略：

- HID link 以低延迟和稳定按键为优先；
- RDX link 以配置可靠性和合理吞吐为优先；
- 不使用一个全局 `connection_update_cnt` 同时驱动两条链路；
- 一条链路拒绝参数更新不影响另一条链路的更新状态。

### 5.7 业务模块只使用窄接口

`rdx_ble_server_info_t` 只保留名称、MAC、广播缓存、设备级 timer/mutex 和 wrapper资源，不再向录音、文件或 LED 模块暴露 per-link 字段。业务模块统一使用：

```c
u8  rdx_ble_rdx_is_connected(void);
u8  rdx_ble_rdx_stream_tx_ready(void);
u16 rdx_ble_rdx_mtu_get(void);
int rdx_ble_rdx_send(u16 att_handle,
                     const u8 *data,
                     u16 len,
                     u8 op_type);
```

必须清理 `rdx_ble_server_get_info()` 调用方对 `ble_con_handle`、`ble_mtu_size`、`ccc_configured`、`stream_tx_ready` 和单 wrapper 的直接访问，避免 registry 与公开结构形成两个真相来源。

### 5.8 携带 link 语义的异步操作必须使用 token

async token只约束会跨越当前调用栈并在未来再次访问BLE link语义的工作，不要求所有纯本地timer机械携带token。

凡异步回调、app_core消息、task queue、timer或完成通知会执行以下任一行为，投递时都必须复制 `slot_index + slot_generation + transport_epoch`，执行时重新解析并验证当前RDX/HID owner：

- 读取或修改RDX/HID owner；
- 读取connection handle、MTU、CCC、加密或peer状态；
- 调用BLE notify/indicate、查询发送长度或等待CAN_SEND_NOW；
- 操作与当前RDX link绑定的录音上传、OTA、文件同步、bulk buffer或keymap响应；
- 在断连后可能把旧业务结果发送给新owner或已复用slot。

```c
rdx_ble_async_token_t token = rdx_ble_link_token_capture(rdx_link);
post_to_app_core(..., token);

/* app_core/timer callback */
rdx_ble_link_ctx_t *link = rdx_ble_link_token_resolve_rdx(&token);
if (!link) {
    return;
}
/* 只有此后才能读取link状态或尝试BLE发送。 */
```

取消timer、清空队列或设置业务stop flag只能作为源码可控异步路径的辅助收敛，不能替代callback入口的token校验。RDX owner断开、slot复用或transport deinit后，旧token必须失效；新的RDX owner不得继承旧录音、OTA、文件或发送任务。

不可修改的`librdxApp.a`内部异步工作是唯一例外：其队列节点无法补充token，因此不能用“执行时读取当前owner”代替归属校验。第6.5节通过每次上电最多一个RDX会话保证旧静态库工作没有机会进入同一开机周期的新owner；源码token与boot-lifetime屏障是叠加关系，不互相替代。

纯本地且与BLE link无关的timer不适用，例如只更新本地LED动画、文件系统维护或不读取/发送任何连接状态的离线任务。是否需要token以callback实际访问的数据和副作用为准，而不是以源文件位于 `rdx_record.c`、`rdx_ota.c` 或 `rdx_app.c` 为准。

## 6. 连接生命周期与清理边界

### 6.1 RDX link 断开

仅当断开链路实际激活了RDX时执行。第一步必须把RDX runtime从`ACTIVE`原子切换到`QUIESCING`并关闭ATT输入门，随后才允许执行其他清理：

- 停止 RDX force-disconnect timer；
- 停止同步与文件 busy timer；
- 清理 RDX发送状态和 bulk buffer；
- 中断或收敛与 App 连接相关的在线录音/传输；
- 清理 OTA 会话；
- 调用 `rdx_hogp_keymap_config_on_disconnect()` 取消尚未完成的 App keymap事务；
- 清除该 link 的 RDX CCC 和 tx-ready；
- 释放 `rdx_link_index`；
- 将RDX runtime置为`FAILED`，保持输入关闭直到整机重启。

不得执行：

- 清除另一个 HID link 的 CCC；
- 清除 HID peer identity；
- 把按键路径切回离线；
- 主动断开 PC；
- 重置整个 BLE transport。

`HCI_EVENT_DISCONNECTION_COMPLETE`所在BTStack callback负责关闭输入、失效owner/token并执行local-only清理。不得在callback内同步删除/重建静态库协议任务，也不得用固定延时重新开放RDX。

### 6.2 HID link 断开

仅当断开链路实际激活了 HOGP 时执行：

- 清理 HID report runtime；
- 清理该 link 的 CCC、加密、suspend和peer缓存；
- 释放 `hid_link_index`；
- 使 `rdx_hogp_keyboard_is_ready()` 收敛为 0；
- 后续物理按键恢复离线产品功能。

不得执行：

- 取消 App keymap事务；
- 停止 RDX notify；
- 清理 RDX文件/OTA状态；
- 把 App连接标记为断开。

release report 的发送边界必须与 Disconnect Complete 分开：

- 本地主动断连前、keymap apply/切换前、HID suspend前和 CCC disable前：链路仍 ready 时可以尽力发送 release；
- 远端主动断开、supervision timeout 或异常掉线：不能保证发送，只清理本地 pressed/report state；
- 收到 `HCI_EVENT_DISCONNECTION_COMPLETE` 后：ACL 已不存在，禁止再调用任何 ATT send API；
- `rdx_hogp_on_disconnected()` 必须提供 local-only cleanup 路径，不能间接调用 `rdx_hogp_keyboard_release_all()`。

### 6.3 公共链路清理

任一连接断开都执行：

- 按 callback `hdl + disconnected_handle` 定位且只接纳对应 slot 一次；
- 在 slot 清零前记录其 capability 与 slot generation，并执行对应 local-only cleanup；
- 清理该 connection handle 的 runtime CCC；
- 仅当该 slot 是 capability owner 时释放对应 owner index；
- 清空连接态并推进 slot generation，使旧异步 token失效；后续SM/MTU/Encryption事件由 `hdl + connection_handle` 映射拒绝，CAN_SEND_NOW还必须校验RDX pending token；
- 减少 active link count；
- 若存在空闲 wrapper，延迟恢复统一广播；
- 只有 active link count 变为 `0` 才恢复自动关机策略；
- deinit/poweroff 时显式断开两个 wrapper 并逐个释放。

### 6.4 `ble_conn` 语义拆分

当前代码中的 `g_rdx_ble_server_info.ble_conn` 同时被用于：

- 任意 BLE 是否连接；
- RDX App 是否连接；
- LED 显示；
- 录音在线模式；
- 广告禁止；
- 自动关机。

双连接后必须拆成明确接口：

```c
u8 rdx_ble_link_active_count(void);
u8 rdx_ble_any_link_connected(void);
u8 rdx_ble_rdx_is_connected(void);
u8 rdx_hogp_keyboard_is_connected(void);
u8 rdx_hogp_keyboard_is_ready(void);
```

调用方必须按业务含义选择接口，禁止继续使用一个模糊布尔值。

### 6.5 不可修改静态库的RDX单次会话屏障

`librdxApp.a`是不可修改、不可重入的单例legacy runtime。LLVM IR审计确认，现有free/create接口不足以形成可证明的运行时重建屏障：`rdx_protocol_task_free()`先删除queue/semaphore/mutex再删除task且忽略返回值，`rdx_uxfile_task_free()`只删除task，UXFILE timer、mutex和buffer没有完整清理，create/init路径也没有完整失败回滚。因而当前实现明确禁止在owner切换时调用这些接口销毁或重建静态库。

当前安全边界是“每次上电最多一个RDX会话”，状态转换如下：

```text
boot/init: OFF -> STARTING -> READY
first valid RDX access: READY -> ACTIVE
RDX disconnect: ACTIVE -> QUIESCING -> FAILED
reboot: FAILED -> OFF -> STARTING -> READY
```

- `OFF`：BLE transport未初始化；
- `STARTING`：初次启动RDX runtime；
- `READY`：本次上电尚未消费RDX会话，只有此状态允许claim；
- `ACTIVE`：唯一RDX owner已绑定runtime epoch，合法命令和OTA输入可以进入静态库；
- `QUIESCING`：断连处理中，输入已关闭且旧token已失效；
- `FAILED`：本次上电的RDX会话已经消费，后续RDX claim、CCC enable和private value write保持拒绝，直到整机重启；
- `RESETTING`：为未来可能的可验证重建方案保留，当前生产路径不得进入。

`s_rdx_runtime_consumed_this_boot`是只增不减的boot-lifetime标志。BLE server或transport局部reinit不得清除此标志，避免用局部重初始化伪装成新的静态库生命周期。首次合法RDX CCC enable、普通命令或OTA写入必须先从`READY`成功claim并进入`ACTIVE`，然后才调用静态库入口；普通服务发现和静态读取不会消费本次会话。

RDX owner断开时必须按以下顺序执行：

1. `ACTIVE -> QUIESCING`，立即关闭RDX输入门；
2. 推进runtime epoch并失效owner token、pending send、CAN_SEND_NOW及sync-data/stream-ready timer token；
3. 执行现有RDX业务的local cleanup，释放该link的RDX capability；
4. 进入`FAILED`，同一开机周期不再允许新的RDX owner；
5. 不调用`rdx_protocol_task_free/create()`、`rdx_uxfile_task_free/init()`或固定延时重建；
6. 保持另一条HID链路、加密状态、CCC和report发送独立可用。

该降级不等于“一拖二失效”。目标场景仍是Windows占用HID owner、App占用RDX owner，两条BLE连接可以同时工作；限制仅发生在RDX owner断开之后：同一开机周期内App可以重新建立ACL，但不能再次使用RDX服务，必须重启设备后才恢复`READY`。

未来若要支持同一开机周期内RDX owner替换，必须由新的静态库契约提供逐task/worker退出确认、queue/timer/mutex/buffer完整销毁、可重复create及失败回滚。该能力属于可选增强，不能通过修改现有ABI、固定延时或忽略清理返回值实现，也不阻塞当前单次会话方案的实机可行性验证。

## 7. HID 在线热修改 Keymap

### 7.1 现有能力可直接复用

当前 `rdx_hogp_keymap_config.c` 已实现：

- request decode和参数校验；
- request id冲突处理；
- revision并发控制；
- 幂等重试；
- keymap CRC；
- 双 VM slot；
- prepare/apply/commit；
- VM失败回滚；
- executor publish；
- App断开时的 generation失效。

四号方案不修改该协议与持久化 ABI，只修正它所依赖的 BLE发送目标和断连边界。

### 7.2 热更新流程

```text
PC HOGP link 已 ready
        │
手机 App 通过 RDX link 发送 SET_KEYMAP
        │
RDX link 完成协议、revision、CRC和busy校验
        │
准备 inactive VM slot
        │
通过 HID link 发送/收敛旧 report release
        │
更新 RAM active keymap
        │
提交并校验 VM slot
        │
失败则恢复旧 keymap
        │
通过 RDX link 返回成功/失败响应
        │
下一次物理按键使用最新 committed keymap
```

整个过程中：

- PC HID connection handle 不改变；
- HID CCC 不关闭；
- Windows 不需要重新枚举设备；
- App 不需要主动断开；
- 不切换广播模式；
- 不触发 PC自动回连。

### 7.3 并发边界

keymap commit 在 `app_core` 串行执行，继续保留单 pending request。提交期间若物理按键事件可能并发进入 executor，应保证：

- 先取消旧 release timer；
- 先释放当前按键状态；
- 完整复制 keymap后再发布 active generation；
- 回滚时同样先 release再恢复旧 map；
- 任何错误都不能让半份 keymap 可见。

## 8. 安全、配对与 Peer 状态

### 8.1 HOGP安全策略

继续使用三号方案已经验证的：

```text
IO_CAPABILITY_NO_INPUT_NO_OUTPUT
+ Just Works
+ Bonding
+ Secure Connections
```

HID动态写继续要求加密。只有 Input CCC 写入或 bonded subscription恢复才触发 HOGP attach，不恢复“连接即配对”。

### 8.2 Peer identity 必须 per link

当前 `rdx_hogp_keyboard.c` 只缓存一个 `s_peer_identity_con_handle`。双连接后必须把 peer address/type 保存到对应 link slot，禁止通过全局“最近 peer”推断。

bonded HID subscription store 已支持多个 peer entry，可以继续复用；查询和更新必须传入当前 HID link 对应 peer identity。

以下情况必须拒绝：

- 全零 peer；
- 全 `0xFF` peer；
- stale connection handle；
- RDX手机 peer 继承 Windows的 HID CCC；
- 第二条链路覆盖第一条链路尚未完成的 subscription更新。

### 8.3 RDX访问策略

四号方案不把 wrapper、slot、HID bond或加密状态当作 RDX远端身份认证。沿用当前产品已批准的“连接并按协议访问即有权调用 RDX”语义。

若未来需要真正的 App身份认证，应增加独立挑战/响应或密钥协议；不得重新引入伪 owner作为替代。

## 9. 代码改造范围

### 9.1 `lib_btstack_config.c`

- 在产品开关下将 `config_le_hci_connection_num` 改为 `2`；
- 保持 `config_le_gatt_server_num = 1`；
- 保持 `config_le_gatt_client_num = 0`；
- 保持 SM支持启用。

### 9.2 `t2620_project_config.h`

- 增加正式双连接产品开关；
- 不修改工具生成的 `sdk_config.h/c`。

### 9.3 `rdx_ble_server.h/.c`

- 单 wrapper改为两个 wrapper数组；
- 删除 server 内 per-link影子状态，统一调用 link registry；
- Connection Complete不再覆盖全局 handle；
- 所有 HCI/L2CAP/SM/ATT入口传递并校验 wrapper + connection handle；
- 广告改为选择空闲 wrapper；
- 延迟广告使用 slot generation + transport epoch；
- ATT callback按 wrapper/connection映射；
- 静态 RDX/HID read不再触发 capability attach；
- RDX发送定向到 RDX wrapper；
- RDX CCC改为 multi ATT API；
- 连接参数和 MTU改为 per-link；
- 断连清理按 capability拆分；
- app disconnect只断开 RDX link；
- deinit断开并释放两个 wrapper。

### 9.4 `rdx_ble_session.h/.c`

- 作为唯一 link registry拥有固定双 slot、owner index、slot generation 和 transport epoch；
- 删除 `on_connected()` 中全局 reset现有连接的行为；
- 增加按 handle查找、分配、释放和计数接口；
- 提供唯一 HID/RDX claim API，并保证同链路 capability互斥；
- capability ownership保持到 owner link断开；
- 同步事件按 hdl + con_handle过滤，异步事件按 slot + generation/epoch过滤；
- 禁止 `rdx_ble_server_info_t` 或业务模块复制 per-link状态。

### 9.5 `rdx_hogp_keyboard.h/.c`

- HOGP runtime绑定明确 HID slot/wrapper；
- 静态 HID read不再 attach；
- 只有 claim成功后才修改 HOGP单例、CCC和peer状态；
- CCC、加密、suspend和peer按 HID link处理；
- report发送定向到 HID wrapper；
- SM事件按 connection handle过滤；
- Disconnection Complete只做local cleanup，不再尝试发送release；
- HID断连不清理 RDX session。

### 9.6 `rdx_hogp_key_action.c`

- 保持单一 active keymap；
- release与report发送使用当前 HID link；
- 保留 timer和apply时的 release边界。

### 9.7 `rdx_hogp_keymap_config.c`

- 保留现有协议、revision、CRC和VM事务；
- response始终通过当前 RDX link发送；
- 只有 RDX link断开才使 pending generation失效；
- HID link断开不取消 App配置事务。

### 9.8 `multi_protocol_main.c`

- 保留唯一 multi ATT初始化；
- 根据双连接压力结果调整正式 ATT发送 buffer；
- 避免 RDX模块维护未使用的重复 ATT RAM。

### 9.9 `rdx_app.c`、`rdx_record.c`、`rdx_ota.c`及文件同步模块

- 清理对 `rdx_ble_server_get_conn_handle()` 和 `rdx_ble_server_get_info()` 中per-link字段的直接依赖；
- 改用RDX owner窄接口，不缓存wrapper或“最近连接handle”；
- 所有会在未来读取link状态或尝试BLE发送的app_core消息、timer和task queue携带async token；
- RDX owner断开后旧token无条件失效，不能把旧录音、OTA、文件或keymap结果发送给新owner；
- 纯本地且不访问BLE状态的timer保持原有业务生命周期，不强制增加token。

## 10. 分阶段实施方案

### Phase 0A-T：纯 wrapper transport POC（已通过）

目标：只验证当前芯片、SDK二进制版本和 `config_le_gatt_server_num = 1` 下的双 Peripheral wrapper transport，不接入 RDX命令、capability ownership、HID report发送、订阅持久化或完整业务。此阶段验证的不是两个 wrapper 同时广播，而是任意时刻仅由一个空闲 wrapper广播，并依次建立两条同时存在的 Peripheral ACL。由于生产复合 Profile始终公开HOGP，Windows枚举必须使用最小HOGP控制面兼容层，不能再把所有动态写统一拒绝。

实施内容：

1. 设置 `config_le_hci_connection_num = 2`；
2. 分配两个 RDX wrapper；
3. 两者注册同一 Profile与callbacks；
4. 上电由第一个 wrapper广播；
5. 第一条连接的完整callback fan-out返回后，使用短延迟one-shot timer由第二个wrapper恢复相同广播；严禁在Connection Complete callback内同步开启第二wrapper广播；
6. callback日志记录 `hdl`、wrapper index、事件 connection handle和`app_ble_get_hdl_con_handle(hdl)`；
7. Connection Complete只允许事件发生时的active advertising wrapper接纳；其他wrapper即使暂时读到同一connection handle也必须拒绝并清理stale绑定；
8. 两个Central只做服务发现、静态读取以及Windows完成驱动枚举所需的HOGP控制面写入；
9. HOGP最小控制面只处理per-connection Input CCC、Protocol Mode、Control Point和Output Report的合法性与加密门控，不claim、不持久化、不进入HOGP单例、不发送report；
10. RDX和其他动态写继续返回标准 `0x0e`；
11. 任意一条断开后，另一条保持连接。

Go条件（transport范围）：

- 两个 Central 可以先后成功建立 ACL，并至少在日志观测窗口内同时保持连接；正式 App 因 Phase 0A-T 主动拒绝 RDX 业务写而主动断开，不计为 transport失败；
- 两个 wrapper映射正确；
- 无第二连接覆盖第一 wrapper handle；
- 同一次Connection Complete只有active advertising wrapper接纳一次；
- Windows HID Input CCC写成功且不会出现驱动错误；
- 无 ATT数据库异常或服务缺失；
- 单边断开不导致另一边HCI断开。

2026-07-22 实机验收证据：

```text
PC      → wrapper 0 / connection handle 0x0050
空闲槽  → wrapper 1延迟恢复统一广播
App     → wrapper 1 / connection handle 0x0051
双连接  → active count = 2，停止广播
App写入 → RDX 0x0009被POC返回0x0e
App断开 → reason 0x13（Remote User Terminated Connection）
PC链路  → 未被连带断开
```

该时序证明双wrapper能够依次承担单一活动广播并同时承载两条Peripheral ACL，wrapper/connection handle映射正确，Phase 0A-T判定为Go。它不证明HID report或RDX业务可用：PC链路仅完成HOGP枚举控制面，未claim HID、未进入ready；App链路的RDX runtime被主动隔离。

Phase 0A-T期间不得为了快速演示而把完整 RDX/HOGP 单例直接接入两个 wrapper。最小HOGP控制面只是公开复合Profile面向Windows时的枚举契约，不代表HID capability已经接入；本阶段只回答“双wrapper能否同时承载两条Peripheral connection”这一不可由预编译库静态证明的问题。

### Phase 0B：双连接 ATT 隔离 POC（已通过）

在 Phase 0A通过后验证：

- 两条连接分别读取同一份 Profile中的相同 handle；
- 两条连接的 MTU、CCC 和 notify目标相互独立；
- wrapper/connection映射在两条连接同时存在时保持稳定；
- 一个 wrapper 的 Disconnect Complete不会被另一个 wrapper重复处理；
- HID wrapper的CAN_SEND_NOW不会唤醒RDX发送信号量；
- 第二slot连接完成、slot快速复用或deinit后，旧ADV timer不会重新开启广播。

使用不接入HOGP业务单例的最小CCC test harness补充验证：

- 未加密 HID Input CCC enable只触发pairing request并返回 `0x0f`，不claim、不写CCC、不持久化；
- capability拒绝路径先返回标准 `0x0e`，观察Windows是否出现异常重试、错误配对、缓存污染或重连循环；
- 自定义 `0x80/0x81` 仅作为对照实验，不在Phase 0B前冻结为外部ABI。

Phase 0B通过前，不进入完整 capability和预编译RDX会话接入。Phase 0B的临时test harness不得演变成第二套生产registry或业务状态中心。

当前实现与验收状态（2026-07-22，Go）：

- 已增加仅供POC使用的双slot transport观测状态，按`wrapper + connection_handle`隔离连接、加密、MTU和generation；
- MTU已切换到`ble_op_multi_att_set_send_mtu(connection_handle, mtu)`；
- HID Input CCC与Battery CCC均使用`multi_att_set_ccc_config()`，断连仅清理对应connection的CCC；
- Battery CCC `0x000f`作为无业务副作用的定向notify harness：哪条连接开启CCC，就仅向该connection发送一次Battery Level；
- 未加密HID Input CCC enable仍只请求配对并返回`0x0f`，不写CCC、不claim、不持久化；
- CAN_SEND_NOW继续观测并丢弃，不唤醒RDX发送；
- 延迟连接广播和断连广播均校验`wrapper index + generation + transport epoch`；
- RDX动态写继续返回`0x0e`，正式RDX/HOGP业务单例仍未接入；
- Phase 0B Host contract及完整Host软件测试已通过；
- 实机确认`wrapper 0 / 0x0050`与`wrapper 1 / 0x0051`两条ACL同时存在，并分别完成per-link MTU配置；
- 两条连接均可读取共享Profile中的相同静态handle，包括Device Name `0x0003`与Battery Level `0x000e`；
- 两条连接分别写入Battery CCC后，日志确认notify定向调用对应`connection_handle`，且`0x0051`关闭CCC不影响`0x0050`；
- 两个wrapper的CAN_SEND_NOW均保持隔离，不唤醒RDX发送；
- 单边断开仅释放对应wrapper，并在该空闲wrapper上恢复统一广播；
- 第二连接占满slot后，旧连接广播timer被stale token正确拒绝；
- 正式App写RDX `0x0009/0x0015`仍按POC边界返回`0x0e`，随后主动断开不计为ATT transport失败。

实机验收证据：

```text
Link 0  → wrapper 0 / connection handle 0x0050 / MTU payload 514
Link 1  → wrapper 1 / connection handle 0x0051 / MTU payload 514
共享读取 → 两条link均可读取0x0003与0x000e
CCC隔离 → 0x0050与0x0051分别配置Battery CCC
定向发送 → Battery notify分别以0x0050、0x0051调用multi ATT发送
CCC关闭 → 0x0051写0只清除自身Battery CCC
事件隔离 → 两个wrapper的CAN_SEND_NOW均被观测并丢弃
断连恢复 → 单边0x13断连后，释放的wrapper恢复统一广播
stale防护 → 满连接时旧ADV timer输出stale connect ADV timer ignored
```

上述结果证明Phase 0B要求的共享ATT数据库访问、per-connection MTU/CCC、定向notify调用、事件过滤、断连隔离和stale ADV timer防护均已建立，Phase 0B判定为Go。该结论仅批准进入Phase 1 transport收敛，不表示正式RDX App会话或HID report已经接入。

### Phase 1：双 link transport（已通过）

- 建立唯一 link registry，不在 server/session分别维护数组；
- 完成 wrapper/handle映射；
- 完成 slot generation、transport epoch和统一异步token校验；
- 完成携带link语义的app_core消息、timer和task queue token边界；
- 完成 per-link MTU、加密、peer和连接参数；
- 完成单一空闲 wrapper广告调度；
- 完成 CCC multi API迁移；
- 冻结 link lifecycle host contract。

当前实现与验收状态（2026-07-22，Go）：

- `rdx_ble_session.c`已成为固定双slot link registry的唯一所有者，server内Phase 0B影子registry已删除；
- Connection Complete、Disconnect Complete和动态ATT入口均按`wrapper + connection_handle`解析同一slot；
- MTU与加密状态已写入匹配link，MTU发送配置继续使用per-connection multi ATT接口；
- peer address/type、初始连接参数与Connection Update结果均保存到匹配link，slot释放时随该link生命周期清理；
- 延迟广播已复用registry的`slot_generation + transport_epoch` token；Disconnect Complete先release slot并推进generation，再捕获空闲slot token，避免有效恢复任务被误判为stale；
- token解析已拆分为`rdx_ble_session_idle_token_resolve()`与`rdx_ble_session_link_token_resolve()`，分别冻结空闲广告任务和后续连接态业务任务的生命周期语义；
- Phase 1 Host contract冻结唯一registry、路由、per-link MTU/加密/peer/连接参数、异步token、multi ATT、断连广播顺序和capability detached边界；
- Host Software Tests 11/11通过，Phase 1 static contracts 8/8通过，固件编译与链接通过；
- PC-first实机确认`wrapper 0 / 0x0050`与`wrapper 1 / 0x0051`同时连接，两条链路分别完成事件路由和per-link transport操作；
- 两侧依次断开时，释放的wrapper均恢复统一广播并可重新连接，另一条链路保持；
- 满连接后的`stale connect ADV timer ignored`是旧延迟任务被正确拒绝；未出现`stale disconnect ADV timer ignored`、`registry conflict`或wrapper/connection mismatch。

实机验收证据：

```text
PC首次连接   → wrapper 0 / connection handle 0x0050
空闲槽广播   → wrapper 1 / rdx_adv_enable ret=0
nRF随后连接  → wrapper 1 / connection handle 0x0051 / active count=2
Link 0断开   → wrapper 0释放，wrapper 1连接保持
Link 0恢复   → wrapper 0恢复广播并重新连接为0x0050
Link 1断开   → wrapper 1释放，wrapper 0连接保持
Link 1恢复   → wrapper 1恢复广播，继续完成共享Profile访问
MTU隔离      → wrapper 1 / 0x0051完成MTU payload 514
CCC与notify  → Battery CCC及测试notify定向到0x0051，ret=0
stale防护    → 满连接时旧connect ADV任务被拒绝；无stale disconnect
```

上述结果证明Phase 1要求的唯一双slot registry、wrapper/connection映射、per-link transport状态、peer与连接参数归属、generation/epoch token、单一空闲wrapper广告调度和双向slot复用均已建立，Phase 1正式判定为Go，可以进入Phase 2。

Phase 1不提前实现capability业务语义。`rdx_ble_server_info_t`中的legacy单连接字段、RDX CCC/stream-ready、录音/OTA/文件异步任务、CAN_SEND_NOW和预编译协议发送边界，必须在Phase 2建立唯一RDX/HID capability owner后再迁移；否则无法区分“任意BLE连接”和“当前RDX/HID业务连接”。RDX/HOGP capability与正式定向发送在Phase 1继续保持detached。

### Phase 2：1 + 1 capability

#### Phase 2A：Capability owner最小单元（已通过）

本单元只建立双连接业务归属和安全隔离边界，不提前放开尚未完成async token迁移的RDX生产业务。已完成：

- 在唯一双link registry中增加 `NONE / RDX / HID` capability以及唯一`rdx_link_index`、`hid_link_index`；
- claim统一返回 `OK / BUSY / CONFLICT / STALE`，支持同类幂等、同link异类互斥和同类owner唯一占用；
- capability一旦claim即粘性绑定到物理link，CCC disable只改变ready/tx状态，ownership保持到物理断连；
- 静态ATT read不claim capability，不触发HOGP/RDX业务单例；
- 未加密HID Input CCC enable只请求配对并返回`0x0f`，不claim、不写CCC、不持久化；加密后的CCC重试或bonded subscription恢复才claim HID；
- HOGP runtime定向绑定实际HID owner wrapper，加密事件、CCC和report发送按owner路由；
- RDX私有value write与CCC进入唯一RDX claim入口，RDX CCC改用per-connection multi ATT接口；
- RDX生产命令、OTA等value write在进入预编译协议库前返回`0x0e`，CAN_SEND_NOW只识别RDX owner但不唤醒旧发送信号量；
- HID/RDX owner断连按capability分别清理，再释放registry slot并恢复统一广播。

2026-07-22阶段验证结果：

```text
Firmware Build and Link                         PASS（人工构建）
Unified Host Software Tests                     12/12 PASS
Phase 2 Capability Contracts                    10/10 PASS
git diff --check                                PASS
```

实机日志证据：

```text
COM3_2026-07-22_19-35-15.log
  - PC-first使用wrapper 0 / 0x0050成为HID owner，第二wrapper延迟广播；
  - 手机App在wrapper 1 / 0x0051 claim RDX，RDX生产写被fence；
  - RDX link断开后只detach RDX并恢复该wrapper广播，后续连接可重新claim；
  - nRF仅服务发现而未写业务CCC时保持NONE，不误claim capability；
  - bonded HID subscription恢复后达到ccc=1、enc=1、ready=1。

COM3_2026-07-22_19-42-46.log
  - 双连接期间HID report持续发送；
  - HID owner断开后HOGP runtime清理，释放wrapper恢复广播；
  - 两个wrapper均完成断开、复用和重连，stale connect ADV timer被拒绝。

COM3_2026-07-22_19-49-14.log
  - PC在wrapper 0恢复HID owner，nRF在wrapper 1同时在线；
  - nRF独立读取复合GATT、开启Battery CCC并完成定向notify；
  - 第二链路在线期间HID按键持续可用；
  - RDX owner侧CAN_SEND_NOW保持ignored，不唤醒未迁移的旧发送状态；
  - 未见wrapper mismatch、claim冲突、report发送失败、驱动错误或异常断连。
```

阶段判定：Phase 2A为Go并正式收尾。该判定证明双连接capability归属、HID owner数据面和RDX业务fence可用，允许停止重复的PC+nRF基础连接POC；它不批准解除RDX runtime fence，也不代表Phase 3在线Keymap或最终产品验收完成。

#### Phase 2A未验证项与已知追溯项

以下项目不阻塞本最小单元收尾，但必须保留到后续修正和回归，不能从最终验收矩阵中删除：

1. **Windows全新配对顺序尚未闭环。** 三份阶段日志中的PC均可通过既有bond恢复LTK和HID subscription；日志仍出现`Just Works rejected: no HID candidate`。SDK可能在应用层收到未加密Input CCC callback前发出`SM_EVENT_JUST_WORKS_REQUEST`。后续应在双方清除bond后验证首次配对；若复现驱动错误，应允许connected且capability为`NONE`的link成为provisional HID pairing candidate，只确认Just Works，不claim HID、不写CCC。
2. **Disconnection Complete后的release路径需回归。** `COM3_2026-07-22_19-42-46.log`曾在disconnect accepted后打印全零`HOGP report_send`。ACL完成断开后不得再调用ATT send；后续应把该路径收敛为取消按键release timer并仅清理本地pressed/report状态。本项在`COM3_2026-07-22_19-49-14.log`中因没有发生断连而未复验。
3. **真实设备BUSY/CONFLICT矩阵未穷举。** Host contract已覆盖claim结果、幂等和同link互斥，但第二个同类owner抢占、RDX后尝试HID以及HID后尝试RDX仍需在后续回归抽测。
4. **反向连接顺序未正式冻结。** nRF/App先claim RDX于wrapper 0、Windows后claim HID于wrapper 1的wrapper inversion场景尚未形成完整验收日志。
5. **RDX核心双连接业务已通过实机验收。** Phase 2B-3a/3b/3c/3d已关闭录音、OTA和Keymap等源码可控的owner token边界，Phase 2B-4已用每次上电最多一个RDX会话隔离不可修改静态库的receive/file/bulk任务；真实RDX命令、录音启停与保存、并行HID上报、断连后HID保持以及重启后RDX恢复均已通过。OTA、完整文件业务和在线Keymap仍按各自后续阶段验收。
6. **长稳和压力测试尚未执行。** 包括接近同时连接、自动回连竞争、反复断连复用、多次上电、持续HID输入叠加高频App notify以及slot generation/transport epoch长期回绕行为。

#### Phase 2B：RDX transport与异步业务迁移（安全降级及核心实机验收已通过）

- 审计`librdxApp.a`外部transport边界，确认输入、输出、CAN_SEND_NOW和断连清理均可重绑定到唯一RDX owner；
- 为所有会读取RDX owner、connection handle、MTU、CCC或执行BLE发送的跨task消息和timer携带async token；
- 迁移录音、OTA、文件同步、bulk发送、异步App响应和完整CAN_SEND_NOW状态；
- 在`librdxApp.a`不可修改的前提下，将其作为单例runtime按RDX会话串行使用；旧owner断开后进入`FAILED`，同一开机周期不允许新RDX owner claim；
- 使用runtime状态门解除首次RDX会话的production value write fence，并完成真实App业务回归；
- 根据Windows与正式App实机结果冻结claim内部结果到ATT错误码的最终映射；
- 完成RDX/HID定向发送和断连清理的最终回归。

Phase 2B的当前隔离模型是“源码异步token + 每次上电一个RDX会话”。前者阻止源码可控的旧请求、timer和响应越过owner边界；后者保证无法携带token的静态库receive/file/bulk工作不会在同一开机周期遇到新RDX owner。两者缺一不可，且不要求、不允许修改静态库ABI。

##### Phase 2B-1：RDX核心transport token化（已通过）

2026-07-23完成以下最小单元：

- registry新增`rdx_ble_session_rdx_token_capture()`与`rdx_ble_session_rdx_token_resolve()`，解析时同时校验slot、generation、transport epoch、RDX owner和runtime active；
- 普通发送与OTA发送先捕获不可变snapshot，冻结`token + wrapper + connection handle + MTU`，ATT入队前再次校验wrapper当前handle；
- pending token在ATT入队前建立，入队失败时按同一token回滚；连续成功入队使用计数，避免多个CAN_SEND_NOW只接纳第一次；
- CAN_SEND_NOW先按事件wrapper解析当前link，再校验RDX ownership和pending token，匹配后才允许清bulk/send-confirm并post预编译库发送信号量；
- RDX owner detach、transport init和deinit均清除pending token，旧slot或旧epoch事件不能唤醒新owner；
- `rdx_ble_server_get_conn_handle()`、`rdx_ble_server_get_mtu()`和`rdx_ble_server_is_stream_tx_ready()`在双连接构建中改为解析当前active RDX owner，不再直接返回legacy全局链路字段；
- 新增`test_rdx_dual_link_phase2b_transport.ps1`，冻结上述边界；RDX production value write仍返回标准`0x0e`，本单元不解除输入fence。

验证结果：

```text
Unified Host Software Tests                     13/13 PASS
Phase 2B Core Transport Contracts                6/6 PASS
git diff --check                                PASS
Firmware Build and Link                         PASS
PC-first双连接实机回归                          PASS
```

2026-07-23实机日志`COM3_2026-07-23_10-33-41.log`确认：

- PC以wrapper 0 / `0x0050`保持bonded HID连接，nRF以wrapper 1 / `0x0051`接入第二条链路；两条链路同时在线时HID report持续发送，无驱动错误、report失败或异常断连；
- 第二条链路接入后旧广播任务输出`stale connect ADV timer ignored`，没有在满连接状态错误恢复广播；
- nRF写RDX CCC后只在wrapper 1 claim RDX owner，日志保持`runtime=fenced`；随后RDX value `0x0006`写入被明确fence，没有进入预编译协议业务；
- HID发送期间产生的CAN_SEND_NOW在RDX wrapper侧两次输出`can_send_now ignored: no current RDX pending token`，没有出现accepted日志，也没有唤醒旧RDX发送信号量；
- nRF主动断开只detach RDX owner，并在释放的wrapper 1恢复统一广播；重新连接后可再次claim RDX，连续两次value写仍被fence，PC链路未被清理；
- 日志只包含上电时的正常Reset Source，未发现assert、watchdog、wrapper mismatch、registry conflict或claim rejected。

本次串口捕获夹杂大量高频底层输出，nRF第二次连接时的Connection Complete文本未被完整保留，但这不构成功能失败：断连路径已清空slot并推进generation，后续RDX claim只有在新的Connection Complete重新接纳该slot后才能成功；紧随其后的connection update和ATT访问也确认wrapper 1已重新在线。该证据限制保留在文档中，后续长稳或断连压力测试应使用降低噪声的日志配置复核完整事件序列。

`librdxApp.a`符号审计结果：

- `rdx_protocol.c.o`的普通/OTA输出引用`rdx_ble_server_send()`与`rdx_ble_server_ota_send()`，连接和MTU查询引用server窄接口；符号表未发现其直接引用`app_ble_hdl`或`app_ble_att_send_data()`；
- 该对象仍直接引用`ble_user_cmd_prepare()`与`rdx_ble_server_get_info()`；Phase 2B-1完成时尚未还原直接BLE命令参数和legacy结构字段访问，因此当时不能证明其multi ATT语义；后续Phase 2B-2已通过LLVM IR确认命令号、参数和实际字段读取范围；
- 因此Phase 2B-1的结论是“核心输出与发送完成边界可控”，不是“预编译库transport审计全部通过”。Phase 2B-2关闭legacy查询兼容视图问题后，跨task业务token和真实bulk/file活性验证仍是解除RDX production input fence的前置条件。

本单元已完成Host contract、人工固件构建和PC-first实机回归，阶段判定为Go。该结论只批准继续迁移RDX异步业务，不批准解除production value write fence；正式RDX命令、录音、OTA、文件同步和在线Keymap仍不属于本单元验收范围。

##### Phase 2B-2：legacy query兼容视图收敛（已通过）

2026-07-23完成以下最小单元：

- 对`librdxApp.a`中的`rdx_protocol.c.o`执行LLVM bitcode审计，确认`rdx_protocol_get_buffer_vaild_len()`固定调用`ble_user_cmd_prepare(9, 1, ...)`，即单连接`BLE_CMD_ATT_VAILD_LEN`；
- 同一审计确认预编译对象通过`rdx_ble_server_get_info()`只读取结构字段index 2和index 6，对应`ble_conn`与`ble_mtu_size`，没有读取wrapper、CCC、stream-ready或send mutex；
- `rdx_ble_server_get_info()`保留符号和结构ABI，但在双连接构建中返回前根据active RDX owner token刷新legacy连接、MTU、CCC和ready视图；无active RDX runtime时明确返回disconnected视图，不把HID link伪装为RDX会话；
- 该兼容视图不修改`rdx_ble_server_hdl`，设备级primary wrapper仍只由server生命周期持有，唯一link registry仍是链路状态的唯一真相来源；
- `rdx_record.c`不再直接读取legacy `ccc_configured/stream_tx_ready`，在线音频发送门控统一调用`rdx_ble_server_is_stream_tx_ready()`；
- `rdx_led_ctrl.c`改用`rdx_ble_server_has_active_link()`判断任意物理BLE link，避免PC仅占用HID时被错误显示为未连接；
- 除server中的ABI兼容函数外，RDX源码模块已不存在`rdx_ble_server_get_info()`调用或`rdx_ble_server_info_t *`依赖；
- 新增`test_rdx_dual_link_phase2b_compat.ps1`，冻结兼容ABI、owner视图、源码窄接口、LED物理连接语义、录音ready门控和owner wrapper最终容量检查。

`BLE_CMD_ATT_VAILD_LEN`的处理边界如下：

- 预编译库中的旧查询无法在不替换vendor archive或全局包装variadic stack API的情况下改写为`BLE_CMD_MULTI_ATT_VAILD_LEN`，本阶段不采用这两种高风险手段；
- 旧查询只能作为预编译发送任务的流控提示，不能作为发送目标或所有权判断；
- 真正ATT入队前，`rdx_ble_server_send()`仍使用snapshot中的RDX owner wrapper执行`app_ble_att_vaild_len_get(send_hdl)`，随后再次校验owner token、wrapper和connection handle；因此旧查询即使高估空间，最终入队也会被owner wrapper检查拒绝，不会把数据发到HID link；
- 旧查询若低估空间，可能造成预编译bulk/file任务延迟或停顿。该问题属于吞吐/活性风险，不是定向发送隔离的证明；正式解除输入fence前必须在真实App的bulk/file回归中验证。

当前验证结果：

```text
Unified Host Software Tests                     14/14 PASS
Phase 2B Compatibility Contracts                 7/7 PASS
Phase 2B Core Transport Contracts                6/6 PASS
git diff --check                                PASS
Firmware Build and Link                         PASS (user manual build)
PC-first双连接回归                              PASS
```

本单元的最小实机验收不测试RDX业务：

1. PC-first连接并确认HID可输入，随后nRF连接第二条链路；
2. nRF只写RDX CCC以claim RDX owner，日志必须仍为`runtime=fenced`；
3. nRF写RDX value必须继续返回/记录fenced，不应进入App命令处理；
4. 两条链路同时在线时持续触发HID输入，PC不得驱动错误、掉线或失去输入；
5. nRF断开并重连一次，PC HID保持在线，释放slot恢复广播，新连接可重新claim RDX；
6. 全程不得出现wrapper mismatch、stale token accepted、assert或watchdog。

2026-07-23实机验收证据：

```text
COM3_2026-07-23_11-05-03.log
  - PC以wrapper 0 / 0x0050恢复bonded HID owner，nRF以wrapper 1 / 0x0051接入第二条链路；
  - nRF写RDX CCC后claim RDX owner并保持runtime=fenced，RDX value写入继续被明确fence；
  - 双连接在线期间HID press/release持续发送，RDX wrapper的CAN_SEND_NOW因无pending token被拒绝；
  - nRF断开只detach RDX owner，释放的wrapper 1恢复统一广播，随后成功重连；
  - nRF重连后PC HID仍可发送，未出现驱动错误或PC链路被连带断开。

COM3_2026-07-23_11-13-57.log
  - 重连后的wrapper 1 / 0x0051再次写RDX CCC，连续两次均得到幂等claim并保持runtime=fenced；
  - claim前后两次RDX value写入均保持fenced，没有进入预编译协议业务；
  - wrapper、event connection handle与wrapper connection handle一致；
  - 未发现wrapper mismatch、stale token accepted、assert、watchdog或异常复位。
```

阶段判定：Phase 2B-2为Go并正式收尾。它只关闭legacy查询结构的源码扩散和预编译库兼容视图问题；跨task录音、OTA、文件、bulk、异步App响应与在线Keymap仍未携带完整业务token，production value write fence继续保留。后续进入Phase 2B剩余异步业务迁移时，不得把本阶段Go解释为真实App业务已经可用。

##### Phase 2B-3a：录音控制命令异步 token 化（已通过受控验收）

本单元只收敛App下发录音控制命令的第一段异步生命周期，不解除production value write fence。已完成：

- `PROTOCOL_EVENT_CMD_RECORD`收到命令时复制`Record_info`并捕获当前active RDX owner token，不再把无归属的packed值直接投递到`app_core`；
- `app_core`回调在停止本地回放或修改录音状态前重新解析token，owner断开、slot复用或transport epoch变化后直接释放请求并丢弃；
- `rdx_record_cmd_handle_from_rdx()`将token继续传入录音控制层，本地按键、暂停兜底等非App路径继续使用原有无token入口，两者语义保持分离；
- START命令等待`stream_tx_ready`时保存同一token，每次timer重试和真正执行前都重新校验；
- 新RDX owner到来时，可以取消并替换旧owner留下的stale延迟命令，避免旧timer短暂阻塞新会话；
- app_core队列投递失败、内存申请失败和stale回调均有明确清理路径，不遗留请求对象；
- 新增`test_rdx_dual_link_phase2b_record_async.ps1`并接入统一Host runner，冻结上述边界。

Host验证结果：

```text
Phase 2B Record Async Contracts                 6/6 PASS
git diff --check                                PASS
```

本单元尚未覆盖，且仍是解除RDX输入fence的前置工作：

- 录音task启动后，在线录音session尚未持有固定RDX token；
- `rdx_protocol_audio_data_indicate()`音频帧、record state/trigger indicate和录音结束回调尚未按录音session token校验；
- OTA、文件同步、bulk、SD异步结果、通用App响应和在线Keymap响应仍未完成token迁移；
- production RDX value write继续返回`0x0e`，因此当前只能验证静态合约和双连接回归，不能用真实App命令证明本单元完成。

人工固件构建和基础实机回归已完成，证据为未提交的`COM3_2026-07-23_11-53-28.log`：

- PC-first在wrapper 0 / `0x0050`保持HID链路，物理按键持续产生press/release report；
- nRF在wrapper 1 / `0x0051`成功claim RDX capability，私有value write继续被fence拒绝；
- nRF断开时只detach RDX owner并在wrapper 1恢复广告，随后可重连；
- nRF重连后PC HID仍持续发送report；未发现wrapper mismatch、stale accepted、assert、watchdog或运行中异常复位。

阶段判定：Phase 2B-3a为Go并正式收尾。该结论只验证token化改动没有破坏现有双连接transport，且因为production fence仍在，真实录音命令不会进入新代码路径；不能将本单元Go解释为录音业务或stale-drop行为已经实机覆盖。待后续录音数据面也完成token化并有受控测试入口后，再专项制造“录音START已排队但RDX owner立即断开并重连”的场景，确认出现stale drop且新owner不会继承旧录音命令。

##### Phase 2B-3b：在线录音 session 与音频数据面 token 化（已通过受控验收）

本单元将录音控制命令携带的RDX owner token继续绑定到实际在线录音session，重点关闭“旧录音task在RDX重连后向新owner发送音频”的数据面漏洞，仍不解除production RDX value write fence。已实现：

- App来源的START命令在真正修改`record_status.run`前，将命令携带的token固定为本次在线录音session token；PAUSE保留ownership，物理STOP完成`rdx_record_run_exit()`后才释放；
- active session存在时，来自不同slot generation或transport epoch的新RDX owner不能控制、接管或继承旧录音session；本地无token录音入口继续保持独立语义；
- 两套产品编译分支的`rdx_record_run_init()`都只有在固定session token仍解析为当前active RDX owner时才进入`RECORD_MODE_ONLINE`，否则按离线录音处理；
- 两套`rdx_record_run_data_handle()`中的`rdx_protocol_audio_data_indicate()`均在发送前校验固定session token、stream中断状态和当前RDX stream-ready状态；
- 录音启动/退出路径中的直接record-state indication只在固定session token仍有效时发送，旧session失效后不会动态改投新owner；
- 3秒stream-resume timer在投递时复制固定session token，callback同时校验timer token、session token和当前RDX owner；断连、slot复用或新owner接入后旧timer只执行stale drop；
- 新增`test_rdx_dual_link_phase2b_record_session.ps1`并接入统一Host runner，冻结session绑定、owner替换拒绝、双产品分支数据门控、timer token和STOP释放边界。

软件验证结果：

```text
Host Software Tests                              16/16 PASS
Phase 2B Record Session Contracts                 7/7 PASS
git diff --check                                      PASS
Firmware Build and Link                               PASS
PC-first + nRF Controlled Regression                  PASS
```

本单元的人工验收继续保持受控范围：

1. 人工编译并烧录固件，确认无新增编译告警或链接错误；
2. PC-first连接并启用HID，持续验证物理按键press/release正常；
3. nRF连接第二条链路、写RDX CCC完成capability claim，确认production runtime仍显示`fenced`；
4. nRF断开并重连，确认只detach/reclaim RDX owner，PC HID始终在线且继续出键；
5. 日志中不得出现wrapper mismatch、stale event accepted、assert、watchdog或运行中异常复位。

由于production fence仍会拒绝RDX value write，本轮实机回归不能真正启动App在线录音，也不能直接观察音频帧stale drop。正式解除fence前仍需完成：

- `rdx_app.c`中的record trigger/state timer及其`app_core`队列消息按投递时session token校验，不能只在执行时读取当前全局session；
- 非当前产品分支中仍直接投递的`rdx_protocol_record_state_indicate`回调，以及录音mark、完成/退出异步结果逐项token化；
- 录音数据面具备受控测试入口后，专项制造旧owner START排队、断开、slot复用和新owner重连，确认旧命令、旧timer、旧音频和旧状态均不会到达新owner；
- OTA、文件同步、bulk、SD异步结果、通用App响应和在线Keymap响应继续按后续最小单元迁移。

人工固件构建和基础实机回归已完成，证据为未提交的`COM3_2026-07-23_14-03-24.log`：

- 两个wrapper正常注册，PC-first在wrapper 0 / `0x0050`完成HID owner绑定并持续产生HID report；
- nRF在wrapper 1 / `0x0051`连接，RDX CCC写入成功claim capability且明确保持`runtime=fenced`；
- RDX value write按预期返回fenced，没有进入预编译协议业务；
- wrapper 1随后再次完成Connection Complete，重连后PC HID仍继续发送report；未发现wrapper mismatch、assert、watchdog或运行中异常复位；
- 两次`stale connect ADV timer ignored`属于异步token防护按预期拒绝旧timer，不是验收失败。

本次串口输出存在明显交织，RDX断连、owner detach和恢复广播的完整文本行未被保留；但wrapper 1第二次Connection Complete且`connected_count=2`，可间接证明旧连接已释放并完成重连。日志中重复的`add_sys_timer_in_irq 6054744`来自SDK BLE controller路径，本单元未将其视为录音session回归失败，保留到长稳阶段继续追踪。

阶段判定：Phase 2B-3b为Go并正式收尾。该结论证明源码可构建、双连接transport未被破坏，且基础断连重连后HID仍持续工作；由于production fence仍在，真实在线录音、音频帧stale drop和完整record state/trigger异步链路尚未实机执行，不能据此宣称录音业务已完整可用，也不得解除production RDX输入fence。

##### Phase 2B-3c：录音剩余状态、OTA 与源代码异步边界 token 化（源码完成，业务仍 fenced）

本单元把 Phase 2B-3b 之后仍在源码控制范围内的链路继续收敛，但不宣称正式 RDX App 已解锁：

- 录音 state/trigger 队列消息改为复制 `slot_index + slot_generation + transport_epoch`，`app_core` 执行前重新解析；
- 录音 mark、state indication 和结束/退出路径只允许向固定在线录音 session token 对应的 owner 发送；
- 录音状态延迟 timer 在投递时捕获 token，owner 断开、slot 复用或 transport epoch 变化后丢弃；
- OTA begin 建立固定会话 token，后续数据包、超时回调和 OTA response 均验证该 token；
- 普通 RDX/OTA 源码异步出口增加 token-bound send API，禁止回调先校验旧 owner、发送时又重新捕获新 owner；
- 断连延迟文件清理在执行时检查是否已有 replacement RDX owner，避免清理新会话的 file/bulk 状态；
- production RDX value-write fence 继续保留。

本单元的限制必须明确记录：`librdxApp.a` 内部的 file/bulk 接收队列、发送任务和若干 timer 没有源码级 token 参数，当前只能通过现有 disconnect cleanup 和发送出口做外围防护，尚不能证明旧预编译任务不会在 slot 复用后访问新 owner。因此，不能在本单元后解除 production fence，也不能把 Phase 3 视为已进入。

验证要求：Host source-async contract、完整 Host runner、人工固件构建均通过；真实录音/OTA/file/bulk 业务回归须在预编译库边界审计完成后另行执行。

人工固件构建和 PC-first+nRF 受控回归已完成，证据为 `COM3_2026-07-23_14-45-45.log`：

- 两个 wrapper 正常注册；PC 连接到 wrapper 0 / `0x0050`，完成加密并成为 HID owner，`ccc=1 / enc=1 / ready=1`；
- nRF 连接到 wrapper 1 / `0x0051`，日志出现 `connected_count=2; defer idle-wrapper advertising`，两条链路同时在线；
- 双连接期间 HID 持续产生 press/release report，未出现 PC 驱动错误、HID report 发送失败或异常断连；
- nRF 的 RDX capability claim 只绑定 wrapper 1，并保持 `runtime=fenced`；RDX value write 被记录为 `RDX value write fenced`，没有进入预编译协议业务；
- 满连接期间两次出现 `stale connect ADV timer ignored`，属于旧广播任务被 generation/epoch 正确拒绝；
- wrapper 1 后续再次完成 Connection Complete，PC HID 仍继续发送，说明 slot 复用没有破坏 HID owner；
- 未发现 `wrapper mismatch`、`stale token accepted`、assert、watchdog 或异常复位。

本次串口输出没有完整保留 `Disconnection Complete`、owner detach 和恢复广播文本，因此断连隔离不能仅凭本日志单独闭环；wrapper 1 后续再次完成 Connection Complete 可作为 slot 释放/复用的间接证据。日志中的大量 `add_sys_timer_in_irq 60549b4` 与此前阶段一致，暂归类为 SDK BLE controller 路径警告，保留到 Phase 4 长稳回归继续追踪；上电阶段的 `power on tone play err` 也不属于双连接 transport 失败。

阶段判定：Phase 2B-3c 受控实机回归为 Go，可以正式收尾本源码迁移单元。该结论只证明源码级录音/OTA 异步 token 改造没有破坏双连接 transport、HID 定向发送、RDX owner claim 和 stale timer 防护；不解除 production RDX input fence，不代表录音、OTA、文件同步、bulk 或在线 Keymap 业务已经验收。

##### Phase 2B-3d：在线 Keymap 源码事务与响应 token 化（源码完成，业务仍 fenced）

本单元只关闭在线Keymap在源码可控范围内的跨task和发送归属，不修改预编译RDX静态库ABI，也不进入Phase 3正式业务验收：

- 保持`librdxApp.a`导入的`rdx_app_custom_command_parse(char *, char *)`签名、`RdxProtocolCallbacks`布局、`rdx_ble_server_info_t`布局和既有server入口不变；
- service私有的`rdx_hogpkm_owned_request_t` envelope组合transport-neutral请求与RDX async token；custom回调捕获active RDX owner后再投递`app_core`，codec/store头文件不依赖BLE；
- pending处理、VM prepare、RAM executor apply、VM commit发布和最终响应前均重新验证同一个token，owner断开、slot复用或transport epoch变化后停止后续副作用；
- decode错误和BUSY响应使用独立token-bearing队列对象，投递失败、stale回调和正常完成均释放对象；
- Keymap响应不再进入`rdx_protocol_packet_send_priority()`的预编译单例队列，而是调用`rdx_ble_server_send_for_token()`定向入ATT队列；
- Host contract固定`librdxApp.a`的SHA-256为`C540D70540DC4D61E15D1CA13579CD2342D4EA972FF0A74A1AFCCC04B1EF4ACA`，防止无审计替换静态库后继续沿用当前ABI结论；
- production RDX value-write fence继续保留。

本单元仍不能解决静态库接收队列的输入归属：ATT value write在进入`rdx_app_custom_command_parse()`前，已经经过`librdxApp.a`内部不携带token的receive queue。源码回调只能捕获“执行回调时”的owner，无法证明它就是“接收原始ATT包时”的owner。因此当前改造可以阻止已进入Keymap源码的旧请求、旧事务和旧响应跨会话泄漏，但不能作为解除production input fence的依据。

软件验证结果：

```text
Phase 2B Keymap Async Contracts                  8/8 PASS
Host Software Tests                           18/18 PASS
Firmware Build and Link                             PASS
git diff --check                                    PASS
```

阶段判定：Phase 2B-3d源码与Host contract收敛为Go，本源码单元正式关闭；尚未执行真实Keymap业务和双连接实机回归。静态库已冻结且不再把“获取新版静态库或扩展ABI”作为当前一拖二验证的实施前提；遗留receive/file/bulk跨会话风险由Phase 2B-4的单次会话屏障关闭。不得通过改变现有静态库函数签名、结构体布局或调用约定绕过该限制。

##### Phase 2B-4：不可修改静态库的单次RDX会话屏障（安全降级已实现 / Automated Gate Passed / On-device Gate Passed）

静态库清理API审计未能证明runtime可安全销毁并重建，因此本单元采用第6.5节的fail-closed方案，而不是实现不可靠的同一开机周期owner切换。该方案验证的产品边界为：

```text
two simultaneous BLE links
+ one HID owner
+ one RDX owner
+ immutable singleton librdxApp.a
+ at most one RDX owner/session per boot
```

已完成实现：

- 增加`OFF / STARTING / READY / ACTIVE / QUIESCING / RESETTING / FAILED`显式状态模型；
- `s_rdx_runtime_consumed_this_boot`不被server/transport局部reinit清零；
- 首次合法RDX CCC、普通命令或OTA写入在进入静态库前完成`READY -> ACTIVE` claim；
- owner断开先进入`QUIESCING`并推进runtime epoch，使旧owner、pending send和timer token失效，再执行现有业务清理并进入`FAILED`；
- `FAILED`状态拒绝同一开机周期的后续RDX claim/value write，HID生命周期和report数据面保持独立；
- sync-data与stream-ready timer持有并校验RDX token；
- 断连路径不调用已审计为不完整的`rdx_protocol_task_free/create()`或`rdx_uxfile_task_free/init()`；
- 重启后boot-lifetime静态状态重新初始化，RDX恢复`READY`。

本阶段保持以下ABI与二进制不变：

```text
librdxApp.a
rdx_app_custom_command_parse(char *, char *)
RdxProtocolCallbacks
rdx_ble_server_info_t
静态库已导入的既有server API
```

自动化验证结果：

```text
RDX immutable-runtime fallback contracts           9/9 PASS
Host Software Tests                              19/19 PASS
librdxApp.a SHA-256                                  PASS
Firmware Build, Link and Image Packaging             PASS
git diff --check                                      PASS
```

`librdxApp.a`的SHA-256仍为`C540D70540DC4D61E15D1CA13579CD2342D4EA972FF0A74A1AFCCC04B1EF4ACA`。本轮可刷机验证配置将`TCFG_RDX_LOCAL_PLAYBACK_ENABLE`和`TCFG_DEC_OGG_OPUS_ENABLE`设为`0`，以消除CODE0超限；`TCFG_STENC_OPUS_ENABLE`保持`1`，RDX在线录音/Opus编码能力仍保留。固件已经完成编译、链接和`ISDdownload`镜像打包。

阶段判定：Go。源码、自动化准入和第11.7节第1至11项双连接实机业务验收均已通过，确认一个RDX owner与一个HID owner可以同时工作；反向连接顺序保留为后续追溯项。本阶段不验证、也不承诺同一开机周期内更换RDX owner。若产品必须支持App断开后不重启即可再次使用RDX，则需要静态库提供新的、可审计的生命周期契约，属于后续方案变更。

### Phase 3：在线 Keymap热配置

- 接通 App在线修改、VM事务和HID executor；
- 覆盖 HID按住/释放期间提交keymap；
- 验证App响应始终回到RDX link；
- 验证PC连接无断开、无重新枚举。

### Phase 4：回归、清理与正式启用

- 删除单连接断言和全局 handle兼容路径；
- 更新三号方案状态为历史基线，四号方案成为正式架构；
- 更新 `AGENTS.md`、测试契约和产品说明；
- 完成 Windows、正式 App、多次上电和长稳测试；
- 双连接成为唯一正式运行模型。

## 11. 测试与验收矩阵

### 11.1 Host contract

至少冻结以下内容：

- `config_le_hci_connection_num == 2`；
- `config_le_gatt_server_num == 1`；
- 两个 wrapper都注册 `rdx_profile_data[]`；
- 两个 wrapper使用相同MAC、名称和广告构包函数；
- 只有一个 registry拥有 `links[2]`、owner index和slot generation；
- RDX CCC不再调用全局 `att_set_ccc_config()`；
- MTU不再调用全局 `ble_op_att_set_send_mtu()`；
- HID/RDX发送均选择明确 wrapper；
- 第二次 Connection Complete不会 reset第一条 link；
- Connection Complete只被对应wrapper接纳一次；
- 同一事件到达另一wrapper时无副作用地过滤；
- 广告停止条件为两个slot均占用；
- 延迟ADV callback重新校验slot generation与transport epoch；
- 任意单边断开只清理对应 capability；
- HID断开不调用 `rdx_hogp_keymap_config_on_disconnect()`；
- RDX断开不清理 HID ready runtime；
- HOGP static read不attach或请求配对；
- RDX static read不attach或进入预编译协议库；
- capability claim通过唯一API完成，同一link不能同时claim RDX/HID；
- 未加密HID CCC enable在claim前返回 `0x0f`，且不写CCC、不持久化；
- claim内部结果与ATT错误码映射分离，`0x80/0x81`不是预先冻结的ABI；
- CCC写0只改变ready/tx状态，不释放owner index；
- Disconnection Complete路径不调用HID ATT send；
- 携带link语义的异步BLE工作在入口校验async token；
- keymap handle、Report Map和8-byte report契约保持不变。

Host contract只能冻结源码结构和禁止项，不能证明wrapper实际绑定、callback分发、controller双连接调度、SM peer隔离或广播恢复时序；这些项目必须由Phase 0A/0B和后续实机矩阵验证，不能以PowerShell静态检查替代。

### 11.2 真实设备连接顺序

```text
[ ] PC先连HOGP，App后连RDX
[ ] App先连RDX，PC后连HOGP
[ ] PC和App接近同时发起连接
[ ] Windows自动回连与App后台回连竞争
[ ] 两条链路建立后不再广播
[ ] 任一条断开后恢复广播
[ ] 延迟ADV到期前目标slot状态变化时不会错误开启广播
[ ] slot复用或deinit后旧timer被slot generation/epoch拒绝
```

### 11.3 热配置

```text
[ ] HID ready时GET keymap
[ ] HID ready时SET keymap
[ ] 保存成功后下一次按键立即使用新map
[ ] 保存期间Windows不掉线
[ ] 保存期间HID CCC不变化
[ ] revision冲突正确返回
[ ] 重复request幂等
[ ] VM写入失败回滚旧map
[ ] App断开取消pending事务
[ ] HID断开不取消App事务
```

### 11.4 断连隔离

```text
[ ] App断开，PC键盘继续输入
[ ] PC断开，App继续读取和修改配置
[ ] App断开不清除HID bonded CCC
[ ] PC断开不停止RDX notify
[ ] stale disconnect不清理新复用slot
[ ] stale encryption/MTU事件被忽略
[ ] HID link的CAN_SEND_NOW不唤醒RDX semaphore
[ ] RDX link的MTU变化不修改HID link MTU
[ ] 本地主动断连前可release，远端掉线后不再尝试ATT发送
```

### 11.5 安全与peer

```text
[ ] Windows首次配对与加密
[ ] 首次未加密CCC enable返回0x0f且不占用HID owner
[ ] 加密完成后的CCC重试才claim HID并持久化subscription
[ ] Windows跨上电自动回连并恢复CCC
[ ] App服务发现不弹出HID配对
[ ] App peer不继承Windows CCC
[ ] 第二个PC不能抢占现有HID sink
[ ] 第二个App不能覆盖现有RDX session
[ ] 同一link已claim RDX后不能再claim HID
[ ] 同一link已claim HID后不能再claim RDX
[ ] HID/RDX CCC写0后ownership保持到断连
[ ] 标准0x0e拒绝路径无无限重试、错误配对或缓存污染
[ ] 若测试0x80/0x81，其Windows与正式App行为已记录并决定最终映射
```

### 11.6 稳定性

```text
[ ] 连续按键同时反复读写keymap
[ ] 两条BLE链路保持8小时以上
[ ] 高频App状态notify期间HID无明显丢键
[ ] 旧录音/OTA/文件/app_core异步消息不会向新RDX owner发送
[ ] 两条链路交替断开/回连压力测试
[ ] 第二wrapper分配/注册失败时第一wrapper正确回收
[ ] poweroff/deinit期间所有延迟ADV timer失效
[ ] 低电量和休眠/唤醒恢复
[ ] 不同Windows版本和蓝牙适配器组合
```

### 11.7 RDX单次会话屏障与一拖二实机验收

本节是Phase 2B-4当前方案的实机准入矩阵。2026-07-23使用本轮已打包镜像完成验证，主日志为`COM3_2026-07-23_17-52-16.log`：

```text
[x] 1. 重启设备，确认本次上电尚未消费RDX会话
[x] 2. Windows先连接并完成HID加密/CCC，确认HID ready=1
[x] 3. App或nRF连接另一wrapper，启用RDX CCC并发送一条真实RDX命令
[x] 4. 日志出现“[BLE_PHASE2B] RDX runtime ACTIVE ... one-session-per-boot”
[x] 5. 两条链路同时在线时，RDX命令/响应可用，HID press/release持续可用
[x] 6. 全程无wrapper mismatch、cross-send、stale token accepted、assert或异常复位
[x] 7. 主动断开RDX owner，日志出现“RDX runtime FAILED ... RDX requires reboot”
[x] 8. RDX断开后Windows不掉线、不重新枚举，HID按键仍可正常发送
[x] 9. 同一开机周期重新连接App：ACL可以连接，但RDX CCC/value write必须稳定拒绝
[x] 10. App重连并被拒绝RDX期间，HID仍保持可用
[x] 11. 再次重启设备，重复RDX claim，确认runtime恢复READY并可再次进入ACTIVE
[ ] 12. 反向顺序抽测：App先claim RDX、Windows后claim HID，两条业务仍定向到各自wrapper
```

日志确认Windows/HID先连接后进入`ready=1`，第二条App链路使`connected_count=2`，随后RDX runtime进入`ACTIVE`。双连接在线期间HID press/release和RDX录音启停、响应及文件保存同时成功。RDX owner以`reason=0x13`断开后runtime按设计进入`FAILED`并恢复空闲wrapper广播；App可重新建立ACL，但RDX claim以`RDX_BLE_CLAIM_NOT_READY`稳定拒绝。补充实测确认此时HID继续上报，整机重启后RDX重新进入`READY -> ACTIVE`并恢复录音业务。

通过1至11即可判定“静态库不修改条件下，一个RDX owner与一个HID owner双连接共存”可行；第12项关闭已有的反向连接顺序追溯项。App断开后同一开机周期无法再次使用RDX是当前明确限制，不应当作为失败判据。若首次会话中的真实RDX命令/响应与HID不能同时工作，或任一链路发生错发、互相清理、异常断开，则核心一拖二方案判定为No-Go并进入备选方案评估。

同一开机周期RDX owner重建仅作为未来可选增强。只有静态库提供逐task/worker退出确认、所有queue/timer/mutex/buffer完整清理、可重复create和失败回滚后，才建立另一套重建验收矩阵；当前固件不得尝试该路径。

## 12. 主要风险与对策

| 风险 | 影响 | 对策 |
|---|---|---|
| 只改连接数、不改单例状态 | 第二连接覆盖第一连接，任意断开导致全局清理 | 双 wrapper + per-link session必须原子实施 |
| server/session各维护一份links | 两个状态中心不一致，释放与发送目标漂移 | link registry唯一拥有links、owner和slot generation |
| 一个 wrapper承载两个handle | `app_ble_get_hdl_con_handle()`、发送、广告和断连语义不匹配 | 遵循Find My/RCSP双wrapper实践 |
| App枚举HID触发attach | 手机误占HID、弹配对提示 | HID只在Input CCC或bonded恢复时attach |
| 未加密CCC写失败前已claim HID | 放弃配对的Central占住HID sink直到断连 | 未加密路径只请求配对并返回0x0f，加密重试后才claim |
| 静态RDX read触发attach | 通用枚举误占RDX session或唤醒预编译库 | RDX只在CCC enable或合法私有write时claim |
| 同一link同时claim RDX/HID | 手机同时占用两个capability，PC无法成为HID sink | MVP同链路capability互斥且ownership粘到断连 |
| RDX CCC仍为全局 | 两条链路相互覆盖订阅 | 全面迁移到`multi_att_*ccc_config` |
| RDX响应发错Central | PC收到私有notify或App收不到响应 | 发送始终选择`rdx_link->ble_hdl` |
| HID report发错Central | App收到键盘report、PC无输入 | 发送始终选择`hid_link->ble_hdl` |
| 一条断开清理另一条 | HID/App相互掉功能 | link cleanup、RDX cleanup、HID cleanup三层拆分 |
| ATT buffer不足 | RDX吞吐下降、HID send busy | 测量峰值后统一调整multi ATT buffer |
| 两条链路连接参数互相污染 | HID延迟或App不稳定 | 参数索引、MTU和更新状态per-link |
| 双连接同时回连竞态 | wrapper映射错误、stale event误清理 | hdl + con_handle双重校验和slot generation |
| Connection Complete回调内同步开启空闲wrapper广播 | 同一HCI事件继续fan-out时第二wrapper绑定第一条connection handle | 仅active advertiser接纳连接，callback返回后再延迟恢复广播 |
| 复合Profile公开HOGP但POC拒绝全部动态写 | Windows Input CCC写失败、HID驱动错误并主动断开 | Phase 0A保留无claim、无持久化、无report发送的最小HOGP控制面 |
| 旧ADV timer跨slot复用或poweroff执行 | 满连接后错误广播或deinit后访问wrapper | slot generation + transport epoch + callback重检策略 |
| HID CAN_SEND_NOW唤醒RDX | bulk/send-confirm状态错乱 | 只有当前RDX wrapper事件可操作RDX信号量 |
| 旧app_core/录音/OTA/文件任务访问新owner | 旧业务结果跨会话发送或污染新RDX session | 所有携带link语义的异步工作捕获并校验async token |
| 用固定延时冒充静态库quiescence | 延时到期后旧task仍可能处理新owner状态 | 当前方案禁止运行时重建，断开后直接FAILED直到重启 |
| 静态库timer、queue或worker清理不完整 | 旧会话污染同一开机周期的新owner | 每次上电最多一个RDX会话，不给旧工作绑定新owner的机会 |
| BLE局部reinit重新开放RDX | 未重启静态库却开始第二个RDX会话 | boot-lifetime consumed标志不得由server/transport reinit清零 |
| RDX断开后接纳新RDX owner | 新旧会话共享无token静态库队列 | 只有首次READY允许claim；QUIESCING/FAILED拒绝且不占owner index |
| 自定义ATT错误码触发Windows异常行为 | CCC无限重试、错误配对或缓存异常 | Phase 0B先用标准0x0e并实测，0x80/0x81不预先冻结 |
| Disconnect Complete后发送release | 已断ACL上调用ATT send，状态或队列异常 | release只在主动断连前；Complete仅做local cleanup |
| 预编译RDX库不支持双App | 两个App命令状态混乱 | 正式范围限制为单RDX session |

## 13. 最终验收标准

> Phase 2B-4 状态补充：安全降级已实现 / Automated Gate Passed / On-device Gate Passed。源码级录音/OTA/Keymap token边界已经完成，`librdxApp.a`保持不可修改，内部receive/file/bulk队列不补token；对应隔离责任由boot-lifetime consumed标志和runtime状态门承担。首次RDX会话的production输入已放开，断开后进入`FAILED`直到整机重启。19/19 Host tests、固件编译/链接/镜像打包和静态库hash均已通过；第11.7节第1至11项真机验证通过，确认当前产品边界下的一拖二核心方案可行。

本节是完整产品方案的最终验收门槛，不应与阶段验收混淆。截至2026-07-23已关闭Phase 0A-T、Phase 0B、Phase 1、Phase 2A、Phase 2B-1、Phase 2B-2、Phase 2B-3a、Phase 2B-3b和Phase 2B-3c（源码及受控双连接回归）；Phase 2B-3d已完成源码与Host contract收敛；Phase 2B-4已完成单次RDX会话安全降级、自动化准入和第11.7节第1至11项核心真机验收。反向连接顺序、真实OTA/完整文件业务、Phase 3在线Keymap以及Phase 4长稳回归仍保持开放。后续若出现首次配对、断连release、owner抢占、legacy流控活性或异步跨会话问题，应首先回查Phase 2A/Phase 2B的已知追溯项和对应COM3日志，不得把本阶段Go解释为所有最终风险均已实机验证通过。

本方案完成必须同时满足：

- PC与正式手机App可同时保持BLE连接；
- 无需断开PC即可修改HID自定义按键；
- 保存配置后下一次按键立即使用新map；
- Windows不重新枚举、不自动重连；
- 两个连接使用相同复合Profile和统一广播身份；
- 第一条连接后仍能被另一端扫描并连接；
- 两条连接满载后停止广播，释放一个slot后恢复广播；
- 双 wrapper能力已由Phase 0A实机验证，per-connection ATT隔离已由Phase 0B验证；
- 未加密HID CCC写不会claim，最终ATT拒绝码映射已由Windows与正式App实测冻结；
- RDX notify只发送到App link；
- HID report只发送到PC link；
- 同一物理link不能同时占用RDX与HID，CCC关闭不迁移ownership；
- 所有同步事件按wrapper + connection handle路由，所有携带link语义的异步操作受slot generation/epoch保护；
- 所有携带link语义的app_core、录音、OTA和文件异步工作均受async token保护；
- 不携带link token的静态库异步工作受单次RDX会话屏障保护；RDX owner断开后同一开机周期不接纳新RDX owner；
- RDX从`ACTIVE`断开时进入`QUIESCING -> FAILED`，HID继续工作，整机重启后RDX恢复`READY`；
- App断开不影响HID，HID断开不影响App；
- bonded HID CCC继续按peer隔离和跨上电恢复；
- App全服务发现不触发HID配对；
- 现有HID Profile contract、keymap事务和RDX基础业务回归通过；
- `librdxApp.a` SHA-256保持`C540D70540DC4D61E15D1CA13579CD2342D4EA972FF0A74A1AFCCC04B1EF4ACA`，既有函数、回调结构和server ABI均未修改；
- `librdxApp.a` transport输入、输出、发送唤醒和断连边界审计通过，已确认现有ABI不具备安全重复重建条件；
- 第11.7节单次会话与一拖二实机矩阵全部通过，不以局部reinit或固定延时替代整机重启；
- host software tests与Windows固件构建通过。
