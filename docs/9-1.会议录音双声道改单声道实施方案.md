# 会议录音双声道改单声道实施方案

本文统一维护会议录音单声道实施方案、接口核查与联调结论，更新日期：2026-09-17。目标为“双 MIC 采集、择优生成一路 PCM、单声道 Opus 编码”。当前已验证按住录音 stream-only 的固定 MIC0、动态择优单声道编码及 App 声道显示；工程实现已扩展至全部 CHAT 在线、保存和离线业务，CALL 保留原链路。完整保存/回取/回放及音质仍待本轮实机验收。当前实现与剩余工作见第 9 节。

现有链路详见 [9-0.会议录音双声道流程](9-0.会议录音双声道流程.md)。

## 1. 目标与范围

客户要求会议录音输出单声道。此次方案保留 MIC0、MIC3 两路采集，在编码前接入 MIC 择优算法，最终只编码一路声音。

```text
现状：MIC0 / MIC3 → 分别 DNS、AGC → 两路缓存与组帧 → 双声道 Opus → RDX / BLE、存储
目标：MIC0 / MIC3 → 分别 DNS、AGC → 两路缓存与组帧 → MIC 择优 → 单声道 Opus → RDX / BLE、存储
```

择优算法通常输出当前选中的一路；切换时通过交叉淡化混合旧、新通道，最终仍然输出一路 PCM。它不等于始终平均混音，也不负责说话人识别、分离或多人同时发言的完整保留。

本次范围是会议录音场景 `RECORD_SCENE_CHAT`。录音启停、会话绑定、RDX owner、BLE 连接和存储生命周期继续沿用现有机制。其他使用同一编码节点的场景需要排查并保持原有行为，不能无条件将所有场景改为单声道。

## 2. 参考资料及已确认事实

以下算法资料已完成本机核查并用于 2B 集成；固定选路里程碑已提交，动态择优改动尚未提交。

| 资料 | 已确认内容 | 使用方式 |
| --- | --- | --- |
| `sl_mic_select.h` | 支持 2 或 4 路 int16 PCM 输入，输出一路；16 kHz 下按每路 320 点处理；静态单例 | 当前会议场景采用两路配置 |
| `一些参数1.txt` | 能量、削波、切换确认、驻留和淡化参数 | 作为初始调试参数，不直接视为产品最优值 |
| `一些参数2.txt` | 初始化配置示例，其中 MIC 数量按四路填写 | 改为两路，并核对所有配置字段 |
| 参考 `effect_dev2_node.c` | 气导/骨导处理后，以 `nch=1` 编码 | 仅参考单声道编码接法，不能直接覆盖工程文件 |
| `libsl_V2_0722.a` | 已用工具确认导出 `sl_mic_select_*` 和 `sl_audio_*` 符号 | 独立链接、完整固件构建及短时实机运行通过，资源余量待验证 |

参考 C 文件调用的是 `sl_audio_process_bone_air()`，并不是 `sl_mic_select_process()`。当前 MIC0/MIC3 的采集关系不能直接解释成“气导麦 + 骨导麦”，本方案采用头文件描述的 MIC 择优接口。

## 3. 哪些部分复用，哪些部分修改

| 环节 | 计划 |
| --- | --- |
| MIC0/MIC3、ADC、两路 DNS/AGC | 首轮保留，实测 AGC 是否削弱择优依据 |
| EffectDev3 / EffectDev1 写两路缓存 | 复用 |
| Source_Dev1 等齐两路、每路取 320 点 | 复用，保持双路输入 |
| EffectDev2 编码前处理 | 新增择优算法，输出单路 320 点 PCM |
| Opus 编码库 | 复用，会议场景改为 `nch=1`，调整输入及码率 |
| 节点声道协商 | 明确区分双路采集输入与单声道编码输出 |
| RDX/BLE、录音存储 | 尽量复用，核对声道、帧长、封装假设 |
| App 解码、本地回放 | 核对单声道及码率兼容性，必要时同步修改 |

不能简单把 `global_ch_mode` 改成单声道。当前它参与 Source_Dev1 取数和格式传递，提前改成单路可能使择优算法拿不到第二路输入。也不能仅将 Opus 的 `nch` 改成 1，而不处理算法输入、节点协商及输出说明。

### 3.1 数据量与协议边界

| 项目 | 当前双声道 | 目标单声道 |
| --- | --- | --- |
| 采集和择优输入 | 每路 16 kHz、16 bit、20 ms | 保持两路，各 320 点 |
| 编码入口 PCM | 1280 字节：L 640 + R 640 | 择优输出 640 字节 |
| Opus 声道数 | 2 | 1 |
| 按现有公式计算的目标码率 | 32 kbit/s | 16 kbit/s |
| 按目标码率估算的每帧输出 | 约 80 字节 | 约 40 字节 |

编码后长度必须使用 `encLen`，不能将估算值写成固定包长。单声道也可以选择其他码率；首轮以现有公式得到的 16 kbit/s 为候选，音质验收后确定。

“仍是 Opus”不等于“所有包格式不变”。PCM 输入布局、编码声道和码率会变化；外层 RDX 包头、分片及帧边界能否保持不变，需核对库协议和接收端。当前可见 C 源码不足以证明这一点。

## 4. 阶段一：建立基线并核对接口

**目标：确认接入位置和端到端格式约定，形成可对照的双声道基线。**

工作内容：

1. 保存当前会议录音的构建结果、关键日志和样音，记录固件版本及音频资源版本。
2. 沿 `translation_ear_recoder.c → Source_Dev1 → EffectDev2 → Sink_Dev1` 核对场景、格式协商和实际帧长，列出其他复用场景。
3. 验证算法库适配 AC701N/BR28 的工具链、ABI 和依赖；核对头文件与库版本匹配。
4. 确认算法处理返回值的成功/失败语义、两路模式未用输入指针的传法、初始化与销毁约束。头文件未明确的部分以供应方说明或可验证结果补齐，不猜测。
5. 核对 RDX 音频封装、App 解码和本地回放是否写死双声道、80 字节帧长或固定码率，明确是否需要格式标识或版本区分。
6. 明确客户验收的是单声道编码输出，以及择优对多人同时发言的取舍是否符合会议场景需求。
7. 提前采集同一时段的两路实际 PCM，在能够运行该库的目标环境中验证择优，覆盖靠近不同 MIC、轮流发言、噪声和同时发言。对比 DNS/AGC 后的 RMS 与选路结果；若能量差被拉平或噪声持续占优，先确定参数或接入位置调整方向，再进行完整接入。
8. 核查双路采集的时间对应关系、FIFO 丢数方式和单路停产检测能力，明确场景策略传入节点的接口及异常上报路径。

当前编码配置为 `format_mode=0`，JL 接口注释称其为“百度无头”，不能直接假定为接收端可独立解析的标准 Opus 包。接口核对表须记录封装模式、帧边界来源、声道信息来源、实际帧长范围、RDX 分片重组规则和新旧录音识别方式；每项结论注明源码、协议资料或抓包/解码依据。

交付物：基线样音与日志、接口核对表、接收端兼容性结论、待确认问题清单。

通过条件：算法调用约定和场景边界明确；协议及接收端已确定兼容方式；实际双路 PCM 的择优试验已形成接入位置结论。可以先独立验证算法链接，但未确认的协议假设不得作为产品完成依据。

## 5. 阶段二：接入双 MIC 择优与单声道编码

**目标：在会议录音中建立完整的两路输入、一路编码链。**

主要文件：

- `SDK/Makefile`：加入经验证的算法库与头文件路径。
- `SDK/audio/framework/nodes/effect_dev2_node.c`：算法生命周期、两路输入检查、单路输出与单声道编码。
- `SDK/audio/interface/recoder/translation_ear_recoder.c`、`source_dev1_file.c`、`sink_dev1_node.c`：按实际协商需要调整，保证采集输入与编码输出的含义清晰。
- 项目配置头：按职责集中管理功能开关和调试参数，不修改工具生成配置来保存产品策略。

### 5.1 分两步建立链路

**阶段 2A：固定一路，验证单声道链路。** 保留双路采集与 1280 字节组帧，调试配置分别固定取 MIC0、MIC3 的 320 点进行单声道编码，暂不运行动态择优。验证实际单声道编码、App 解码及短录音保存/回放，提前执行阶段三的最小兼容性检查。固定选路仅用于调试，不作为最终交付功能。

**阶段 2B：接入动态择优。** 在 2A 通过后，将固定选路替换为算法输出，沿用已验证的编码和封装参数，再执行完整的阶段三和阶段四。保留固定选路调试能力，便于区分编码/协议问题与算法/音质问题。

### 5.2 实现要求

1. 首轮保留现有双路采集、DNS/AGC 和 FIFO 组帧，输入仍为前 320 点 MIC0、后 320 点 MIC3 的 planar 数据。
2. `sl_mic_select_create()` 配置 `sample_rate=16000`、`num_mics=2`、`analysis_samples=320`。
3. 每次处理严格检查两路输入共 1280 字节、16 bit 和采样率；输出缓冲区至少容纳 320 个 int16 样本。
4. 调用择优算法生成一路 PCM，再用 `nch=1` 的 Opus 编码；第二个 PCM 指针的传法按编码接口验证结果确定，参考示例使用 `NULL`。
5. 避免单声道输出协商反向导致 Source_Dev1 只提供一路数据。编码参数与节点对外格式说明保持一致，并验证框架的帧长计算。
6. 正确管理算法静态单例，录音停止后释放；暂停/恢复、新会话和异常退出均验证状态是否重置，不能重复 create 后继续使用失败状态。
7. 检查内存分配、算法初始化、处理和编码返回值。首版失败策略为停止该次录音并走现有清理路径，不发送未初始化 PCM 或错误格式数据。
8. 使用 `need_buf()` 核对编码工作区容量；保留当前工程的输入长度保护，不能用参考文件覆盖后退回无边界拷贝。
9. 将改动限定到会议场景，其他复用场景继续使用各自既有的输入和编码规则。

### 5.3 场景策略与资源归属

当前 EffectDev2 的 `NODE_IOC_SET_SCENE` 分支为空。阶段一需确认框架场景参数是否与 RDX 场景含义一致、是否在协商和启动前可靠到达；若不满足，通过明确的节点配置接口传递本次录音策略，记录具体接口和调用顺序。

由录音创建入口固定本次场景、采集格式、编码声道、码率和调试选路方式，保存到实例上下文并在协商前生效。运行中不依赖可变全局场景切换编码格式；会议启动缺少有效策略应报错，其他场景保留原有规则。

算法单例须记录持有者和本实例创建成功标志。重复申请失败不得影响已有持有者，只有成功持有的实例才能 reset/destroy，清理须可重复调用。当前 `run_buf`、`tst_buf_LR` 也是全局变量，应核实多实例可能性，采用实例独立存储或明确强制独占，不能只保护新增算法单例。

### 5.4 异常上报与停止流程

当前初始化函数返回 `void`，编码失败主要打印日志，需补齐失败传播机制。初始化失败应阻止节点进入工作状态；处理或编码失败后停止提交该实例音频，将错误原因和会话 token 上报现有录音管理任务。

管理任务核验 token 仍属于当前会话后，触发既有停止和收尾路径；旧会话迟到错误不得停止新会话。同一故障只触发一次停止，任务投递失败也须有明确处置和记录，不能打印后继续发送。避免在音频回调内直接销毁正在执行的链路，释放前必须确保回调已停止访问算法及缓冲区。

验证分配失败、create/process/编码失败、停止与错误同时发生、新会话收到旧错误等情况，记录错误码、停止次数、资源释放和下次启动结果。

### 5.5 双路同步和缺帧处理

两路 FIFO 都达到 320 点只保证数据量，不能独立证明时间对应。核对共同采集时钟、分支处理延迟、启动清空顺序及溢出策略，通过采集序号、累计样本计数或受控同步信号验证对齐关系。

区分“一路有数据但静音/削波”和“一路停止产出”：前者可交给算法，后者可能使组帧一直等待，算法无法自行接管。首版不隐式降级为单路采集；单路停产超过规定超时，或发现无法确认对齐的丢帧时，走异常停止流程。若后续要求不中断恢复，需另行定义成对丢弃、重新对齐和算法状态重置规则。

阶段一依据正常调度抖动和缓存容量确定等待超时的具体毫秒值，并验证正常负载不会误触发。主动制造单路 FIFO 溢出、停产和恢复，验证不会无限等待或持续处理时间错位的数据。

初始参数取参考值：能量优势 3 dB、连续确认 8 帧、最小驻留 1500 ms、交叉淡化 600 ms、冷启动 16 帧。20 ms 一帧下，8 帧为 160 ms，16 帧为 320 ms；实际切换还受驻留和其他条件约束，不能把 160 ms 当成固定响应时间。

交付物：可构建的会议单声道固件、配置说明、算法与编码运行日志。

通过条件：2A 固定选路端到端验证通过；2B 每次处理时间对应的两路 320 点并输出一路 320 点；Opus 实际以单声道初始化并正常编码；场景隔离、缺帧检测和异常清理通过，反复启停无单例残留、内存泄漏或异常输出。

## 6. 阶段三：验证发送、存储和接收端兼容

**目标：App 和本地回放能正确接收、解码单声道录音。**

工作内容：

1. 检查 `rdx_record_run_data_handle()` 到 RDX 库的长度传递，确认全链使用实际编码长度。
2. 根据协议资料或抓包确认音频帧边界、分片、格式标识；如外层格式无需变化，记录依据。
3. 验证 App 单声道解码、持续播放和保存，核对总时长、采样率及声道配置。仅凭左右耳都能听到声音不能证明编码是单声道。
4. 验证普通录音的在线发送与本地保存，以及 stream-only 的发送路径。
5. 验证本地存储聚合缓冲区跨界与停止尾部刷新。跨界丢弃剩余输入的问题已修复并通过主机行为测试（见第 9 节），物理介质落盘仍待实机验收；不能依赖约 40 字节恰好整除缓存。
6. 验证旧双声道录音与新单声道录音的回放兼容方式，避免接收端依赖不明确的格式假设。

交付物：App 解码记录、协议/抓包核对结果、在线与本地样音、完整性检查结果。

通过条件：接收端正确处理单声道，音频无因声道、码率、帧边界或存储跨界造成的截断、错速和破音；外层协议是否改变有明确结论。

## 7. 阶段四：实机调音与资源评估

**目标：验证择优对会议拾音有实际价值，并满足设备资源约束。**

| 场景 | 重点观察 |
| --- | --- |
| 同一说话人分别靠近 MIC0、MIC3 | 选中通道是否合理，弱侧切换是否及时 |
| 说话人移动、轮流发言 | 切换是否自然，是否频繁来回跳转、漏掉句首 |
| 两人同时发言 | 是否过度压低另一方，是否满足客户期望 |
| 静音、背景噪声、风噪、敲击 | 是否把噪声能量当成优质人声 |
| 一路遮挡、过弱或削波 | 通道状态及保护性切换是否符合预期 |
| 两路经过独立 AGC | 能量差是否被拉平，择优依据是否失效 |
| 长时间录音及重复启停 | CPU、RAM、功耗、缓存丢数和算法状态 |

以低频统计方式记录选中通道、各路 RMS、通道状态、处理耗时和缓存 drop，避免逐样本日志干扰实时性。调参时对比同一条件下的旧双声道样音、单路原始分支和新择优输出。

阶段一完成接入位置初步验证，本阶段扩大场景并调优。如现有 AGC 影响择优，单独评估 AGC 参数或算法接入位置；不要未经对比就删除现有降噪链。

### 7.1 验收记录与量化门限

以下是首轮工程验证要求，不代表客户已确认的产品指标。阶段一明确各项负责人；阶段四正式测试前填齐待定门限，不能用“听起来正常”替代未定版指标。

| 项目 | 首轮要求与记录方式 |
| --- | --- |
| 长时间录音 | 普通录音和 stream-only 各至少连续 2 小时；记录输入、算法、编码和接收帧数，正常条件下无新增处理导致的丢帧，帧数差异均可解释 |
| 生命周期 | 启停至少 100 次、暂停恢复至少 100 次；清理后资源回到可重复基线，无持续内存增长或单例残留 |
| 实时性 | BLE 发送及存储负载下记录算法加编码耗时的平均值、P99、最大值和 FIFO 水位；按 20 ms 输入周期核算预算，阶段一定版具体余量，不仅凭平均值判定 |
| 同步与异常 | 覆盖单路溢出、停产、恢复和各类失败注入；在已定超时内检测并收尾，无无限等待、重复销毁或旧错误终止新会话 |
| 切换质量 | 固定声源、距离和噪声条件，记录触发条件、实际切换时间、句首损失及伪音；分别评估冷启动、驻留、淡化影响，时延和漏字容忍门限定版后判定 |
| 数据完整性 | 按实际编码帧及长度核对接收/保存数据，覆盖跨界和停止时尾部刷新；无无法解释的字节丢失，解码时长按帧数及已确认的编解码延迟核算 |
| 资源与功耗 | 同条件比较双声道基线，记录 CPU、RAM、功耗峰值及稳态值；产品负责人在正式验收前确定允许增量和最低余量 |

每项记录固件与资源版本、算法参数、测试条件、预期门限、实测值及结论。客户音质认可和工程完整性检查分别留存证据。

交付物：对比样音、参数定版表、资源与长时间运行记录。

通过条件：客户认可会议拾音效果；无明显切换爆音或持续抖动；资源在产品允许范围内，新增处理不导致持续丢帧。

## 8. 阶段五：回归、文档与交付

完成固件构建和仓库统一主机检查：

```powershell
# 在 SDK 目录执行
.\.vscode\winmk.bat all

# 在仓库根目录执行
powershell -NoProfile -ExecutionPolicy Bypass -File .\tests\host\run_host_tests.ps1
```

主机检查用于验证现有产品、传输和生命周期契约，不能代替算法音质、实时性及 App 端验收。新增有针对性的验证应覆盖两路输入边界、算法失败清理、编码参数和场景隔离。

设备回归覆盖：按键/App 启停、暂停恢复、BLE 断连重连、RDX 逻辑释放、普通录音与 stream-only、存储收尾，以及其他共用节点的录音场景。若修改 `.x6flow`，通过 JL 工具重新生成音频资源并核对烧录版本，不直接编辑派生文件。

交付时更新 9-0 对当前实现的描述，保留本方案的阶段验收记录；记录固件版本、算法库版本/哈希、最终参数、App 兼容版本和已知限制。保留可回退的双声道基线，回退时同步核对接收端格式配置。

完成标准：构建与主机检查通过，设备和接收端验收通过，双 MIC 输入、单声道编码输出有明确证据，其他场景无回归。仅完成 `nch=1` 修改或听到声音不能算作完成。

## 9. 当前实现、验证结果与后续工作

本节合并原阶段一核查及 2A 联调记录，仅保留有效结论。用户确认 App 支持单声道后，先推进在线固定选路验证；这不代表前述全部阶段门槛已通过。

### 9.1 接口核查与基线

| 项目 | 已确认结论 |
| --- | --- |
| 算法库 | `libsl_V2_0722.a` 的 MIC 择优成员为 pi32v2/r3；独立目标链接通过，依赖 pow、sqrt。配置 ABI 为 11 个 32 bit 字段，大小 44 字节、末字段偏移 40 |
| 调用约定 | create 成功返回 0，非法配置/已有单例返回 -1；process 正常返回 320，非零长度不匹配返回 -2，未创建或必需指针缺失返回 -1。两路模式 ch3/ch4 及 selected_channels 可 NULL |
| 算法验证边界 | 已完成 IR 核查、目标链接和 19.76 秒实机动态录音，观察到双向切换；音质、严格同步、长时间运行和资源余量待验收 |
| 场景与流程 | STREAM_SCENE_AI_VOICE 不等于 RDX CHAT。历史命名 `PIPELINE_UUID_TRANSLATION_MONO`（0x9463）的图实际包含双 MIC 与 EffectDev2；本次保留该图和 stream.bin |
| 双路同步 | 每路 FIFO 5120 字节，约 160 ms；2A 核查时各自溢出丢弃、无停产检测。2B 已增加故障锁存和超时，见 9.6；尚无成对重同步或实测对齐证明 |
| 本地回放 | 已按 DAT 的 frame_size 选择当前支持的 RAW 配置并计算读数、时长、seek/resume；40 对应单声道，80/旧字段 0 对应历史双声道。保存入口及索引重建仍需配套，见 9.7；不按文件总大小猜格式 |

本机使用 `probe_mic_select.ps1` 和 `probe_mic_select.c` 完成算法编译、链接核查；探针随 2B 算法资料维护。目标断言尚未执行，后续不能与录音中的算法单例并行运行。

算法库 SHA256：`86F30008277A9E196AEF94A7082E802B360EAEC02ED978E8905C61B3798D8DAA`。本机双声道构建基线与清单位于 `cache/meeting-mono-baseline-20260917/`，该目录被 Git 忽略，交付前需另行归档；双路同步 PCM 和基线样音仍待补齐。

### 9.2 已实现的 2A 范围与配置

仅 **CHAT 按住录音 stream-only 会话**启用固定一路单声道。普通 App 录音、本地保存、离线录音及 CALL 保持双声道，避免产生旧回放无法识别的单声道文件。此处为已验收的 2A 历史范围；当前 2B 工程验证版见 9.6。

在 `SDK/apps/earphone/include/t2620_project_config.h` 配置 `TCFG_T2620_MEETING_MONO_DEBUG_MIC`：

| 值 | 行为 |
| --- | --- |
| 0 | 回退原双声道 |
| 1 | 固定 MIC0（2A 验证时默认） |
| 2 | 固定 MIC3 |
| 3（当前默认） | 动态择优，短时运行通过，音质及长测待验收 |

修改后重新构建、烧录，无运行中切换命令。数据路径如下：

```text
MIC0/MIC3 → 各自 DNS/AGC → 双路 FIFO
→ Source_Dev1：每路 320 点，合计 1280 字节 planar PCM
→ EffectDev2：固定取一路 320 点（640 字节）
→ Opus：16 kHz / 1 ch / 16 kbit/s / 20 ms / format_mode=0
→ Sink_Dev1：按实际 encLen 提交 → 原 RDX/BLE → App
```

- recorder 在协商/start 前通过 `NODE_IOC_SET_PRIV_FMT` 传入实例策略；保持采集 `global_ch_mode=AUDIO_CH_LR`，EffectDev2 明确两路输入、一路输出，拒绝不匹配流程及 eSCO 组合。
- 编码工作区按 `need_buf()` 分配，工作区及 PCM 缓冲由实例持有；单声道编码第二 PCM 指针为 NULL，本轮真实编码已运行成功。压缩帧独立提交，长度使用 `encLen`，不写死 40 字节。
- 分配/open 失败阻止启动；坏帧、输出分配或编码失败后停止提交。音频回调记录故障，由 100 ms 定时检查投递 app_core 收尾，队列满重试；epoch 和绑定 token 防止旧会话错误停止新会话。
- `rdx_record.c` 两条本地写入分支共用 append/flush，循环处理跨 8000 字节缓存的余量和结束尾部；写失败停止处理余量，防止重复提交。该修复不改变 stream-only 和原有过滤规则。

### 9.3 App 声道显示修复

根因是 `rdx_record_auto_run()` 在 CHAT 启动/恢复时将 record_mode 固定设为双声道。现根据实际格式，在投递录音任务前设置模式：`format=1` 对应 `mode=0`（单声道），双声道及 CALL 为 `mode=1`；停止/暂停保留本次模式。

单声道状态报文为：

```text
[RDX_RECORD_REPORT] format=1 mode=0 channels=1
开始：*DEV#record#1#0#1#0#1#0#
停止：*DEV#record#0#0#1#0#1#0#
```

sample 是采样率枚举，16 kHz 单/双声道均为 1；其后的 record_mode 才是此次修正的声道模式。外层 RDX 包头和发送入口沿用现有实现。此次显示问题通过 C 源码修复，新增的 devrec 静态库补丁已撤回；原有 uxfile 存储补丁保留。

### 9.4 软件与实机验证

2A 源码修复版固件构建、打包返回 0，统一五组主机契约通过。日志：`cache/meeting-mono-source-build.log`、`cache/meeting-mono-source-host.log`。构建中仍有链接警告，详见日志。

- `test_meeting_mono.py` 提取真实 C 函数编译后在主机执行，覆盖配置 0/1/2、逐样本选路、2→1 协商、变长压缩帧、分配/编码失败、实例隔离和过期会话；同时验证 CHAT/CALL 启停与暂停恢复，以及任务投递前上报模式已生效。框架与编码器为 mock。
- `test_record_storage.py` 验证实际聚合函数，覆盖不同帧长、多块跨界、尾部刷新和写失败，逐字节核对结果；不代替物理介质验证。

实机证据：`COM3_2026-09-17_18-08-43.log`，用户确认 App 已显示单声道。

| 检查项 | 实测结果 |
| --- | --- |
| 上报参数 | `format=1 mode=0 channels=1`，开始和停止报文均保持单声道模式 |
| 实际编码 | `sr=16000 br=16000 nch=1 frame_ms=20 need=24080 mono_mic=1` |
| 结束统计 | 555 帧、22200 字节、`encLen=40..40 failed=0`，按 20 ms/帧计算约 11.1 秒 |
| 采集收尾 | `frames=555 drop=0/0 remain=0/0` |
| App | 用户确认声道显示正确；音质、解码速度和时长尚未试听验收 |

本次证明固定 MIC0 单声道编码与 App 显示一致；40 字节只是本次配置的实测帧长，不代表所有 Opus 配置。尚不能据此宣称完整 2A 或产品验收通过。

### 9.5 后续实施与验收顺序

1. 阶段三接入范围为全部 CHAT 会议业务：按住 stream-only、普通在线发送并保存、离线保存后 App 回取，以及启停、暂停恢复、本地回放。CALL 保持原有双声道。
2. 已复用 DAT 的 `opus` 和 `frame_size` 完成保存及回放适配，源码补充本地格式记录和启动恢复；音频仍为原 RAW，APP 协议和 RDX 静态库不变。现在按 9.7 开始完整实机验收。
3. 验证 App 列表/回取、播放速度、时长、seek/resume，及真实介质跨界写入、停止尾部、重启后格式保持。测试录音至少 10 秒，避免既有少于 4000 字节丢弃策略影响结果。
4. 并行补齐 MIC 物理映射、同步 PCM、DNS/AGC 后能量差与切换听音，执行长录音、反复启停/暂停恢复和异常注入；短时择优运行成功不代替这些验收。

### 9.6 2B 动态择优工程验证版（2026-09-17）

本节记录 2B 历史验证范围：当时仅 CHAT stream-only 接入算法，普通保存与离线录音仍为双声道。当前全 CHAT 实现见 9.7；动态音质仍待验收。

- 库落位 `SDK/apps/common/third_party_profile/rdx_protocol/libsl_V2_0722.a`，头文件 `SDK/apps/common/third_party_profile/rdx_protocol/sl_mic_select.h`；与参考资料逐字节相同，库 SHA256 仍为 `86F30008277A9E196AEF94A7082E802B360EAEC02ED978E8905C61B3798D8DAA`。未修改 RDX 静态库。
- 配置 `TCFG_T2620_MEETING_MONO_DEBUG_MIC=3`：两路 DNS/AGC 后 planar PCM → `sl_mic_select_process()` → 一路 320 点 PCM → 原单声道 Opus。0/1/2 保留回退和固定选路。
- 使用库默认阈值，覆盖采样率 16000、麦克风数 2、分析帧 320；默认 3 dB 优势、8 帧确认、1500 ms 驻留、600 ms 淡化、16 帧冷启动。尚未调音。
- 成功 create 的实例持有单例；重复实例失败不影响原持有者。停止、挂起和初始化失败按归属清理，恢复重新创建。process 返回值必须为 320，否则不提交编码数据，走既有 epoch/binding 故障收尾。
- 动态模式发现任一路 FIFO 丢数即锁存故障 `-20`；源已启动且 500 ms 无完整配对帧锁存 `-21`，由原 100 ms 定时检查请求收尾。500 ms 是首轮工程阈值，尚未实测定版；队列忙时沿用重试。故障锁存后即使恢复来数也不继续动态编码。
- 该检测防止已知丢数后继续择优，并不证明两路采样时间严格对齐。共同采集时钟、分支延迟、启动首帧对应关系仍须同步信号/PCM 实测；不做猜测式重对齐。DNS/AGC 是否拉平能量差也待听音和日志对照。

软件验证包括完整固件链接、五组主机契约，以及真实集成函数配合 mock 算法/编码器的配置 0/1/2/3、两路输入与单路输出、重复实例、create/process 失败、释放重入、FIFO 故障锁存、超时和计时回绕。主机测试不运行厂商 DSP 算法，不代表实机音质或 CPU 余量通过。日志：`cache/meeting-dynamic-build.log`、`cache/meeting-dynamic-host.log`。

**实机运行里程碑：** `COM3_2026-09-17_19-26-02.log` 已验证动态链路。

- 初始化：`sr=16000 br=16000 nch=1 frame_ms=20 mono_mic=3`，算法 ready 为 16000 Hz / 2 MIC / 320 点。
- 录音中 active 多次 1↔2；结束 `frames=988 bytes=39520 encLen=40..40 failed=0`，约 19.76 秒；源 `drop=0/0 remain=0/0`。
- 开始/停止保持 `format=1 mode=0 channels=1`，按松键正常结束，无 mono fault；后续 BLE 断连发生在录音结束之后。
- `max_ms=10` 仅表示这次毫秒计时下算法调用最大值；约每秒的 RMS/active 日志不能判断每次切换是否合理。
- 算法库与头文件已迁至 RDX 目录，哈希不变；迁移后构建与五组主机检查通过，日志 `cache/meeting-library-relocate-recheck.log`、`cache/meeting-library-relocate-host.log`。

**后续设备验证：**

1. 使用 VS Code `all` 构建并烧录本轮产物，连接 App，按住录音。确认 `[mic select] ready`、`mono_mic=3`、`nch=1`，App 仍显示单声道。
2. 分别靠近两只麦克风连续说话各 5–10 秒，轮流至少三次。日志每 50 帧输出 `[mic select] frame=... active=... rms=.../... state=.../... max_ms=...`；active 1 对应输入 MIC0，2 对应输入 MIC3，实际物理位置仍需对照。淡化期间 active 表示目标通道，输出可能混合两路。
3. 记录安静、背景噪声、遮挡一路、两人同时讲话的表现；听取是否漏字、断音、爆音及频繁切换，核对播放速度和时长。无法择优时先看两路 RMS 和状态（0=弱、1=正常、2=削波），不要仅凭 active 不变判故障。
4. 松开录音，检查 `[meeting mono] mic=3 ... failed=0` 和源 `drop=0/0`；反复启停至少 10 次，确认每次可重新 ready，再测断连重连。
5. 必要时将配置切为 1、2、0，分别重编译烧录取得同条件对照。提交完整串口日志和听音结果后，再决定参数及接入位置。

`max_ms` 仅为毫秒级算法调用最大耗时，不含编码/调度，0 表示计时分辨率内；它不代替完整 CPU、RAM、P99 和长时间录音验收。固定选路两只麦克风听音、动态同步性及音质验收仍由实机补齐。

### 9.7 全 CHAT 接入工程版（待实机验收）

用户确认范围为全部 CHAT，CALL 保持原有 PCM 与双声道编码链路。`TCFG_T2620_MEETING_MONO_DEBUG_MIC=3` 现对普通 APP 录音、按住 stream-only、离线录音统一生效；0/1/2 仍可回退及固定选路。独立启动入口也使用相同格式策略，新会话推进 generation 并清空旧标记；暂停续录保留文件身份与格式。

| 环节 | 当前实现 |
| --- | --- |
| 编码与状态上报 | 所有 CHAT 两路 PCM → 动态择优 → 16 kHz、20 ms、16 kbit/s 单声道；开始工作任务前按 `formate` 设置 APP 声道模式 |
| 正常保存 | 新建时更新公开 `frame_size`；保存帧必须与锁定配置一致（单声道 40 / 双声道 80 字节）。原 8000 字节聚合与尾部 flush 不变，stream-only 不落盘 |
| 格式持久化 | 新增源码 `rdx_record_format.c/h`，第一帧落 RAW 前写入同名 `.mta`，包含 SN、名称、场景、开始时间、格式及首帧指纹，并 flush、读回核验。普通保存双声道也记录其格式，便于回退与文件共存 |
| DAT / APP 回取 | 库正常保存已有 `opus`、`frame_size`，不改 JSON schema。单声道 `opus=1084752896`，APP 列表/下载仍使用原协议 |
| 启动恢复 | 在 RDX 文件任务、缓存及定时器启动前，验证 `.mta` 与 RAW 的首帧及帧对齐，纠正或补建 DAT 条目；只在每次启动执行一次，后续重复任务初始化不再触碰活动缓存 |
| DAT 写入中断 | 先完成 `rfmt.tmp`、写入校验提交记录 `rfmt.cmt`，再替换 DAT；重启重放已提交临时索引，校验成功并 flush 后删除提交记录。未通过校验则关闭业务准入，不把未知数据当双声道 |
| 本地回放 | DAT 的 40/80 字节配置决定单/双声道、数据泵、时长、seek/resume；旧字段 0 沿用既有双声道。切换候选成功后才替换活动配置 |
| 故障收尾 | 算法、PCM 配对和保存格式错误通过 epoch/binding 隔离，统一进入带重试的 STOP 屏障；普通在线、离线也能可靠请求收尾，不再借用仅按住录音的释放状态 |
| 闪念 | 本产品 `PROTOCOL_EVENT_CMD_FLASHNOTE` 仅接收并打印，没有录音实现，源码没有调用库的固定双声道闪念通知；不属于本轮录音入口 |

恢复保留现有 DAT 的旧文件条目及 marks。DAT 缺失时，`.mta` 可恢复对应文件的编码参数、大小及 CRC；恢复的结束时间按音频帧数估算，丢失的 marks/暂停墙钟时长不能重建。已删除 RAW 不会因残留 `.mta` 复活；首帧指纹不匹配、未对齐尾部、身份冲突、损坏索引或提交记录均拒绝自动猜测。`.mta` 与 DAT 同时丢失时，无头 RAW 没有可靠格式恢复依据，不能宣称可恢复。旧固件没有这些格式记录，不应将混合单双声道文件的存储介质交回旧固件扫描。

恢复沿用库的最多 300 条索引容量，逐对象处理 JSON，避免构造完整 cJSON 树。临时索引和格式文件仅固件使用，不改变 RAW、BLE 包或 APP 文件协议。首次保存元数据的介质耗时、启动扫描耗时及真实掉电耐久性需设备验证。

软件验证：VS Code 同入口构建、打包及统一五组主机检查通过；新测试执行真实格式恢复 C 和 cJSON，使用 mock 文件系统，覆盖单/双声道元数据、帧长拒绝、写入/flush/close/读回失败、索引缺失、旧条目与 marks 保留、首帧不匹配、未对齐尾部、孤立元数据、重复初始化隔离、DAT 替换中断重放。既有算法/编码器、聚合写入、回放配置与故障隔离检查继续通过。日志：`cache/meeting-all-chat-build.log`、`cache/meeting-all-chat-host.log`。主机通过不等于真实介质或 APP 解码验收。

RDX 静态库未改，SHA256 保持 `2D844D806F0F26B03BE03C8ACE7495A456CB8A4D19E381E4423C50FDA7518C64`；本轮未提交。

**使用 VS Code `all` 构建并按现有方式烧录后，依次验收：**

1. 普通 APP 在线 CHAT 录音 15 秒，暂停 3 秒、继续 15 秒再停止。确认 `mono_mic=3 nch=1`、`format=1 mode=0 channels=1`，保存出现 `[REC_FORMAT] pinned ... format=1 frame=40`，续录保持同一 SN。
2. APP 文件列表与下载播放：显示单声道，语音、速度和有效录音时长正确，无尾部截断；不要用总文件大小推断帧边界。
3. 断开 APP，通过本地按键离线录音至少 15 秒后停止；重连 APP 回取，再重启设备回取同一文件。启动应出现 `[REC_FORMAT] boot reconciled files=...`，文件保持单声道。
4. 本地播放新单声道与旧双声道文件并往返切换，检查 `RAW profile ... channels=1 frame_bytes=40` / `channels=2 frame_bytes=80`，分别验证暂停续播、快进快退、自然结束。
5. 回归按住 stream-only：仍单声道、不新增本地文件。回归 CALL：仍走原双声道链路。连续启停、暂停恢复各至少 10 次，并测试断连及逻辑释放收尾。
6. 在有备份的专用测试介质上，再做 DAT 缺失恢复和录音/索引替换时掉电测试；保留 `.mta`，验证恢复后的格式、CRC 和实际解码。最后完成第 7 节长录音、同步性与音质验收。

### 9.8 首次保存即停止的设备回归修复（实机确认修复）

`除了在线录音其他都不行.log` 中，离线与 APP 保存录音均启动 `nch=1 mono_mic=3`，首次元数据保存报存储格式错误、故障 `-22`，随即主动 STOP；两次编码均为 16 帧、每帧 40 字节、编码失败 0。按住 stream-only 不进入保存分支，可正常录音。

新格式模块把 `f_flush_wbuf()` 的文件系统路径误传为设备名 `sd0`。按 JL SDK 调用约定统一改为 `storage/sd0/C/`，覆盖元数据、索引提交与恢复；主机模拟增加路径校验，避免掩盖此参数错误。新增写入、截断、关闭、刷新返回值及故障代码行日志，便于排查设备上可能存在的其他存储问题。RDX 静态库不改。

统一五组主机检查通过（`cache/meeting-format-flush-host.log`）。重新烧录后，优先验证离线及 APP 保存录音各至少 15 秒，确认出现 `pinned ... format=1 frame=40`、正常停止保存，再测试重启后的下载与回放。本次设备日志尚不能证明这些业务验收通过。
### 9.9 单声道本地播放解码适配（单声道完整回放实机通过）

`COM3_2026-09-18_10-08-15.log` 确认 SN=3、SN=4 均成功 pinned 单声道格式并保存 `opus=1084752896`，前一轮首次保存即停止问题消失。SN=3 本地播放正确识别 `channels=1 frame_bytes=40`，却立即报 `dec_err: 40`、随后 `dec_end`，因此本地回听尚未通过。

原因是 SourceDev0 单声道分支仍选择普通 `AUDIO_CODING_OPUS`，与录音使用的 `get_opus_stenc_ops()` 不匹配；匹配的 `AUDIO_CODING_STENC_OPUS` 底层又全局固定 80 字节 CBR。只读检查 JL `media.a(opus_dec.c.o)` 与 `lib_ogg_opus_dec.a(opus_demo.o)` 确认，匹配的解码器固定输出 48 kHz LR，并支持带 8 字节包头的 raw 输入：前 4 字节为大端包长，后 4 字节在当前库实现中未校验。

修复：本地播放启用解码器 raw 包头模式，SourceDev0 按文件的 40/80 字节帧长取帧，仅在内存送解码时附加包头；单/双声道文件统一走 STENC 解码及原双路播放流。文件、BLE、录音编码参数不变；已消费位置仍只计算原始音频字节，包头不计入暂停/seek 位置。普通 Opus 回退仅保留在未启用本地播放的产品配置中。此实现取代历史文档中“解码全局固定 80 字节”的要求。

VS Code 同入口构建打包及统一五组主机检查通过：`cache/meeting-playback-fix-build.log`、`cache/meeting-playback-fix-host.log`。新增测试执行实际 SourceDev0 取包和格式函数，检查单双声道包头、有效载荷和输出配置；未在主机执行 JL 真正解码器，音质及速度仍待烧录。验收优先回听已有 SN=3、SN=4，再测旧双声道、暂停续播、快进快退及自然结束。播放流日志的 48 kHz/双路是解码后 PCM 输出，不表示文件变回双声道。RDX/JL 静态库均未修改。

### 9.10 本地回放实机结果与排查经验（2026-09-18）

证据：`COM3_2026-09-18_10-28-13.log`。本轮确认“动态择优 → 单声道编码 → 保存 → 本地完整回放”链路跑通，不代表全部 CHAT 场景及异常恢复验收完成。9.8、9.9 的待烧录描述是当时的验证记录，当前结果以本节为准；CALL 继续保持原链路。

| 检查项 | 实机证据与结论 |
| --- | --- |
| 择优与编码 | 三次录音分别为 576、354、461 帧，均 `encLen=40..40 failed=0`；源统计均 `drop=0/0 remain=0/0`，观察到 active 1/2 切换 |
| 保存格式 | 出现 `pinned ... format=1 frame=40` 和 `opus=1084752896`，未再出现首次保存即 STOP |
| 文件识别 | 10:29:47 打开 SN=1 的 `b2fa24.raw`，18000 字节，`channels=1 frame_bytes=40`（日志第 2872–2873 行） |
| 播放时长 | 设备时间 92.260 秒开始，101.361 秒 EOF，约 9.1 秒；18000 / 40 × 20 ms = 9 秒，与文件有效音频时长吻合 |
| EOF 与释放 | `drain complete: cbuf empty`、`eof ... read=18000, written=18000`；随后 DAC 停止、解码器与 SourceDev0 释放（第 2979–3063 行），本次录音回放区间无 `dec_err` |
| 播放协商 | source 为 48 kHz/2 ch，decoder 与 DAC 为 48 kHz/1 ch；播放路径的协商格式不改变文件的单声道编码属性 |

排查经验：

- 按编码、元数据落盘、文件格式识别、解码、EOF/释放逐段核对。`nch=1` 和 APP 显示正确只能证明对应环节；文件能打开也不代表解码器匹配。
- 编解码匹配要同时核对实现、分包方式及 PCM 输出协商。此次通过内存中的包头向 STENC 解码器传递 40/80 字节帧长，RAW 文件及 BLE 协议不变；暂停/seek 的消费位置不能包含临时包头。
- 错误必须结合时间区间、播放源及回调归属判断。日志较早的 `dec_err: 40` 出现在提示音流程，不是 10:29:47 开始的录音回放失败；提示音问题仍需单独核查，不能据此宣称全日志无异常。
- 以文件帧数计算有效音频时长，对照播放耗时、读写计数和资源释放。时长吻合支持播放节奏正常，音质、音调和尾音完整性仍须实际听音确认。
- 主机 mock 要校验 SDK 参数约定。此前 `f_flush_wbuf()` 误传设备名 `sd0`，实机要求文件系统路径 `storage/sd0/C/`；宽松模拟会掩盖此类问题。主机通过不能替代真实介质及 JL 解码器验证。此次修复均在源码完成，未修改 RDX/JL 静态库。

当时剩余问题（后续修复与实机结果见 9.11、9.12）：EOF 收尾期间出现 `app_core timer_no_response`，约 170 ms 后完成资源释放并继续运行，未见卡死；仍需定位收尾阻塞，不能把告警算作已解决。

后续补测旧双声道文件及单双声道往返切换、暂停续播、快进快退、重复播放；继续完成 APP 下载回听、重启后格式保持、CALL 回归，以及长录音、同步性、音质和掉电恢复验证。启动日志 `boot reconciled files=2` 仅证明本次扫描执行成功，不代表索引丢失或掉电恢复已验收。

### 9.11 自然结束收尾阻塞修复（单次自然结束实机通过）

9.10 的 `timer_no_response` 发生于 EOF 定时回调同步调用 `dev_flow_player_close()`：`jlstream_stop(..., 50)` 等待淡出与 DAC 停止，期间占住 `app_core`，本次日志约 170 ms 后才释放。不能通过屏蔽定时器告警或直接取消淡出来判定解决。

自然结束改用临时 `pb_close` 任务执行音频停止；`app_core` 每 10 ms 查询完成信号，完成后才释放音频流、关闭文件并发布停止状态。等待期间保持 DRAINING 和播放器占用，不再继续泵数据；避免存储/电源准入提前看到空闲。任务创建失败保留占用并重试；如果同时收到显式停止、暂停或切换，原同步关闭入口等待该任务完成，再统一释放，避免重复 stop 或释放仍被使用的对象。显式操作的等待仍保留，本轮针对自然 EOF 的定时回调阻塞。

采用 SDK 实际可链接的 `os_task_create/os_sem_query/os_sem_pend/os_task_del`；仅有头文件声明的 `thread_fork/thread_kill/os_sem_accept` 在本工程不可链接，不能直接使用。主机行为测试执行实际关闭函数，覆盖创建失败重试、完成前保持占用、重复查询完成、显式关闭与自然收尾交错、只停止/释放一次及原同步关闭路径；不替代真实 OS 调度测试。

验证日志：`cache/meeting-playback-close-build.log`、`cache/meeting-playback-close-host.log`。烧录后连续自然播放至少 10 次，确认 EOF 附近不再出现由此次同步停止引起的 `app_core timer_no_response`、尾音完整、可再次播放；同时测试末尾按停止/切换及播放结束后立即录音。单次自然结束的实机结果见 9.12，连续与边界操作仍待补测；RDX/JL 静态库不改。

### 9.12 自然结束异步收尾实机结果与经验（2026-09-18）

证据：`COM3_2026-09-18_10-50-44.log`。本次单声道本地播放自然结束正常，EOF 清理期间未再出现 `app_core timer_no_response`，确认 9.11 的异步停止方案在本次设备运行中生效。此次只有一次完整本地回放，不能据此认定连续回放、任务无泄漏或全部 CHAT 验收完成。

| 检查项 | 实机证据与结论 |
| --- | --- |
| 文件与格式 | 第 875–887 行打开 SN=1、`b59dfa.raw`，17920 字节，`channels=1 frame_bytes=40`；设备时间 19.495 秒开始播放 |
| 播放与耗时 | 17920 / 40 × 20 ms = 8.96 秒有效音频；28.558 秒缓冲排空，距开始约 9.063 秒；本次录音回放区间无 `dec_err` |
| 异步停止 | 第 923–938 行：28.558 秒创建 `pb_close`，随后淡出、DAC 停止，28.731 秒音频流停止完成；这段等待未触发 `app_core` 超时提示 |
| EOF 与释放 | 第 939–943 行：28.740 秒 `eof ... read=17920, written=17920`，随后释放流、解码器及 SourceDev0，28.742 秒完成；从创建清理任务到 EOF 约 182 ms |
| 后续业务 | 播放结束后成功连接 APP，又完成两次录音，其中后一次保存 SN=2 并校验 DAT 成功；证明后续业务可继续，不等同于结束瞬间重入测试 |
| 录音链路 | 三次分别 459、292、925 帧，均 `encLen=40..40 failed=0`，源均 `drop=0/0 remain=0/0`；观察到择优 active 1/2 切换，上报保持 `channels=1` |

本轮经验：

- 异步收尾消除的是 `app_core` 在淡出和 DAC 停止期间的同步等待，不是取消音频停止耗时。约 182 ms 的收尾仍存在；验收应同时看到临时任务启动、停止完成、资源释放，以及该区间不再报主任务超时。
- 缓冲排空不等于硬件已停止。保持 DRAINING 和播放器占用直到停止完成，再释放并开放后续业务，避免存储、电源或新播放过早接入；本次顺序符合预期，末尾并发操作仍需单独测试。
- 全日志仍有开机阶段的 `timer_no_response`，以及提示音链路的 `dec_err:40`。必须按设备运行时间、音源及回调定位，不能将其归为本次录音回放故障，也不能宣称全日志无异常或这些问题已解决。
- `read=written=文件大小` 证明文件数据完整送入播放链路，配合耗时和释放日志支持完整回放；尾音、音调和听感仍需试听。串口中有交错打印，计算耗时优先使用同一链路的设备运行时间。

后续验收保持：连续自然回放至少 10 次并检查任务/资源回收；末尾按停止或切换；结束后立即重播及录音；旧双声道与新单声道往返播放、暂停续播、快进快退。APP 下载回听、重启格式保持、CALL 回归，以及长录音、PCM 同步性、择优音质和掉电恢复仍按 9.7 执行。本次没有修改 RDX/JL 静态库。
