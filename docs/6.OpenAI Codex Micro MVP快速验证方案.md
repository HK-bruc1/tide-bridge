# RDX 私有 GATT + 标准 HID 键盘 + Codex Micro 复合方案

> 文档状态：Phase 2 实机 Gate 已通过，Phase 3A 已完成，Phase 3B 静态实现与审核已完成
>
> 已知进展：2026-07-29，Windows Codex Desktop `26.721.4979.0` 已在 composite/C1 laboratory identity 下完成设备发现、ID 6 `device.status` 往返和控制面连接。最初的“可检测但无法连接”由 `v.oai.rgbcfg` 兼容性校验过严导致，修复后客户端显示已连接。名称单变量实验确认 `Beanstalk RKB [认证尾码]` 不影响连接，标准 HID 键盘、Codex 控制面和 Key 5 Fast `ACT06` 均已实机验证。统一到 `all` 时关闭 reference Manufacturer/PnP 后，Codex Desktop 无法识别；在同一 `all` 入口恢复 `Work Louder` 与 `303A:8360` 后，实机再次识别并连接成功。由此确认失败原因是身份配置被遗漏，而不是 `all` 入口或产品名称。Phase 2 以 commit `70586c5` 和 `db_update_data.bin` SHA-256 `0DAE373DAFB78873AE189627364D4598479E237813E5EE1C62A6C62D3AAE799F` 作为行为基线收口，现正式进入 Phase 3。当前仍未拆分验证 Manufacturer 与 PnP 的单项依赖，该工作保留到 Phase 4。
>
> 广播兼容修正：2026-07-31，Codex 集成后 nRF Connect 可发现设备、固件广播启动成功，但正式 RDX APP 无法发现。对照三号方案候选 C 与五号双连接方案后确认，commit `09cf4d0` 重新在 Primary ADV 的 `Flags + Name` 后追加了 Generic HID Appearance `0x03C0`，违反了此前正式 APP 与 Windows 已通过实机验证的固定布局。删除 `03 19 C0 03` 后正式 APP 立即恢复发现，Codex/HOGP 功能无可见变化。修正镜像 `db_update_data.bin` 长度为 `1046800`，SHA-256 为 `1C3C05724E6A75DD1764F79F33E3CBCFBD6F1C1EBE0B6665701803B735344174`。因此 `70586c5` 继续作为 Codex Report、RPC、身份和按键行为基线，但其广播 Appearance 已被本次实机结论明确覆盖。
>
> 边界：本方案基于受控实验构建和开源兼容实现得出的互操作设计，不是 OpenAI 公开发布的第三方设备协议规范。参考身份只允许存在于实验镜像中。

## 1. 目标与结论

目标产品不是以 Codex 替换整个 BLE 服务，而是在一个共享的 BLE ATT 数据库中并存：

```text
RDX 私有 GATT                 私有 APP 配置、数据与既有业务
        +
HID over GATT Service 0x1812
        +-- 标准键盘 Report ID 1    Windows 标准键盘 Usage
        +-- Codex Vendor Report ID 6  Codex JSON-RPC 传输
```

两种 HID Report 可以在同一条 BLE 连接上并存；不同的 Report ID、Report Characteristic、Report Reference 和 Input CCC 将它们隔离。二者报文格式不同不是不能共存的原因，反而正是 HID Report Map 要解决的问题。

产品的五个物理按键继续由 RDX 私有 GATT 上的 APP 配置。但配置模型从“只保存 HID 键值”升级为“保存带类型的动作”。一次物理按下只路由到一个动作类型，因此不会同时发送普通键盘输入和 Codex 动作。

## 2. 名词与边界

| 名词 | 含义 |
|---|---|
| RDX 私有 GATT | 既有 RDX UUID 服务、APP 命令、配置和业务数据；不是 HID。 |
| 标准键盘 | HID Usage Page `0x07`、Report ID `1`、8 字节键盘报告。Windows 键盘驱动直接解释。 |
| Codex Report | Vendor Defined Usage Page `0xFF00`、Report ID `6`、63 字节输入/输出报告。Codex 客户端解析其 framing 和 JSON-RPC。 |
| HID core | 共享的 HID Service、加密、owner、CCC、Protocol Mode、Control Point 和生命周期层。 |
| 动作映射 | 一个物理键到一个“键盘动作 / Codex 动作 / 禁用”的持久化配置。 |

Codex 的 `ACT06` 不是标准 HID usage，也不是可写入标准键盘 report 的键值；它是 Codex Fast Command Key 的逻辑动作名称。例如设备通过 ID 6 发送：

```json
{"method":"v.oai.hid","params":{"k":"ACT06","act":1}}
```

因此 APP 不能把 `ACT06` 当作标准 HID usage 下发，也不应直接下发任意 JSON 或方法名。

## 3. 目标运行拓扑

### 3.1 连接与 owner

保持当前固定的两 wrapper、最多两条 Peripheral ACL 的架构：

```text
Link A ── RDX 私有 GATT owner
       └─ 可同时成为 HID owner

Link B ── 可成为 HID owner（若 A 未占用）
```

只保留两个 capability：`RDX` 和 `HID`。Codex 是 HID 内的 report provider，**不得**新增 `CODEX owner`。同一 link 同时拥有 RDX 和 HID 时是 `RDX_HID` 复合会话；RDX 私有 APP 可以配置全局键映射，HID link 则承载键盘和 Codex 上报。

全局键映射允许由手机上的 RDX link 配置、由电脑上的 HID link 执行，但写入权限不得仅依赖“存在一个 RDX owner”。SET/RESET 必须同时满足：当前 RDX runtime active、request token 仍属于该 owner、产品绑定/认证成功、非 OTA/关机/录音等禁止状态。发布构建必须提供 fail-closed 的产品授权实现，不能继续依赖默认允许的 weak hook。

HID 发送的前提保持不变：正确 HID owner、连接有效、链路加密、相应 Input CCC 已启用、未 suspend、generation token 未过期。每个 report 都有独立 CCC：键盘 ID 1 的订阅不能视为 Codex ID 6 的订阅，反之亦然。

### 3.2 共享与独立内容

| 可复用 | 必须独立 |
|---|---|
| HID Service `0x1812`、BLE link、MTU、加密、配对、owner、generation token、ATT 可发送事件、断连清理 | Report ID、Input/Output Report characteristic、Input CCC、报告长度、payload 编码、键盘按下状态、Codex RX/TX 队列与 RPC 状态 |
| Protocol Mode、HID Information、Control Point、peer identity、订阅持久化框架 | Report Map 中各 collection 的定义和各 report 的 Report Reference |

ATT 是串行传输，两个 report 的 notify 会在同一链路上按顺序发出；这不影响应用层同时具备“标准键盘”和“Codex”两种能力。

## 4. HID Profile

### 4.1 Composite 为目标产品配置

最终目标配置使用 composite Report Map：

```text
Keyboard Collection (Usage Page 0x07, Report ID 0x01)
    +
Vendor Collection   (Usage Page 0xFF00, Report ID 0x06)
```

目前 ID 6 Vendor Collection 固定为 29 字节：

```c
0x06, 0x00, 0xFF,  /* Usage Page 0xFF00 */
0x09, 0x01,        /* Usage 1 */
0xA1, 0x01,        /* Collection Application */
0x85, 0x06,        /* Report ID 6 */
0x15, 0x00, 0x26, 0xFF, 0x00,
0x75, 0x08, 0x95, 0x3F,
0x09, 0x01, 0x81, 0x02, /* 63-byte Input */
0x95, 0x3F,
0x09, 0x02, 0x91, 0x02, /* 63-byte Output */
0xC0
```

Composite Report Map 总长为 99 字节：70 字节标准键盘 collection + 29 字节 Codex collection。

### 4.2 固定句柄布局

保持一个静态 ATT 数据库和一套稳定句柄。目标 composite 布局如下：

| 句柄 | 属性 | 归属 |
|---|---|---|
| `0x0016-0x0025` | HID 通用属性及 ID 1 Input/Output Report | HID core + keyboard provider |
| `0x0026-0x0029` | ID 6 Input Report、CCC、Report Reference | HID core + Codex transport |
| `0x002a-0x002c` | ID 6 Output Report、Report Reference | HID core + Codex transport |
| `0x002d-0x0031` | Device Information Service | profile aggregate |

修改 Report Map、Service 或 Attribute 布局后，Windows 必须删除旧设备并重新配对。运行时不得改变 ATT 表或 Report Map；如果产品需要切换键盘行为，只切换键的动作映射，不切换描述符。

### 4.3 三种构建 persona

| Persona | 当前映射 | 私有 GATT | ID 1 键盘 | ID 6 Codex | 用途 |
|---|---|---:|---:|---:|---|
| `keyboard` | C0 / mode 0 / 显式诊断宏 | 是 | 是 | 否 | 隔离标准键盘回归问题 |
| `codex-only` | V1 / mode 1 / 显式诊断宏 | 是 | 否 | 是 | 隔离 Codex/Windows/传输问题 |
| `composite` | C1 / mode 2 / `all` | 是 | 是 | 是 | 当前产品默认与主验证入口 |

`all` 是命令行和 VS Code 唯一固件构建入口，默认采用 `composite`。`keyboard` 和 `codex-only` 只允许通过显式编译宏临时诊断，不再提供独立 Make target 或 VS Code task。当前 `all` 同时是受控的 laboratory identity 验证镜像，不是正式量产身份镜像。

三种 persona 使用同一套产品名称机制：默认名称为 `Beanstalk RKB [认证尾码]`，并保留 `VM_RDX_BLE_NAME` 的产品配置能力。Codex mode 和 laboratory identity 不得覆盖广播名称或 GAP Device Name；名称与参考 Manufacturer、PnP VID/PID 等实验身份相互独立。

## 5. 模块化架构

JL SDK 只允许一份 ATT 数据库，所以模块化应体现在 Profile 片段、handle 分发和明确 API 边界，而不是让多个模块各自注册 `app_ble_profile_set()`。

```text
rdx_ble_server                 JL wrapper、广播和 SDK callback 桥接
        |
        +-- rdx_ble_session    两 link、owner、加密、MTU、token、epoch
        |
        +-- rdx_gatt_profile   静态 ATT 表与按 handle 的模块分发
              |
              +-- rdx_private_gatt
              +-- rdx_hid_service
                    +-- rdx_hid_keyboard
                    +-- rdx_codex_transport
                          +-- rdx_codex_rpc
                          +-- rdx_codex_app
        |
        +-- rdx_input_router   五个物理键到一个动作的独占路由
```

### 5.1 `rdx_ble_server`

只承担 SDK 适配职责：wrapper 创建/释放、连接事件、ATT read/write 入口、`ATT_EVENT_CAN_SEND_NOW`、广播和 profile 注册。它不得继续包含 Codex RPC、按键策略或大量 HID 细节。

### 5.2 `rdx_gatt_profile`

拥有唯一的 `rdx_profile_data[]` 和句柄常量；使用编译期 profile fragment 组合 RDX 私有服务、HID 片段和 DIS。ATT dispatcher 按 handle range 转发到私有 GATT 或 HID core，模块不直接依赖 server 的大 `switch`。

### 5.3 `rdx_hid_service`

拥有 HID 共有状态：Protocol Mode、HID Information、Control Point、HID owner、加密检查、suspend、各 report CCC、peer-scoped 订阅恢复及所有 ready-drop 清理。它向 report provider 暴露受限 API，例如：

```c
int rdx_hid_report_notify(report_id, value_handle, data, len);
u8  rdx_hid_report_is_ready(report_id);
void rdx_hid_report_release_all(void);
```

ID 1 和 ID 6 使用独立的 `ready` 判断；任一 CCC 的变化都只影响其对应 provider。

### 5.4 `rdx_hid_keyboard`

只处理标准 8 字节键盘报告和 LED Output Report。它不理解 Codex、APP keymap 协议或物理键策略。

### 5.5 `rdx_codex_transport`、`rdx_codex_rpc`、`rdx_codex_app`

| 模块 | 责任 |
|---|---|
| `rdx_codex_transport` | ID 6 的 63 字节 framing、RX 重组、TX FIFO、`APP_BLE_BUFF_FULL` 重试、link token 与 timeout。 |
| `rdx_codex_rpc` | 完整 JSON 边界、解析、allowlist、参数验证、JSON-RPC result/error 序列化。 |
| `rdx_codex_app` | `device.status`、`sys.version`、受支持灯效、Agent Key 等产品语义；不直接操作 ATT。 |

ID 6 ATT Value 固定为 63 字节，不含 Report ID：

```text
[0x02][N][N 字节 UTF-8 JSON][zero padding]
```

其中 `N` 为 `0..61`。TX 消息以 LF 结束后分片；ATT callback 只校验、复制和入队，cJSON 解析只在 `app_core` 执行。队列、深度、字符串、JSON nesting、超时和 generation 必须保持有界，未知方法必须 fail-closed。

RPC 参数校验必须区分“传输安全边界”和“主机可演进业务字段”。`v.oai.rgbcfg` 与 `v.oai.thstatus` 的兼容行为应和参考实现一致：前者只要求 `params` 为 JSON object，后者只要求为 JSON array；内部灯光字段均按可选字段处理，已知字段按能力消费，不支持或缺失的字段忽略。不得要求 `ambient`、`keys` 或每个灯光项同时提供 `c/b/e/s` 全部字段，否则 Codex Desktop 的合法初始化配置会收到 `-32602 Invalid params`，并被客户端视为控制面初始化失败。未知 method、错误顶层类型、超长 frame 和错误 owner 仍须 fail-closed。

63 字节 ATT Value 要求 effective ATT MTU 至少为 `66`。Windows HID capabilities 中 `InputReportByteLength` 和 `OutputReportByteLength` 必须均为 `64`，其中第一个字节是 Report ID `0x06`，后续 63 字节才是 ATT Value。若任一条件不满足，先保存 MTU/HID caps 证据并停止 Codex RPC 判断，禁止用截断或软件补齐伪造成功结果。

### 5.6 `rdx_input_router`

物理键事件只能调用统一入口：

```c
int rdx_input_router_click(u8 physical_key_id);
```

它读取当前动作映射并用 `switch(kind)` 路由。业务代码不得直接从 `rdx_app.c` 调用 `rdx_hogp_keyboard_report_send()` 或 `rdx_codex_micro_fast_key_click()`。

固件内部键号固定为零基：用户可见键 1～5 分别对应 `physical_key_id=0..4`。因此硬编码 Codex 键 5 必须落在 `key_id=4`；当前 `num_idx==0` 的临时 Codex 旁路必须删除，不能仅把判断改成另一个数字后继续保留旁路。

切换映射、HID owner 释放、CCC disabled、suspend、断连和 generation 更新时，router 必须先取消所有 pending timer，并向对应 provider 发送 release；不得出现 stuck keyboard key 或 stuck Codex key-up。

## 6. 五键动作映射与私有 APP 协议

### 6.1 为什么要升级协议

当前 `hogpkm` V1 每键存储：

```text
[modifier][usage1][usage2][usage3][usage4][usage5][usage6]
```

五键共 35 字节，只能表示标准键盘 usage。它应继续作为 legacy ABI，而不能把 Codex 名称硬编码进 usage 字节数组。

V2 使用固定 8 字节 union entry：

```text
[kind][data0][data1][data2][data3][data4][data5][data6]
```

| `kind` | 名称 | `data` 解释 |
|---:|---|---|
| `0x00` | `NONE` | 所有 data 必须为 0。 |
| `0x01` | `KEYBOARD` | `data0=modifier`，`data1..6=标准 HID keyboard usages`。 |
| `0x02` | `CODEX_FAST` | 所有 data 必须为 0。固件转换为 Fast Command Key `ACT06` 的 down/up，不携带 Agent 专用的 `ag` 字段。 |

V2 五键 payload 是 40 字节。继续使用私有命令 `hogpkm`，wire header 的 version 为 `0x02`，现有 opcode、request ID、base revision、payload length、little-endian 编码和 CRC32 语义保持不变。新旧协议均必须先验证完整 frame，再进入 `app_core` 的 token-bound 事务。

V2 的长度上限必须按下表冻结，不能继续沿用 V1 的 49/98 和 50/100：

| 内容 | 计算 | 二进制长度 | hex 长度 |
|---|---:|---:|---:|
| V2 SET request | `10 header + 40 map + 4 CRC` | 54 | 108 |
| V2 GET response | `10 header + 1 status + 40 map + 4 CRC` | 55 | 110 |
| 完整 GET uplink | `*APP#hogpkm#` wrapper + 110 hex + 尾部分隔符 | 130 | 不适用 |

因此实现至少需要 `MAX_REQUEST_FRAME_LEN=54`、`MAX_REQUEST_HEX_LEN=108`、`MAX_RESPONSE_FRAME_LEN=55`、`MAX_RESPONSE_HEX_LEN=110`，完整 uplink 静态缓冲至少为 131 字节（含 NUL）。这些 workspace 继续放在模块 BSS，不能压入 `app_core` 小栈；host contract 必须断言计算关系而不是只断言孤立常量。

`GET_CAPS` 使用不超过 8 字节的紧凑 payload，至少编码：status、wire-version mask、physical key count、entry length、action-kind mask、Codex command mask 和 test-mode flag。当前 command mask 只允许 Fast。

V1/V2 共用一个 canonical V2 runtime map 和同一个 revision 域，兼容行为固定为：

1. V1 SET 中全零 legacy entry 转换为 `NONE`；其余 entry 转换为 `KEYBOARD`，然后原子替换 canonical map。
2. V1 GET 仅在当前 map 全部为 `NONE/KEYBOARD` 时允许降级编码。
3. 当前 map 含任一 Codex action 时，V1 GET 返回新的 `MAP_NOT_REPRESENTABLE` status，不能伪造为零 usage、截断或静默丢弃。
4. 硬编码宏开启时，所有 SET/RESET 返回 `TEST_MODE_ACTIVE` status，不能写 VM 或覆盖运行时映射。
5. V2 收到未知 kind、未宣告 Codex command、非零 reserved data 或错误长度时返回 `INVALID_KEYMAP`。

新 status 从当前 `0x00..0x0C` 之后分配：`MAP_NOT_REPRESENTABLE=0x0D`、`TEST_MODE_ACTIVE=0x0E`。APP 必须显式处理，不能归并成通用失败。

### 6.2 硬编码与 APP/VM 双路径

在实现和联合调试 APP V2 协议之前，使用已有宏选择动作映射来源。当前 `all` 默认开启硬编码 composite 动作映射；后续联合调试时只需把同一宏改为 `0`，即可恢复 APP 下发与 VM 持久化路径：

```c
#ifndef RDX_HOGP_KEY_ACTION_TEST_ENABLE
#define RDX_HOGP_KEY_ACTION_TEST_ENABLE 1
#endif
```

该宏定义在 `rdx_app_config.h`，只选择 canonical map 来源，不建立按键旁路，也不自动启用 laboratory identity。值为 `1` 时使用硬编码五键映射并拒绝 APP SET/RESET；值为 `0` 时加载 APP/VM 映射并恢复持久化事务。当前项目 overlay 另外显式打开 identity guard，因此 `all` 得到 composite + 硬编码五键映射 + 产品名称 + reference Manufacturer/PnP。

当前硬编码映射为：

| 物理键 | 硬编码动作 | 预期上报 |
|---:|---|---|
| 1 | `KEYBOARD / Ctrl+C` | 仅 ID 1 键盘 down/up |
| 2 | `KEYBOARD / Ctrl+V` | 仅 ID 1 键盘 down/up |
| 3 | `KEYBOARD / Backspace` | 仅 ID 1 键盘 down/up |
| 4 | `KEYBOARD / Enter` | 仅 ID 1 键盘 down/up |
| 5 | `CODEX_FAST / Fast` | 仅 ID 6 的 `ACT06` down/up |

键 5 的用户语义固定为“切换 Codex Fast 模式”。参考工程将 Fast 定义为第一个 Command Key `ACT06`；相比依赖任务槽分配的 Agent Key，Fast 的结果更直观，也不执行 Approve、Decline 或 Send 等上下文相关命令。按下发送 `act=1`，延时释放发送 `act=0`。

硬编码宏只替换“动作映射的来源”，不能建立旁路：

```text
宏为 1：built-in test map ──> canonical action map ──> input router ──> provider
宏为 0：APP / VM map ───────> canonical action map ──> input router ──> provider
```

两条路径必须调用同一个 `rdx_input_router_click()`、同一套 ID 1/ID 6 ready 判断、同一套 release cleanup 和同一套 ATT transport。禁止在物理键 callback 中使用配置宏直接调用 Codex 或 keyboard send API。

宏为 `1` 时，built-in map 是唯一动作来源。现有 APP/VM V1 键映射不得在运行中覆盖它；SET/RESET 明确返回 `TEST_MODE_ACTIVE`。启动日志和诊断信息显示 `action_map_source=TEST`。

硬编码阶段的通过条件是：

1. Composite 设备仍被 Windows 和 Codex 客户端识别。
2. 物理键 1～4 只产生预期标准键盘事件，且无任何 ID 6 消息。
3. 物理键 5 连续触发 Fast `ACT06` down/up，且无任何 ID 1 副作用。
4. ID 1 或 ID 6 任一 report 未 ready 时，仅对应类型失败，不回退、不串到另一 provider。
5. 断连、CCC disable、suspend 和重连过程中无 stuck keyboard key 或缺失的 Codex key-up。

只有该阶段通过后，才开始 APP V2 的 wire protocol、VM migration 和 UI 联合调试。这样能把问题明确分为“Composite HID/路由问题”和“APP 配置协议问题”。

### 6.3 APP 合约

APP 先请求 `GET_CAPS`，再决定 UI 和 wire version。V2 capability payload 固定为 8 字节：

```text
byte0 status
byte1 wire_version_mask   bit0=V1, bit1=V2
byte2 physical_key_count  固定 5
byte3 entry_len           V2 固定 8
byte4 action_kind_mask    bit0=NONE, bit1=KEYBOARD, bit2=CODEX_FAST
byte5 codex_command_mask  bit0=FAST
byte6 flags               bit0=TEST_MODE_ACTIVE
byte7 reserved            必须为 0
```

APP 的每键 UI 是“动作类型选择器”，而不是只能选择键盘键值：

```text
物理键 N
  - 标准键盘快捷键（修饰键 + 1..6 个标准 usage）
  - Codex Fast 模式
  - 禁用
```

APP 只下发稳定的 typed value `CODEX_FAST`，永远不下发任意 Codex JSON、任意 RPC method、任意 `ACTxx` 字符串或未经固件 capability/allowlist 放行的参数。固件是 Codex 协议适配器，负责生成实际的 `ACT06` down/up JSON 消息。

### 6.4 兼容与存储迁移

V2 使用新的 VM IDs 和新的 record magic，不覆盖现有 51 字节 `HKM1/HKC1` 记录。V2 data record 固定为 56 字节：12 字节 header + 40 字节 canonical map + 4 字节 record CRC；commit record 继续为 20 字节，但使用新的 magic/schema。新 IDs 暂记为 `VM_RDX_INPUT_MAP_SLOT_A/B` 和 `VM_RDX_INPUT_MAP_COMMIT_A/B`，实施前必须在 `syscfg_id.h` 冻结真实编号并加入冲突检查。

启动选择顺序固定为：

1. 优先读取并验证 V2 A/B data + commit，选择 revision 更新的有效槽。
2. 没有有效 V2 时读取 V1 A/B，将全零项转换为 `NONE`、其余项转换为 V2 `KEYBOARD` entry。
3. 转换后先写入并读回验证新的 V2 槽和 commit，成功后才发布 canonical runtime map；旧 V1 记录保留作为回滚证据，不原地覆盖。
4. V1/V2 都无效时加载产品默认 canonical map。

所有写入继续保持现有顺序：验证 request/token → prepare 非活动槽 → 读回验证 → release 旧动作 → apply RAM → 再次验证 token → commit → 发布 revision/active slot。任一步失败都恢复旧 runtime map，不能留下半发布状态。

硬编码宏关闭后，旧 APP 仍可通过 V1 SET 配置纯标准键盘；V1 GET 的可表示性规则按 6.1 执行。新 APP 优先使用 V2，且必须识别 `MAP_NOT_REPRESENTABLE`、revision conflict、storage error 和 `TEST_MODE_ACTIVE`。

### 6.5 双 Report CCC 存储迁移

当前 HOGP subscription record 只保存 peer 是否订阅，不能表示 Composite 中两个独立 CCC。存储 schema 升级为 `HCS2`，每个 peer entry 保持 8 字节并复用原 reserved byte：

```text
byte0 valid
byte1 subscription_bits  bit0=Keyboard ID1, bit1=Codex ID6
byte2..7 SM peer identity
```

`HCS1` 有效 entry 迁移为 `subscription_bits=KEYBOARD_ID1`；历史实现没有持久化 Codex CCC，因此不得猜测并设置 bit1。任一 CCC 写入只更新自己的 bit 并保留另一 bit；加密完成后分别恢复对应 handle。恢复、禁用、bond reset 和 peer eviction 都必须写后读回验证，并确保一个 peer 的 bitmask 不会应用到另一个 peer。

### 6.6 按键运行规则

| 映射类型 | 条件 | 行为 |
|---|---|---|
| `NONE` | 任意 | 消费事件，不发送报告。 |
| `KEYBOARD` | ID 1 ready | 仅发送标准键盘 down/up。 |
| `CODEX_FAST` | ID 6 ready | 仅发送 ACT06 down/up JSON。 |
| `KEYBOARD` / `CODEX_FAST` | 对应 report 未 ready | 消费事件、记录诊断，不隐式回退到另一种类型。 |

“不隐式回退”是安全与可预测性要求：配置成 Codex 的键在 Codex 不可用时不能变成 Enter、A 或其他普通键盘输入。

## 7. 身份、广告与安全边界

Codex 能力、HID personality 与设备身份必须是三个独立维度：

```text
Capability:  private GATT / keyboard report / Codex report
Personality: keyboard / codex-only / composite
Identity:    product identity / laboratory reference identity
```

实验 Manufacturer、VID/PID、PnP 和其他参考身份字段只能通过明确的 laboratory guard 启用。不得因 Codex 模块存在就自动将参考身份带入产品镜像；发布构建应扫描这些参考字节。产品名称始终走 `BLE_LOCAL_NAME`/`VM_RDX_BLE_NAME`。

### 7.1 广播布局是正式 APP 兼容 ABI

Composite 继续复用三号方案候选 C，以及五号一拖二方案“广播内容保持不变”的既有结论。两个 wrapper 只扩展 Peripheral ACL 容量，不产生第二个产品身份，也不得改变任一空闲 wrapper 发出的字节布局：

```text
Primary Advertising Data:
    Flags 0x0A
    Complete/Shortened Local Name

Scan Response Data:
    RDX Manufacturer Data（必须是第一个 AD Structure）
    Complete 16-bit Service UUID 0x1812
```

当前完整名称下，预期原始结构为：

```text
ADV: 02 01 0A 13 09 42 65 61 6E 73 74 61 6C 6B 20 52 4B 42 ...
RSP: 1A FF <RDX Manufacturer Data> 03 03 12 18
```

统一广播禁止加入 `03 19 C0 03`（Generic HID Appearance `0x03C0`）、Keyboard Appearance `0x03C1` 或其他额外 AD Structure。Appearance 只是连接前的设备类别提示，不启用 HID、Report Map、Report ID、CCC 或 Codex transport；Windows/Codex 仍通过 HID UUID、连接后的 HID Service/Report Map、Vendor Report ID 6 和实验 PnP/DIS 身份完成识别。

该限制不是 31 字节预算优化，而是正式 APP 的外部兼容约束。历史实机已经证明 nRF Connect 能完整显示多种合法 BLE 布局，但正式 APP 可能依赖 Primary ADV 中 `Flags + Name` 的字段数量、顺序、偏移或总长度。2026-07-31 再次复现：仅追加 Generic HID Appearance 即导致正式 APP 不显示，删除后立即恢复。因此不能以“BLE 规范允许”“nRF 可见”或“Manufacturer Data 未改变”为理由扩展广播。

任何后续 Codex、HOGP、身份或双连接修改都必须同时满足：

1. Host contract 明确拒绝 Primary ADV 中的 Appearance，并冻结 Manufacturer Data 在 Scan Response 首项、HID UUID 在其后。
2. 保存 nRF Connect 或等价主动扫描的 ADV/Scan Response 原始字节，不能只检查 `app_ble_adv_enable()` 返回值。
3. 使用正式 RDX APP 验证前台、后台和快速重连发现；通用扫描器成功不能替代正式 APP Gate。
4. 删除 Windows 旧 bond 后重新配对，验证 HID/Codex 识别不依赖历史 Appearance 或 GATT 缓存。
5. 第一条 ACL 建立后，由另一空闲 wrapper 发出的广播必须保持完全相同的候选 C 布局。

HID 动态属性继续要求加密的 Just Works bond，不虚假宣称 MITM。ID 1 和 ID 6 的 CCC intent 都必须按 SM peer identity 分别持久化，不能从一个 peer 泄漏到另一个 peer。

## 8. 分阶段实施

| 阶段 | 状态 | 当前结论 |
|---|---|---|
| Phase 0 | 已完成 | composite/C1 laboratory 基线、身份差异和实机证据已冻结。 |
| Phase 1 | 已完成 | 五键已统一进入 typed router，Key 5 独占 Fast `ACT06`。 |
| Phase 2 | 已通过 | `all` 硬编码 composite 镜像已完成实机功能 Gate。 |
| Phase 3 | **进行中** | 3A 已完成；3B 静态实现与审核已完成，Windows/JL clean 构建和 3B 实机冒烟待补。 |
| Phase 4 | 未开始 | HCS2、双 CCC 持久化、产品身份与授权加固。 |
| Phase 5 | 未开始 | APP/VM V2 typed action map。 |
| Phase 6 | 未开始 | 完整互操作、压力和资源验收。 |

### Phase 0：冻结已识别基线

建立 evidence session，保存已被 Windows Codex 客户端识别的固件 commit、构建时间、`TCFG_RDX_CODEX_MICRO_MODE`、identity guard、`RDX_HOGP_KEY_ACTION_TEST_ENABLE`、完整编译命令、Report Map、ATT 布局、PnP/DIS、ADV/Scan Response、Windows/App/Adapter 版本、HID caps、MTU 和串口 ID 6 trace。

如果无法确认当时是 codex-only/V1 还是 composite/C1，基线状态必须写成“persona unknown”；补做一次可复现的 `all` 新鲜配对，不能把未知 persona 的识别结果当成 composite 证据。

### Phase 1：最小 typed router 垂直切片

- 保持当前 C1 Profile、server、HID/Codex 模块和 APP/VM V1 不动。
- 定义 canonical typed action entry/map 和 `rdx_input_router_click()`。
- 将现有键盘 test map 转成 canonical map；新增 `CODEX_FAST` executor。
- 删除 `rdx_app.c` 中 `num_idx==0` 的 Codex 直接旁路；所有五键只进入 router。
- 用户键 1～5 显式映射到内部 `key_id=0..4`，Codex 固定为 `key_id=4`。
- provider 仍可暂时调用现有 keyboard/Codex API，但 router 必须是唯一上层入口。

### Phase 2：硬编码 composite 实机 Gate（已通过）

- 历史 `codex-c1-keytest` 目标用于隔离验证键 1～4 的 ID 1 与键 5 的 Fast ACT06/ID 6；实机确认功能正常后已经删除。
- 2026-07-29 Fast 版历史 keytest 烧录包 SHA-256 为 `0DAE373DAFB78873AE189627364D4598479E237813E5EE1C62A6C62D3AAE799F`；该值仅作为验证证据，不再是当前构建入口。
- `RDX_HOGP_KEY_ACTION_TEST_ENABLE=1` 已成为当前 `all` 默认；该宏只控制硬编码/APP-VM 映射来源，不控制 identity。
- 首次统一后的 product-identity `all` 已完成 clean 全量构建；`db_update_data.bin` SHA-256 为 `23254101235BF1BD43223C6CEB5D6B99691DB573BC9C29BFA0FB23A4C01FFB1C`。该镜像包含产品 PnP `1234:0001`，实机反馈 Codex Desktop 无法识别，因此不能作为后续连接基线。
- 当前 `all` 已显式恢复 reference Manufacturer `Work Louder` 与 PnP `303A:8360`，同时保留产品名称 `Beanstalk RKB [认证尾码]`。clean 全量构建后的 `db_update_data.bin` SHA-256 为 `0DAE373DAFB78873AE189627364D4598479E237813E5EE1C62A6C62D3AAE799F`；`app.bin` 包含 `Beanstalk RKB`、`ACT06`、`action_map_source=TEST`、`Work Louder` 和 reference PnP，不包含 `AG00`、`Codex Micro` 名称或 product PnP `1234:0001`。2026-07-29 实机确认该 `all` 镜像恢复 Codex Desktop 识别和连接，证明统一入口没有丢失功能，遗漏的是 identity guard；该镜像继续标记为 laboratory image。
- 验证 MTU ≥66、Windows Input/Output Report length=64、两个 CCC 独立 ready。
- 功能 Gate 覆盖新鲜配对、键盘 click、ACT06 down/up、断连和 CCC disable；完整 100 次键盘/ACT06 压力矩阵保留到 Phase 6，最终要求零双发、零 stuck key。
- Gate 未通过时只修垂直链路，不开始 APP V2、VM V2 或大规模文件拆分。

**阶段结论（2026-07-29，广播项于 2026-07-31 修订）**：核心功能 Gate 已通过。`all` 下的 composite HID、产品名称、参考身份、标准键盘、Codex 控制面和 Key 5 Fast `ACT06` 已在同一镜像中完成实机回归。commit `70586c5` 与上述 SHA-256 继续作为 Phase 3 的 Report/RPC/身份/按键行为基线；其 Generic HID Appearance 不属于有效广播基线，已由第 7.1 节候选 C 和 2026-07-31 正式 APP 实测结果覆盖。更长时间、完整 100 次压力矩阵及资源 watermark 仍在 Phase 6 统一验收，不阻塞行为保持的 Phase 3 拆分。

### Phase 3：行为保持的模块拆分（进行中）

Phase 3 从 commit `70586c5` 开始。本阶段的唯一目标是把已验证的垂直链路收敛到清晰的模块边界；不增加用户可见功能，不修改 APP wire protocol 或 VM schema。

#### 3A：Profile 聚合与 handle dispatcher（已完成）

- 新建 `rdx_gatt_profile.c/.h`，使其成为唯一 `rdx_profile_data[]` 和全部 handle range 的 owner。
- 使用编译期 fragment 聚合 RDX private GATT、HID Service 和 DIS，不改变任何展开后的 ATT 字节或 handle。
- 提供按 handle range 的 read/write dispatcher；`rdx_ble_server` 只转交 SDK callback 上下文。

2026-07-30 状态：`rdx_gatt_profile` 已成为 GAP、RDX private、Battery、OTA、HID 和 DIS 全部 handle 常量及唯一 ATT 聚合表的 owner。聚合表由 `rdx_gatt_private_profile.inc`、`rdx_hid_profile.inc` 和 `rdx_dis_profile.inc` 三个编译期 fragment 顺序组成；fragment 重组后的 token 序列与 Phase 2 基线完全一致。read/write dispatcher 在聚合模块内按 handle 选择窄 provider，`rdx_ble_server` 的 SDK callback 仅校验当前连接、构造 callback context 并转交 dispatcher；provider 实现暂留 server，分别在 3B 和 3D 继续迁移。强化后的 host contract 会拒绝 handle 常量回流、route-only 假 dispatcher、callback 内业务 switch 或 fragment 顺序漂移，6 个 host contract 全部通过。

commit `989bff6` 已完成实机冒烟：Windows/Codex Desktop 能识别设备，五个物理按键均能触发各自既定功能。该结果确认 3A 的 Profile 聚合与 handle dispatcher 没有破坏已冻结的识别和按键链路，3A 据此收口。此次反馈未包含可复核的 Windows/JL clean `all` 构建日志，该构建证据仍需在后续 Windows 验证会话中归档，但不再阻塞进入 3B。

#### 3B：HID core 与 keyboard provider（静态实现与审核已完成）

- 新建 `rdx_hid_service.c/.h`，迁移 Protocol Mode、HID Information、Control Point、HID owner、加密、suspend、ID 1/ID 6 runtime CCC、peer identity 和 ready-drop 清理。
- 将 `rdx_hogp_keyboard.c/.h` 收敛为 keyboard provider，只保留 8 字节键盘 Input Report、LED Output Report 和键盘 release。
- HID core 统一暴露 report-scoped `is_ready/notify` API 和 ready-drop 分发，ID 1 与 ID 6 不得共用 ready 标志。

2026-07-30 状态：共享 HID runtime 已迁移到 `rdx_hid_service.c/.h`，包括当前 HID owner wrapper、连接/加密/suspend/Protocol Mode、ID 1 与 ID 6 独立 CCC、peer identity、HCS1 键盘订阅持久化、ATT HID 分发和 ready-drop 生命周期清理。`rdx_hogp_keyboard.c/.h` 现只保留 8 字节键盘 Input Report、LED Output Report、键盘发送/release 和对应 ATT provider。Codex ID 6 发送改用 report-scoped HID core API，仍由当前 HID owner wrapper 发出；HCS1 继续只保存键盘订阅，没有提前实现 Phase 4 的 HCS2 或 Codex CCC 持久化。

静态审核确认 disconnect、加密失效、Suspend 和 CCC disable 均在 ready 状态下降前调用对应 provider cleanup；ATT profile 字节、handle、Report Map、identity、五键映射和 `ACT06` 语义均未改变。该次模块迁移本身未修改 ADV/Scan Response，但随后 2026-07-31 的正式 APP 回归发现 Phase 2 基线已经错误携带 Generic HID Appearance；广播现已独立修正为第 7.1 节候选 C，不能再以旧 Phase 2 字节为冻结依据。跨平台复核曾发现初次迁移丢失了 Phase 2 基线中 `connected`、`encrypted`、`suspended`、`keyboard_notify_enabled` 和 `codex_notify_enabled` 的 `volatile` 限定；现已在 HID core 状态结构中恢复，并新增 `HID_RUNTIME_FLAGS_ARE_VOLATILE` host contract，防止 Windows JL `-flto -Oz` 优化下再次退化。

6 个 host core contract、macOS Clang 对 `rdx_hid_service.c`、`rdx_hogp_keyboard.c`、`rdx_codex_micro.c`、`rdx_input_router.c` 的前端语法检查及 `git diff --check` 已通过。2026-07-31 又在 Windows 使用 `winmk.bat all` 完成 JL 全量编译、链接和打包；移除 Appearance 的镜像已通过正式 APP 发现回归。3B 的 Codex Desktop 识别、五键、ID 6 和断连/CCC 完整冒烟仍待补，通过前不得将 3B 标记为完整完成。

#### 3C：Codex transport、RPC 与产品语义

- 将 `rdx_codex_micro.c/.h` 按现有行为拆为 `rdx_codex_transport`、`rdx_codex_rpc` 和 `rdx_codex_app`。
- transport 仅保留 63 字节 framing、RX 重组、TX FIFO、timeout、token/generation 和 buffer-full 重试。
- RPC 仅保留 cJSON 边界、method allowlist、参数顶层类型校验与 JSON-RPC result/error。
- app 仅保留 `device.status`、`sys.version`、灯光兼容响应和 Fast `ACT06` 产品语义，不直接调用 ATT API。

#### 3D：Server 收口

- `rdx_ble_server.c` 最终只保留 wrapper 创建/释放、广播、连接事件、SDK callback bridge、profile 注册和 dispatcher 调用。
- 删除 server 中的 HID 业务 switch、Codex 知识和 profile 字节，但不在本阶段重写既有 RDX 业务逻辑。
- 保留 `rdx_ble_session` 作为两条 link、owner、token、MTU 和 generation 的唯一运行时真相来源。

#### Phase 3 冻结边界

- 不改变 `rdx_profile_data[]` 展开字节、handle、Report Map、Report Reference、Manufacturer/PnP 或产品名称；ADV/Scan Response 只能保持第 7.1 节候选 C，不继承 Phase 2 中已被实机否决的 Appearance。
- 不改变两 wrapper 拓扑、RDX/HID owner 规则、token/generation 语义、MTU 要求、五键硬编码映射或 `ACT06` JSON。
- 不实现 HCS2、HCS1 migration、product identity A/B、授权策略变更、APP V2、VM V2 或新 Codex action。
- 不新增 `app_ble` wrapper、ATT database、Codex owner、专用 Make target 或 VS Code task。

#### 实施与回归规则

- 严格按 `3A -> 3B -> 3C -> 3D` 实施；每个子阶段可以分成多个小提交，但不得跨阶段夹带功能开发。
- 每次移动都先复制已验证行为和静态数据，再删除旧实现；禁止在同一提交中同时改变模块归属和协议语义。
- 每个小提交运行全部 host contracts 和 clean `all` 构建；子阶段结束时重复 composite 新鲜配对、ID 1 键盘、ID 6 `device.status`、Codex 控制面、Key 5 `ACT06`、断连/重连和双 CCC 独立 ready 冒烟回归。
- 每个子阶段保存 profile/report-map fixture、构建产物 SHA-256、Flash/BSS 差异和关键串口日志；拆分后二进制 hash 可以变化，但协议字节和可观察行为必须不变。

#### Phase 3 退出条件

- `rdx_gatt_profile` 拥有唯一 ATT 聚合表和 handle dispatcher，server 不再内联 profile 字节。
- `rdx_hid_service` 拥有 HID 共有状态与 report-scoped ready/notify 边界，keyboard provider 不理解 Codex、peer 存储或物理键策略。
- Codex transport、RPC 和 app 编译为独立模块，且只通过受限 API 依赖 HID core 和产品服务。
- `rdx_ble_server` 不再包含 Codex RPC、键盘 report 状态或 HID 业务逻辑。
- 全部 host contracts、clean `all` 构建和上述子阶段实机冒烟全部通过，无 handle/report-map/identity 漂移，方可进入 Phase 4。

### Phase 4：Composite 状态与产品身份加固

- 实现 `HCS2` 双 report CCC bitmask、HCS1 migration、bond reset 和不同 peer 隔离。
- 验证两条 ACL 上独立 RDX/HID owner、同 link composite owner、wrong-owner 和 stale generation。
- A/B 测试 laboratory identity 与合法 product identity；一次只改变一个身份字段并保存原始证据。
- 在 product identity 尚未被目标 Codex 客户端识别前，只能宣称 laboratory compatibility，不能进入产品完成定义。
- 冻结产品授权 hook；未认证 RDX owner 的 SET/RESET 必须 fail-closed。

### Phase 5：APP V2 动作映射

- 保持相同的 canonical action-map/runtime router，只把 `RDX_HOGP_KEY_ACTION_TEST_ENABLE` 从 `1` 改为 `0`，恢复 APP V2 + VM 来源。
- 验证宏关闭后没有硬编码映射残留，APP V2 成为唯一配置来源。
- 实现精确的 V2 codec、GET_CAPS、长度上限、status 和 V1 可表示性规则。
- 分配新的 VM IDs，实现 56 字节 V2 A/B record、V1 migration、读回验证和 rollback。
- APP 更新为动作类型 UI，并在缺少 V2 capability 时隐藏 Codex 动作。
- 完成 V1 old APP、V2 new APP、V1 data、V2 data 四组兼容矩阵。

### Phase 6：互操作与压力验证

- Composite 为产品主验证；keyboard/codex-only 只保留为诊断 persona。
- 验证标准键盘、Codex RPC、Fast ACT06、APP V1/V2 keymap 写入和回读同时工作。
- 完成两条 ACL、不同 peer、CCC 恢复、Windows 睡眠/唤醒、蓝牙重启、APP/Codex 客户端重启。
- 对零连接、第一条 ACL 已连接以及任一 ACL 断开恢复三个时机分别抓取候选 C 原始广播，并用正式 RDX APP 完成发现 Gate。
- 记录资源预算、watermark、队列峰值、buffer-full 恢复和发布镜像扫描。

## 9. 验证与验收

### 9.1 构建与静态 contract

每次修改运行：

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File .\tests\host\run_host_tests.ps1
Set-Location .\SDK
.\.vscode\winmk.bat all
```

`all` 是 Makefile、Windows wrapper、VS Code 和 evidence session 的唯一构建入口。当前项目 overlay 让它生成受控的 laboratory reference-identity 镜像；persona 或身份 A/B 仍只能通过受控宏临时执行，不能重新增加常用 VS Code build task。

新增/更新的 host contract 至少覆盖：

- composite Report Map、Report Reference、句柄和 DIS 位移；
- ID 1 与 ID 6 各自的 CCC、加密、HCS2 bitmask、HCS1 migration 与 peer 隔离；
- V2 action-map 的 frame/CRC/enum validation，以及 54/108、55/110、130/131 长度关系；
- V1 GET 可表示/不可表示、V1 SET 转换、V2 status 和统一 revision 域；
- V1 到 56 字节 V2 VM record 的 migration、独立 IDs、A/B 回读和断电恢复；
- map 热更新前 keyboard/Codex release 的顺序；
- 用户键 1～5 到内部 key ID 0～4 的映射、Codex 必须是 key ID 4；
- 每种 `kind` 的唯一分发，禁止直接 Codex 旁路和双发；
- test map 和 APP/VM map 必须进入同一 router/provider 路径；
- test mode 下旧 VM/APP 写入不能覆盖 built-in map；
- 未认证 RDX owner、wrong-owner 和 stale token 的 SET/RESET 必须 fail-closed；
- Codex framing、FIFO、buffer-full、wrong-owner、stale generation fail-closed；
- Primary ADV 只能包含 Flags + Name 且不得包含 Appearance；Scan Response 必须保持 RDX Manufacturer Data 首项、HID UUID `0x1812` 在后；
- 当前 `all` 必须显式记录 experimental guard、reference Manufacturer/PnP 和硬编码宏值；正式发布前必须关闭 reference identity，专用 keytest target 不得重新出现。

### 9.2 Windows 与 APP 手工矩阵

Windows Host 工具必须用 SetupDi/HidP 读取真实 top-level Usage 和 report lengths，以 `WriteFile` 发送 ID 6 Output、以 overlapped `ReadFile` 接收 ID 6 Input；`HidD_GetInputReport` 不能替代 notify。设备侧必须同时记录 MTU exchange、63 字节 write callback、两个 CCC 和 owner/token。PowerShell HIDClass 枚举只能作为辅助证据。

#### 9.2.1 实机案例：可检测但无法连接

2026-07-29 的实机现象为：Codex Desktop 能显示 `Codex Micro`，但提示 `Codex found your Codex Micro but couldn't connect`，随后自动重连。此提示不能直接归因于 BLE、配对、键盘重映射工具或安全软件；“设备发现成功”和“Codex 控制面初始化成功”是两个独立阶段。

本次按以下证据链定位：

1. 串口日志确认连接句柄有效、加密成功、peer identity 已解析、HID owner 已绑定，并且 ID 6 CCC 恢复后 `ready=1`。这证明 BLE/HOGP 链路没有掉线。
2. Windows PnP/HidP 确认设备无问题码，并同时枚举出 Keyboard `Usage 0001:0006` 与 Vendor `Usage FF00:0001` 两个 top-level collection；Vendor Input/Output report length 均为 64。
3. Host HID 探针通过 Report ID 6 写入 `{"method":"device.status","id":1}\n`，固件能返回完整的多片 JSON response。这证明 Output write、63 字节 framing、Input notify、CCC 和 JSON-RPC response 队列均可用。
4. 最后检查 Codex Desktop 日志，而不是继续修改 BLE 参数。日志目录通常位于 `%LOCALAPPDATA%\Packages\OpenAI.Codex_*\LocalCache\Local\Codex\Logs\YYYY\MM\DD\`。关键记录为：

```text
[CodexMicroService] Connecting with HID
[CodexMicroService] Error calling RPC ... method: v.oai.rgbcfg
[CodexMicroService] error while sending lighting config, error: Error: Invalid params
[CodexMicroService] Codex Micro connection failed ... control-plane initialization failed
[CodexMicroService] Disconnecting HID device
```

根因是固件给 `v.oai.rgbcfg` 增加了参考实现不存在的强校验：要求 `ambient` 和 `keys` 同时存在，且各自完整包含 `c/b/e/s`。Codex Desktop 下发的初始化 payload 未满足这组本地附加约束，因此固件返回 `-32602`；客户端收到应用层错误后主动关闭 HID，并约每 10 秒重试。版本字符串 `t2620-mvp` 不是本次失败原因。

修复规则如下：

- `v.oai.rgbcfg`：`params` 是 object 即接受并返回 success，内部字段按可选字段处理；
- `v.oai.thstatus`：`params` 是 array 即接受，元素中的已知字段按能力消费；
- 保留 RPC method allowlist、顶层类型检查、JSON/framing 上限、owner/token 检查和未知方法 `-32601`；
- host contract 必须锁定上述顶层兼容边界，禁止重新引入要求所有灯光字段齐全的校验。

历史 `codex-c1` 实验构建修复并烧录后，Codex Desktop 随即显示已连接。以后遇到相同提示，应按“BLE/HID ready → 原生 ID 6 round trip → Desktop 首个失败 RPC”的顺序缩小范围；只要原生 round trip 已成功，就应优先查应用层错误响应，而不是反复调整广播、配对或 MTU。

#### 9.2.2 名称依赖 A/B 结论

2026-07-29 已在同一台 PC、同一 Codex Desktop `26.721.4979.0` 和同一硬件上完成名称单变量验证。B 组保持 laboratory VID/PID、PnP、DIS、地址、composite Report Map、ATT 表和 RPC 不变，只把广播和 GAP Device Name 从参考名称改为产品默认的 `Beanstalk RKB [认证尾码]`。

B 组烧录包 `db_update_data.bin` 的 SHA-256 为 `B10FA418392F660D9ECE46B6FA1791DED6AF8367754727BF868400FDACDE8593`。实机确认结果：Codex Desktop 能发现设备并显示已连接；标准 HID 键盘功能保留；Codex Micro 控制功能可用。因此当前兼容识别不依赖 `Codex Micro` 设备名称。

实验完成后删除临时名称目标和参考名称宏。当前只保留 `all` 产品入口，名称生成与 `VM_RDX_BLE_NAME` 读写不再由 Codex mode 或 laboratory identity 改变。

收口后的历史 `codex-c1` 构建 SHA-256 为 `F364DD16DFB0A04AEBA48F44C1EA0E7675B7CC55E6400E5AEC052D51E49276A9`。其 `app.bin` 包含 `Beanstalk RKB`，不包含 `Codex Micro`，证明临时参考名称没有残留；该 target 随后已被统一的 `all` 入口替代。

该结论仅适用于 Codex Desktop `26.721.4979.0` 和当前 laboratory identity，不能推导 product VID/PID、Manufacturer 或 PnP 身份同样无关。后续身份收口必须继续按单变量实验分别验证，并且借用的参考身份不得进入正式产品镜像。

#### 9.2.3 Manufacturer/PnP 身份依赖回归结论

2026-07-29 在相同硬件、相同产品名称 `Beanstalk RKB [认证尾码]`、相同 composite Report Map、Fast 映射和 `all` 构建入口下完成身份回归：

| 镜像 | Manufacturer / PnP | SHA-256 | Codex Desktop 结果 |
|---|---|---|---|
| product-identity `all` | `JieLi` / `1234:0001` | `23254101235BF1BD43223C6CEB5D6B99691DB573BC9C29BFA0FB23A4C01FFB1C` | 无法识别 Codex Micro |
| laboratory-identity `all` | `Work Louder` / `303A:8360` | `0DAE373DAFB78873AE189627364D4598479E237813E5EE1C62A6C62D3AAE799F` | 能识别并连接 |

这次回归确认：把专用 Make/VS Code target 合并到 `all` 是可行的；问题来自合并时把 `TCFG_RDX_CODEX_MICRO_TEST_IDENTITY_ENABLE` 与 `TCFG_RDX_CODEX_MICRO_EXPERIMENTAL_BUILD_ENABLE` 一并关闭。产品名称不参与当前兼容识别，但 reference Manufacturer/PnP 组合仍是 Codex Desktop `26.721.4979.0` 的已验证连接条件。

该实验一次同时恢复了 Manufacturer 和 PnP，因此不能声称二者都分别必需。下一阶段必须保持其他字节不变，分别执行“只换 Manufacturer”和“只换 PnP”两个单变量实验。正式产品仍不得借用 reference identity。

#### 9.2.4 验收矩阵

| 场景 | 成功条件 |
|---|---|
| 广播兼容 | 零连接、第一条 ACL 已连接和断连恢复时均保持候选 C；nRF 原始字节无 Appearance；正式 RDX APP 前台、后台和快速重连均可发现。 |
| 基线身份 | evidence 明确记录 persona、commit、宏、PnP/DIS、ADV/Scan Response；persona unknown 不计 composite 成功。 |
| HID/MTU | Input/Output Report length 均为 64，effective ATT MTU ≥66，Host 工具完成多片 round trip。 |
| 硬编码 test map | `RDX_HOGP_KEY_ACTION_TEST_ENABLE=1` 时，键 1～4 仅发 ID 1、键 5 仅发 Fast ACT06/ID 6。 |
| 标准键盘映射 | APP 配置 Ctrl+C 等 usage 后，只出现 ID 1 的预期键盘行为。 |
| Codex 映射 | APP 配置 `CODEX_FAST` 后，只出现 ID 6 ACT06 down/up，绝不产生 ID 1 副作用。 |
| 混合五键 | 五键可以分别设置 keyboard、Codex、disabled，按键逐项符合映射。 |
| 双 CCC | 分别开关 ID 1/ID 6 CCC，只影响对应 provider；重连按 peer 恢复相同 bitmask。 |
| APP 配置 | V1/V2 GET/SET/RESET、不可表示 status、revision conflict、断连重连和 VM 重启后行为一致。 |
| 授权 | 未绑定 APP、非 RDX owner、旧 token 和第二 link 均不能修改 action map。 |
| Codex RPC | `device.status` 在 encrypted + ID 6 CCC ready 后 2 秒内完成；`v.oai.rgbcfg`/`v.oai.thstatus` 接受顶层类型正确的可选字段 payload，控制面初始化无 `-32602`，Codex Desktop 显示已连接。 |
| 产品身份 | 合法 product identity 被目标客户端识别；laboratory identity 成功不能替代此项。 |
| 连接恢复 | 删除配对后新鲜配对、设备回连、Windows 睡眠/唤醒、蓝牙和 Codex 客户端重启均无 stuck key。 |
| 压力 | 100 次多片 status、100 次 Codex Fast click、100 次键盘 click 无丢片、交错、错误 owner 或双发。 |

### 9.3 资源门槛

以 keyboard/C0 基线和最终 composite build 做同工具链对比，并冻结以下初始门槛：

- Flash 增量不超过 32 KiB，BSS 增量不超过 4 KiB；
- 连接态最小空闲堆不少于 12 KiB，且相对基线下降不超过 8 KiB；
- `app_core` 栈剩余不少于 1 KiB，Bluetooth task 栈剩余不少于 512 B；
- ATT callback 单次不超过 1 ms，且零动态分配；
- Codex RX 每 link 不超过 1024 字节；TX 不超过 4 条消息和 2 KiB；
- 记录 RX/TX 峰值、drop count、`APP_BLE_BUFF_FULL` 次数及最终恢复结果。

平台 watermark 若不是字节，报告中必须给出换算方式。任一门槛未达到都不能以“功能可用”替代资源验收。

### 9.4 完成定义

只有以下条件全部满足，才能标记为“RDX 私有 GATT + Composite HID 产品方案完成”：

- Windows Codex 客户端在目标环境稳定识别 composite 设备；
- 识别证据明确来自合法 product identity，而不是 laboratory reference identity；
- Primary ADV/Scan Response 始终符合第 7.1 节候选 C，正式 RDX APP 在双 wrapper 各广播时机均可发现；
- 私有 APP 能发现 V2 capability 并可靠配置全部五个物理键；
- 在 APP 联调前，固定 typed map 已独立证明 ID 1/ID 6 的唯一分发和生命周期清理；
- 每个键按映射只进入一种 report provider；
- 标准键盘 ID 1、Codex ID 6 和私有 GATT 同时存在且无互相干扰；
- V1 legacy 数据可安全迁移，V2 A/B 持久化可在掉电后恢复；
- owner、双 CCC、加密、授权、断连和 generation 在双链路下均 fail-closed；
- MTU、HID caps、资源预算、stack watermark、队列峰值和 buffer-full 恢复均达到门槛；
- 实验参考身份未进入产品输出镜像；
- APP V2 接管动作映射后，`RDX_HOGP_KEY_ACTION_TEST_ENABLE=0` 且 built-in test map 不再是有效配置来源。

## 10. 当前代码落点

- `rdx_ble_server.c`：当前 wrapper、广播、连接事件与 SDK callback 桥接；仍含既有 RDX private provider，后续 3D 继续收口。
- `rdx_ble_session.c/.h`：两 link、owner、token 与 runtime 保护；应继续作为唯一连接真相来源。
- `rdx_gatt_profile.c/.h` + `rdx_*_profile.inc`：唯一 ATT 聚合表、全部 handle 常量和按 range dispatcher。
- `rdx_hogp_profile.c/.h`：Report Map、HID Information 和 profile 静态值；handle 已由 `rdx_gatt_profile.h` 统一拥有。
- `rdx_hid_service.c/.h`：共享 HID state、owner wrapper、加密/suspend、双 runtime CCC、peer identity、HCS1 键盘订阅和 report-scoped ready/notify。
- `rdx_hogp_keyboard.c/.h`：8 字节键盘 Input Report、LED Output Report、键盘发送/release 和 provider ATT 回调。
- `rdx_codex_micro.c/.h`：当前 framing、RPC、Fast ACT06；将拆分为 transport/RPC/app。
- `rdx_hogp_keymap_*.c`：当前 V1 五键标准 HID 映射、A/B VM 与事务；将演进为 V2 typed action map。
- `rdx_app.c`：当前物理键入口；应只调用 `rdx_input_router_click()`。

## 11. 非目标

- 不把 Codex 变成第二套 `app_ble` wrapper 或独立 ATT 数据库。
- 不新增 Codex 专属 link owner。
- 不允许 APP 注入任意 Codex RPC、文件操作或系统命令。
- 不在运行时改变 Report Map、ATT 表或身份来切换模式。
- 不将实验参考身份、商标或未公开协议细节作为量产承诺。
- 不让硬编码 test map 绕过 canonical action map、input router 或 report provider。
- 不为 `RDX_HOGP_KEY_ACTION_TEST_ENABLE` 的取值重新增加专用 Make target 或 VS Code task。
- 当前未验证的新 Codex action 不进入 APP capability 列表。
