# 本地录音播放调试记录

## 1. 背景与目标

目标是在设备端读取 SD 卡中的 RDX 本地录音文件，通过 JLStream 解码后从 DAC 播放，并支持按键切换录音。

相关文件：

- `SDK/apps/common/third_party_profile/rdx_protocol/rdx_playback.c`
- `SDK/audio/framework/plugs/source/source_dev0_file.c`
- `SDK/audio/interface/player/dev_flow_player.c`
- `SDK/audio/framework/nodes/effect_dev2_node.c`
- `SDK/audio/st_opus_enc/opus_st_enc_demo.c`

调试日志：

- `录音的全流程日志.log`
- `播放本地录音文件日志.log`
- `最新播放本地录音日志.log`

## 2. 最初的播放方案

最初方案新增了 `rdx_playback.c`，整体流程如下：

1. 按键发送 `APP_MSG_REC_NEXT`。
2. 从 UXFILE/DAT 缓存中查找录音 SN 和文件名。
3. 打开 `storage/sd0/C/<filename>.raw`。
4. 调用 `dev_flow_player_open(1, NODE_UUID_SOURCE_DEV0)` 启动单声道播放流。
5. 定时从文件读取裸 Opus 数据，写入 Source_Dev0 的环形缓冲区。
6. Source_Dev0 每次向 Decoder 提供 40 字节数据。
7. 文件读取完并且环形缓冲区排空后关闭播放器。

该方案还增加了：

- 播放与录音、文件同步、文件传输之间的互斥检查。
- 非阻塞数据泵和尾部排空逻辑。
- 上一首、下一首及自动切换框架。
- `source_dev0_get_free_space()` 和 `source_dev0_is_empty()` 辅助接口。

## 3. 第一轮日志现象

第一份播放日志表明基础播放链路可以正常运行：

- 文件打开成功：`da573.raw, flen=67680`。
- JLStream 创建成功。
- Source、Decoder、DAC 格式协商成功。
- DAC 正常启动。
- 文件的 `67680` 字节全部读取并写入 Source_Dev0。
- 没有文件读取错误或播放器启动错误。

但是播放无正常声音，且播放端协商为：

```text
source  : 16000 Hz, 1 ch, 16 bit
decoder : 16000 Hz, 1 ch, 16 bit
dac     : 16000 Hz, 1 ch, 16 bit
```

当时首先怀疑录音格式和播放参数不一致。

## 4. 录音格式分析

录音日志给出了明确的编码参数：

```text
formate = 2
sr = 16000
br = 32000
ch = 2
fs = 20
```

其中 `formate = 2` 对应 `RECORD_FORMATE_OPUS_16K_STERO`。保存到 DAT 中的 `opus` 字段为：

```text
1093142528 = 0x41280800
```

按 RDX 协议位域解析，同样表示：

- 采样率：16 kHz
- 声道数：2
- 帧时长：20 ms
- 码率：32 kbps

按固定码率计算，每个编码帧大小应为：

```text
32000 bit/s / 8 * 20 ms = 80 bytes
```

而最初播放方案存在三处不一致：

1. 使用 `dev_flow_player_open(1, ...)`，选择了单声道流程。
2. Source_Dev0 声明为普通 `AUDIO_CODING_OPUS`。
3. 文件读取和 Source_Dev0 输出都按 40 字节对齐。

## 5. 第一次修正方案

根据以上分析，第一次修正包括：

1. 在 `t2620_project_config.h` 中启用 `TCFG_DEC_STENC_OPUS_ENABLE`。
2. 播放时使用 `dev_flow_player_open(2, NODE_UUID_SOURCE_DEV0)`。
3. 将播放文件的帧对齐从 40 字节改为 80 字节。
4. 将 Source_Dev0 最大输出缓存从 40 字节扩展到 80 字节。
5. Source_Dev0 只有在缓冲区中存在完整帧时才向 Decoder 输出。
6. 双声道模式声明为：

```text
coding_type = AUDIO_CODING_STENC_OPUS
sample_rate = 16000
channel_mode = AUDIO_CH_LR
frame_dms = 200
bit_rate = 32000
```

7. 保留 Source_Dev0 的普通单声道 Opus 路径，避免影响其他调用场景。
8. 补充 `rdx_playback.h` 引用和 `rdx_playback_init()` 初始化。

主机软件测试全部通过，固件由人工编译确认可以通过。

## 6. 第二轮测试结果

最新播放日志确认第一次修正已经进入运行固件：

```text
source_dev0 NODE_IOC_SET_PRIV_FMT 2
source  : 16000 Hz, 2 ch, 16 bit
decoder : 16000 Hz, 1 ch, 16 bit
dac     : 16000 Hz, 1 ch, 16 bit
```

同时确认：

- `opus_stenc_dec_plug` 已进入 `sdk_used_list.used`。
- 文件仍然可以完整读取和排空。
- 没有 `dec_err`、文件读取失败或 DAC 启动失败。
- 听感从无正常声音变成明显杂音，问题仍未解决。

Decoder/DAC 最终为单声道不一定是错误。单耳设备或当前输出流程可能会将双声道 Opus 下混为单声道。真正异常的是：解码器没有报格式错误，但输出内容是杂音。

另外，两轮播放测试使用的都是：

```text
storage/sd0/C/da573.raw
flen = 67680
```

因此最新日志仍在播放同一个旧文件，不能证明重新录制后的数据是否正确。

## 7. 历史判断修正：本链路不是交织 PCM

早期分析只根据 JLStream 的常见布局，把 `EffectDev2` 输入假定为 `LRLR...` 交织 PCM，并参考
`opus_st_enc_demo.c` 增加了左右声道拆分。后续检查实际生产者 `source_dev1_file.c` 后确认，这个
假定不适用于当前录音链路。

`Source_Dev1` 已经明确将两个缓存组装为：

```text
L0 L1 ... L319 R0 R1 ... R319
```

这说明偶/奇样点拆分会改变录音内容。该拆分版本已经由设备实测确认从“纯杂音/无法辨识”进步到
“可以大致听清 DAC 录音”，是重要阶段性进展；但首要目标仍是正常声音，因此继续按实际生产者
布局修正为平面直传，并通过新录音验证。最终方案见第 12 节。

## 8. 当前结论

目前可以排除：

- SD 卡未挂载或文件打不开。
- 播放器接口完全不可用。
- DAC 未启动。
- 文件没有被完整送入解码器。
- Stereo Opus Decoder 插件未链接。

当前最可能的主因是：

> 录音端将交错双声道 PCM 直接按内存前后两半传给要求平面声道输入的 Stereo Opus 编码器，导致编码内容失真。

第一次修正解决了播放端的 codec 类型、通道数和 80 字节帧边界问题，但无法修复已经错误编码的旧 `.raw` 文件。

## 9. 下一步修改与验证

下一步应修改 `audio_effect_dev2_run()`：

1. 将每个 20 ms、320 点的双声道交错 PCM 拆成独立 L/R 缓冲区。
2. 使用拆分后的两个 `s16 *` 指针调用 `run_wb()`。
3. 增加有限次数的调试日志，打印 `indata_len` 和 `encLen`，确认每次输入 1280 字节 PCM、输出 80 字节 Opus。
4. 删除旧的 `da573.raw`，重新录制一个已知时长的新文件。
5. 记录新文件名、录音时长、文件大小和播放时长。

建议的验证关系：

```text
每秒 Opus 数据量 = 32000 / 8 = 4000 bytes
10 秒录音文件大小约为 40000 bytes
单帧输入 PCM = 16000 * 20 ms * 2 ch * 2 bytes = 1280 bytes
单帧输出 Opus = 80 bytes
```

新录音播放时应满足：

- Source 协商为 `16000 Hz, 2 ch`。
- 无 `dec_err`。
- 文件读取量和写入量等于文件长度。
- 播放时长接近实际录音时长。
- 人声清晰，不再是杂音。

## 10. 无关或次要日志

以下日志不是本次杂音问题的直接根因：

- `dev online aready, err` / `sd add fail`：SD 已经在线时重复调用 `dev_manager_add()`。
- `flash info not found`：启动阶段 Flash 信息提示。
- `tone_player_faild`：提示音资源问题。
- `timer_no_response`：同步文件访问或停止流程造成的短时任务延迟，需要后续优化，但不是 Opus 内容变成杂音的直接原因。

## 11. 2026-07-13 实际修正同步

根据 `最新录音日志.log` 和 `最新播放录音文件日志.log` 重新核对后，最新现象比第 6 节记录的旧现象更明确：

```text
[PB]open ok: storage/sd0/C/aeee28.raw, flen=38880
source_dev0 NODE_IOC_SET_PRIV_FMT 2
source  : 16000 Hz, 2 ch, 16bit
decoder : 16000 Hz, 1 ch, 16bit
dac     : 16000 Hz, 1 ch, 16bit
[JLSTREAM]dec_err: 40
[JLSTREAM]dec_end: 0
```

`aeee28.raw` 的大小为 `38880` 字节，按 80 字节 stereo Opus 帧计算是 `486` 帧，约 `9.72` 秒；播放却在约 0.2 秒内结束。因此当前最新失败不是“文件完整播放但杂音”，而是解码器一开始就报错并结束。

### 11.1 播放端直接原因

当前工作区实际源码中，`source_dev0_file.c` 的双声道分支仍声明为普通 Opus：

```c
fmt->coding_type = AUDIO_CODING_OPUS;
```

同时 `t2620_project_config.h` 没有定义 `TCFG_STENC_OPUS_ENABLE` 和 `TCFG_DEC_STENC_OPUS_ENABLE`，生成的 `sdk_used_list.used` 中也只有：

```text
opus_dec_plug
opus_encoder_plug
```

没有：

```text
opus_stenc_plug
opus_stenc_dec_plug
```

这会导致 80 字节 stereo Opus 裸帧被送进普通 Opus 解码器。普通 Opus 路径通常按单声道/40 字节帧配置工作，因此日志出现 `decoder : 16000 Hz, 1 ch` 和 `dec_err: 40` 是合理结果。

`TCFG_DEC_OGG_OPUS_ENABLE` 不能替代 stereo Opus 解码插件。它用于普通 OGG/raw Opus 配置，不能让 JLStream 自动选择 `AUDIO_CODING_STENC_OPUS` 对应的 decoder。

### 11.2 本次已修改内容

本次修改包括三处：

1. `SDK/apps/earphone/include/t2620_project_config.h`

启用 stereo Opus encoder/decoder 插件注册：

```c
#ifndef TCFG_STENC_OPUS_ENABLE
#define TCFG_STENC_OPUS_ENABLE                    1
#endif

#ifndef TCFG_DEC_STENC_OPUS_ENABLE
#define TCFG_DEC_STENC_OPUS_ENABLE                1
#endif
```

2. `SDK/audio/framework/plugs/source/source_dev0_file.c`

双声道播放时上报 stereo Opus coding type：

```c
if (hdl->ch_num == 2) {
    fmt->coding_type = AUDIO_CODING_STENC_OPUS;
    fmt->channel_mode = AUDIO_CH_LR;
    fmt->bit_rate = OPUS_STEREO_BIT_RATE;
}
```

3. `SDK/audio/framework/nodes/effect_dev2_node.c`

首次上机版本按交织 PCM（`LRLR...`）拆分后，已经能听到 DAC 内容，但仍有明显噪声和变形。
继续追踪实际生产者后确认，`Source_Dev1` 已经输出 `L[320]+R[320]`，本轮改为平面直传：

```c
s16 *pcm_l = tst_buf_LR;
s16 *pcm_r = tst_buf_LR + FRAME_POINT;

memcpy(pcm_l, indata, FRAME_SIZE);
memcpy(pcm_r, indata + FRAME_POINT, FRAME_SIZE);

ret = hdl->opus_stenc->run_wb((void *)run_buf, pcm_l, pcm_r, (u8 *)outdata, &encLen);
```

设备结果从纯杂音进步为可以大致听清 DAC 录音，证明修改 Stereo Opus 编码链路是有效突破。
平面直传属于该方向上的二次修正，需要重新录制文件后同时用 BLE APP 和耳机本地验证。

### 11.3 验证状态

已通过主机检查：

```powershell
powershell -ExecutionPolicy Bypass -File .\tests\host\run_host_tests.ps1
```

结果：

```text
All 2 host software tests passed.
```

固件构建尚未在命令行完成验证。后续已找到 JL 自带的 make：

```text
C:\JL\mc\bin\make.exe
```

但直接执行时，JL Studio 的辅助命令目录未进入 PATH，构建停在：

```text
process_begin: CreateProcess((null), mkdir_win -p ..., ...) failed
make (e=2): 系统找不到指定的文件。
```

这属于命令行构建环境缺失，并非 C 编译错误。后续需要通过 JL Studio 或补齐其构建环境重新编译。
构建后应确认生成的 `SDK/apps/earphone/sdk_used_list.used` 至少包含：

```text
opus_stenc_plug
opus_stenc_dec_plug
```

### 11.4 设备复测要求

复测时不要继续用旧的 `da573.raw` 或本次修复前录出的 `aeee28.raw` 判断最终音质。正确流程是：

1. 重新编译并烧录固件。
2. 开机后重新录一条新文件。
3. 播放新录音文件。
4. 检查播放日志：

```text
source_dev0 NODE_IOC_SET_PRIV_FMT 2
source  : 48000 Hz, 2 ch
无 dec_err
播放时长接近 文件大小 / 4000 bytes/s
```

例如 10 秒录音应约为 `40000` 字节，播放时长也应接近 10 秒。

## 12. 2026-07-13 可听但变形问题分析与二次修正

### 12.1 新日志结论

新录音 `8cac51.raw` 大小为 `18880` 字节：

```text
18880 / 80 = 236 帧
236 * 20 ms = 4.72 秒
```

录音端日志参数正确：

```text
sr : 16000, br : 32000, ch : 2, fs : 20
```

本地播放已不再出现 `dec_err: 40`，说明 stereo Opus 插件注册和路由修正有效。但播放从
`00:04:32.200` 持续到 `00:04:46.358`，约 `14.16` 秒，恰好是理论时长 `4.72` 秒的 3 倍。

这说明当前问题不是文件读取慢，也不是文件尾判断错误，而是解码 PCM 的采样率被错误解释。

### 12.2 本地播放三倍慢放的原因

通过固件反汇编中的 `opus_dec.c` 可以确认，JL 的 `opus_stenc_dec_plug` 对 stereo Opus 固定输出
`48000 Hz` PCM。此前 `Source_Dev0` 对 stereo 文件仍上报 `16000 Hz`，导致整个播放流程和 DAC
按 16 kHz 协商。每个 20 ms Opus 包解出 960 个 48 kHz 样点，却被 DAC 按 16 kHz 播放：

```text
960 / 48000 = 20 ms   // 正确
960 / 16000 = 60 ms   // 当前本地播放
```

因此出现精确的三倍时长、音调降低和声音变形。BLE APP 使用自己的 Opus 解码器，通常正确按
48 kHz 输出，所以同一个文件在 APP 和耳机本地听起来明显不同。

修正方式是在 stereo 分支对播放流声明解码输出采样率 48 kHz：

```c
fmt->sample_rate = 48000;
fmt->coding_type = AUDIO_CODING_STENC_OPUS;
fmt->frame_dms = 200;
fmt->bit_rate = 32000;
```

这里的 `48000` 是 decoder/DAC 侧 PCM 采样率；录音编码输入仍是 16 kHz，不改变文件码率和
每帧 80 字节的格式。

### 12.3 BLE APP 变形原因与本轮修正

`Source_Dev1` 在双声道模式下明确组装的是平面 PCM：

```text
L[320] + R[320]
```

当前已验证版本把它当成 `LRLR...` 交织 PCM，再按偶数/奇数样点拆声道。如果此处观察到的
平面布局在运行时保持不变，实际编码会变成：

```text
编码左声道 = 原左声道偶数样点
编码右声道 = 原左声道奇数样点
原右声道数据没有被正确使用
```

这可以解释为什么 BLE APP 正确解码后仍会听到变形。本轮正式应用平面直传：前 320 点作为左声道，
后 320 点作为右声道传给 `run_wb()`。由于录音文件内容会变化，必须重新录新文件验证，不能继续
用 `8cac51.raw` 判断本次录音端修正效果。

### 12.4 双通道缓存稳定性修正（本轮已应用）

新录音日志中大量 `M` 表示 `Source_Dev1` 缓存写不完整，大量 `W` 表示编码线程被唤醒时任一
声道还不足 20 ms。双声道如果只检查左声道长度，可能读取失败或产生长期积压。

本轮同时实施：

1. 只有 MIC 和 DAC 两个缓存都达到 `640` 字节时，才组装一帧 stereo PCM。
2. 数据写入后，一旦两路都满足一帧就主动唤醒编码线程。
3. 每路缓存从约 4 帧扩大到 8 帧，用于吸收两个生产者的短时调度抖动。
4. 去掉逐帧 `M/W/B` 字符刷屏，停止录音时统一打印统计：

```text
source_dev1 stats: frames=... wait=... drop=mic/dac remain=mic/dac
```

复测时 `drop=0/0` 是目标；`frames * 80` 应与最终文件大小基本一致。若 drop 不为 0，日志能够
直接区分是 MIC 还是 DAC 输入发生积压。

### 12.5 本轮完整方案复测

本轮同时修正录音声道排列、双通道缓存同步和本地播放采样率。必须重新录制一条新文件，再分别
通过 BLE APP 和耳机本地播放。旧的 `8cac51.raw` 仍可用于单独验证 48 kHz 播放时长，但不能用于
判断新的录音质量。

录音端预期：

```text
sr : 16000, br : 32000, ch : 2, fs : 20
source_dev1 stats: ... drop=0/0 ...
文件大小约为 4000 bytes/s
```

本地播放端预期：

```text
source_dev0 NODE_IOC_SET_PRIV_FMT 2
source  : 48000 Hz, 2 ch
decoder : 48000 Hz, 1 ch
dac     : 48000 Hz, 1 ch
无 dec_err
播放时长约为 文件大小 / 4000 秒
```

任意新文件的预期播放时长约为 `文件大小 / 4000` 秒。BLE APP 和耳机本地都应能辨识同一段内容，
且速度、音调基本一致。

### 12.6 已确认的阶段性进展与修改边界

本次设备实测必须保留为重要里程碑：

1. 之前版本表现为纯杂音或解码立即失败，无法辨识实际录音内容。
2. 启用 stereo Opus 编解码插件、修正播放路由，并首次修改 `effect_dev2_node.c` 后，已经可以
   大致听清 DAC 录音，不再是纯杂音。
3. 在 `8cac51.raw` 阶段，剩余问题是噪声较多、声音变形，以及耳机本地与 BLE APP 听感不同；
   该结论已被后续 `a171c0.raw` 复测更新，见第 13 节。
4. 首要目标是恢复正常声音，因此本轮继续修改 `EffectDev2` 和 `Source_Dev1`；音质参数优化放在
   声音速度、声道和基本清晰度恢复之后。
5. 当前完整方案为：`EffectDev2` 平面直传、`Source_Dev1` 双路同步与统计、`Source_Dev0` 48 kHz
   播放声明。三项已在后续新固件和新录音中完成首轮设备验证，结果见第 13 节。

## 13. 2026-07-13 当前 Git Diff 阶段结论（以最新设备日志为准）

本节是当前阶段的汇总结论。前文记录了逐步排查过程，其中部分中间判断已被后续源码检查和设备
日志修正；后续评审和优化应优先以本节为准。

### 13.1 最终结论

本地录音从“无法播放/纯杂音”发展到“可以辨识但速度和声音变形”，并不是一个单点问题，而是
以下问题叠加：

| 层级 | 原因 | 直接表现 | 当前解决方案 |
|---|---|---|---|
| 播放入口 | 原工程没有完整的本地录音播放器和持续数据泵 | 按键无法稳定选择、读取和完整播放文件 | 新增 `rdx_playback.c/.h`，接入按键消息、文件列表、定时数据泵和播放生命周期 |
| Codec 路由 | 80 字节双声道 Opus 包曾被按普通单声道 Opus 处理 | 启动后立即 `dec_err: 40` | 启用 `opus_stenc_plug`/`opus_stenc_dec_plug`，双声道 Source 使用 `AUDIO_CODING_STENC_OPUS` |
| PCM 排列 | `Source_Dev1` 已输出 `L[320]+R[320]`，中间版本却按 `LRLR...` 再拆分 | BLE APP 和本地都能听出内容，但声音变形 | `EffectDev2` 直接把前后两个 320 点平面声道传给 `run_wb()` |
| 双路同步 | MIC/DAC 两个缓存到达时机不同，旧逻辑只充分检查一路 | 缺声道、缓存积压、噪声或不连续 | 两路都满 640 字节才编码，主动唤醒编码线程，缓存扩大到 8 帧并统计 drop |
| 播放采样率 | JL stereo Opus decoder 固定输出 48 kHz，播放流曾声明 16 kHz | 播放时长约为正常值 3 倍，音调和声音明显变形 | 双声道播放流声明 48 kHz，使 decoder 和 DAC 都按 48 kHz 工作 |
| 播放收尾 | 文件读完后立即关闭会截断 Source 缓存中的尾部数据 | 文件尾不完整或播放提前结束 | EOF 后等待 Source_Dev0 缓存排空，再关闭播放流并自动切换下一条 |

因此，当前已经确认的核心格式为：

```text
录音输入 PCM : 16000 Hz, 2 ch, 16 bit, 每声道 320 点/20 ms
Opus 文件     : stereo Opus raw, 80 bytes/20 ms, 32000 bit/s
文件速率      : 4000 bytes/s
解码输出 PCM : 48000 Hz（JL stereo Opus decoder 固定输出）
本地 DAC      : 48000 Hz, 1 ch
```

### 13.2 当前 Git Diff 中各修改的作用

1. `rdx_playback.c/.h`、`rdx_app.c`、`Makefile`

建立本地播放功能，并把 `APP_MSG_REC_NEXT` 接到 `rdx_playback_next()`。播放器按 80 字节边界读取，
以 560 字节块向 Source_Dev0 投递；同时处理文件列表刷新、录音/文件传输互斥、EOF 排空、自动下一条
和错误文件跳过。

2. `t2620_project_config.h`、`sdk_used_list` 生成链路

启用 stereo Opus encoder/decoder 插件，确保最终固件包含：

```text
opus_stenc_plug
opus_stenc_dec_plug
```

3. `effect_dev2_node.c`

修正编码输入指针类型和长度检查，并按照 Source_Dev1 的实际平面布局传入左右声道：

```text
pcm_l = indata[0 ... 319]
pcm_r = indata[320 ... 639]
```

4. `source_dev1_file.c`

MIC 和 DAC 两路各自使用 8 帧缓存；只有两路均达到 20 ms 才输出一帧。缓存满时不再部分写入，
避免半帧破坏样点边界；停止时输出 `frames/wait/drop/remain` 统计。

5. `source_dev0_file.c`

双声道文件固定按每帧 80 字节投递，使用 stereo Opus coding type，并将播放 PCM 采样率声明为
48 kHz。新增可写空间和缓存空状态接口，供播放器实施背压和 EOF 排空。

6. `lib_media_config.c`

当前把 OGG/raw Opus CBR 包长配置为 80 字节。该配置随当前 diff 保留，但实际本地双声道播放的
核心路由是 `AUDIO_CODING_STENC_OPUS`，不是普通 `AUDIO_CODING_OPUS`。

### 13.3 最新录音日志验证

最新文件为 `a171c0.raw`，最终大小 `34240` 字节：

```text
34240 / 80 = 428 帧
428 * 20 ms = 8.56 秒
34240 / 4000 = 8.56 秒
```

编码参数符合预期：

```text
sr : 16000, br : 32000, ch : 2, fs : 20
```

录音期间没有 `enc err` 或 `len err`。停止统计为：

```text
source_dev1 stats: frames=439 wait=2094 drop=2/2 remain=0/0
```

解释：

- `frames=439`：Source_Dev1 一共向后级提供了 439 个 PCM 帧。
- `wait=2094`：数据流查询时两路尚未同时凑齐一帧的次数，不表示丢了 2094 帧。
- `drop=2/2`：MIC 和 DAC 各有两次完整写入因缓存满而放弃，数量很小且对称。
- `remain=0/0`：停止时两个输入缓存均已清空。

`frames=439` 与文件中的 428 帧相差 11 帧，约 220 ms。日志时序显示文件完成最终写入后，
Source_Dev1 才停止；这批差异主要属于停止阶段仍产生但未进入文件的尾帧，不是播放过程丢帧。

录音日志末尾的 `dec_err: 40` 出现在 `tone_player: tone_en/power_off.*` 之后，属于关机提示音解码，
与 `a171c0.raw` 的录音编码无关。

### 13.4 最新播放日志验证

播放器正确打开新文件：

```text
[PB]open ok: storage/sd0/C/a171c0.raw, flen=34240
```

格式协商已全部恢复到正确采样率：

```text
source  : 48000 Hz, 2 ch, 16bit
decoder : 48000 Hz, 1 ch, 16bit
dac     : 48000 Hz, 1 ch, 16bit
```

文件从 `00:01:09.013` 播放到 `00:01:17.642`，实际约 8.629 秒；理论值为 8.56 秒，误差约
0.8%，属于启动、DAC 缓存和淡出的正常开销。数据完整性也符合预期：

```text
[PB]drain complete: cbuf empty
[PB]eof: SN=2, read=34240, written=34240
```

最新播放日志没有 `dec_err`。结合设备听感“明显改善、似乎已无变形”，可以判定：

- stereo Opus decoder 路由正确；
- 48 kHz 三倍慢放问题已解决；
- 文件读取、背压和 EOF 排空正确；
- PCM 平面排列修正有效，之前的持续声音变形已经基本解决。

日志只能证明格式、时长、错误状态和数据完整性，不能单独量化底噪、频响或轻微削波；这些需要
固定测试音源和 PCM/声学采样进一步确认。

### 13.5 剩余优化项

按优先级建议如下。

#### P1：录音连续性

1. 将 `drop=2/2` 优化到 `0/0`。先用 30 秒、1 分钟和 5 分钟录音观察 drop 是否随时长增长。
2. 如果仍发生缓存满，优先检查编码线程唤醒和 SD 写入阻塞；必要时再扩大缓存。
3. 如果必须丢数据，应成对丢弃 MIC/DAC 同一时间段，避免两个声道时间轴逐渐错位。
4. `wait=2094` 当前不影响正确性，但可减少无效轮询，降低线程调度和日志干扰。

#### P1：停止阶段尾帧

当前 Source_Dev1 比文件多产生约 11 帧。更完整的停止顺序应为：

```text
停止 MIC/DAC 生产者
-> 排空 Source_Dev1
-> 排空 EffectDev2/文件写缓存
-> 关闭文件并保存 DAT
```

优化后应使 `source frames * 80` 与文件大小更接近，同时避免录音末尾少约 200 ms。

#### P1：重复初始化和 SN 消耗

一次录音启动日志先创建了 SN=1 `5a608e.raw`，随后又创建实际保存的 SN=2 `a171c0.raw`；最终
文件列表只有一条记录。这说明录音初始化路径仍可能执行两次。应检查 `rdx_record_run_init()` 和
录音任务消息的调用时序，避免空文件、孤立文件或 SN 跳号。

#### P2：Codec 配置收敛

确认 stereo 播放完全由 `AUDIO_CODING_STENC_OPUS` 处理后，可单独验证以下配置是否仍有必要：

```text
TCFG_DEC_OGG_OPUS_ENABLE
CONFIG_OGG_OPUS_DEC_SET_CBR_PACKET_LEN = 80
```

它们属于普通 OGG/raw Opus 全局配置，可能影响其他 Opus 使用场景。不要在当前稳定版本中直接删除，
应使用同一文件做启用/关闭 A/B 验证后再收敛。

#### P2：剩余音质

核心格式稳定后，再使用同一段固定测试音分别做 MIC-only、DAC-only、MIC+DAC 三组录音，以定位
剩余噪声来自输入源、AGC/DNS、模拟增益还是 Opus 32 kbps。未完成隔离前不建议同时修改码率、
增益和降噪参数，否则无法判断是哪项产生改善或回退。

#### P3：可靠性回归

补充以下设备回归：连续录音 5 分钟、连续播放全部文件、播放中切换上一条/下一条、录音与播放
互斥、文件传输期间禁止播放、文件损坏/非 80 字节对齐时跳过，以及多次录音后重启再播放。

### 13.6 当前阶段判定

当前版本已经达到“本地录音文件可完整解码、速度和音调正确、没有持续变形”的核心目标。剩余
工作属于录音尾帧、极少量缓存 drop、重复 SN 和音质精调，不需要推翻现有 stereo Opus 方向。

### 13.7 为什么增加本地播放还修改了录音端

本地播放功能和录音端修正是两件相互独立的事情。理论上，如果现有录音文件内容完全正确，新增
本地播放只需要实现文件选择、数据泵、decoder 路由和 DAC 输出，不需要修改录音编码链路。

本项目之所以同时修改录音端，是因为排查本地播放时发现原有 `.raw` 文件虽然尺寸和 80 字节帧
结构有效，但编码前 PCM 排列及 MIC/DAC 双路同步存在问题。这类问题已经写进文件，所以即使 BLE
APP 使用正确的 Opus decoder，也可能听到声音变形。BLE APP 与耳机本地听感都发生变化，进一步
证明录音内容本身也是问题的一部分，而不只是新增播放器的问题。

#### 录音端修改产生的实际影响

1. 新录音的音频内容发生变化

`EffectDev2` 现在按照 `Source_Dev1` 的实际平面布局编码：

```text
输入布局：L[320] + R[320]
pcm_l    = indata[0 ... 319]
pcm_r    = indata[320 ... 639]
```

这会改变新生成 Opus 包中的音频样本，但目的是恢复正确声道，而不是改变协议格式。

2. 文件格式和 BLE 协议保持兼容

录音格式仍然是：

```text
16000 Hz, 2 ch, 16 bit PCM input
20 ms/Opus frame
80 bytes/Opus frame
32000 bit/s
headerless stereo Opus raw
```

因此 BLE APP 的文件传输协议、文件长度计算和 Opus 解码方式不需要改变，文件速率仍约为
`4000 bytes/s`。

3. 只影响重新录制的新文件

固件升级不会重写存储中的旧 `.raw` 文件。旧文件里已经存在的声道排列或缺帧问题会继续保留；
只有新固件重新录制的文件才使用平面直传和双路同步方案。评估本次录音端修正时必须使用新文件。

4. MIC/DAC 同步策略发生变化

双声道模式下，只有 MIC 和 DAC 两路都达到 640 字节（每路 320 点、20 ms）才生成一个 Opus 帧。
这避免一路不足时仍编码零数据或不完整数据。潜在影响是：如果某一路长期没有输入，编码会等待，
而不是继续生成缺少该声道的帧；当前 RDX 的 MIC+DAC stereo 录音场景要求两路都有效。

5. 缓存满时改为放弃完整写入块

旧逻辑可能只写入 PCM 块的一部分，容易破坏样点边界和后续声道时间轴。新逻辑在剩余空间不足时
放弃整个输入块并增加 drop 计数。这样故障边界更明确，但 drop 仍可能造成短暂缺口或噪点；最新
日志中的 `drop=2/2` 需要继续优化到 `0/0`。

6. RAM 占用增加

原来每路缓存约 2560 字节，两路合计约 5120 字节；当前每路缓存约 5120 字节，两路合计约
10240 字节，因此额外占用约 5 KB heap。正常情况下不会固定增加 160 ms 延迟，因为达到一帧就会
开始编码；8 帧只是最大缓冲能力。长时间录音仍需观察堆余量、碎片和稳定性。

7. 新增统计不改变音频格式

`frames/wait/drop/remain` 只用于判断录音连续性：

```text
source_dev1 stats: frames=439 wait=2094 drop=2/2 remain=0/0
```

其中 wait 是等待两路同时就绪的查询次数，不等于丢帧；drop 才表示实际未写入的输入块。

#### 播放端修改不会反向改变录音文件

以下修改只作用于耳机本地播放，不会改变 BLE APP 下载到的文件：

- `Source_Dev0` 按 80 字节边界读取；
- 双声道选择 `AUDIO_CODING_STENC_OPUS`；
- decoder/DAC 按 48 kHz 输出；
- 播放器实施缓存背压和 EOF 排空。

因此，当前方案可以分成两个独立目标：

```text
录音端：保证写入文件的 PCM 声道、帧边界和 MIC/DAC 时间关系正确
播放端：保证既有 80 字节 stereo Opus 文件被完整、按正确采样率解码
```

最新设备结果表明两部分修正共同生效：文件格式保持兼容，本地播放时长正确，BLE APP 和本地均
不再出现之前的持续声音变形。后续应优化 drop、停止尾帧和 RAM 余量，而不是恢复旧的录音排列。
